=====================Reduction starts.======================

The initial program size is 447
The command line options are:
---
helpFlags:
  groupName: "Help"
  help: false
verbosityFlags:
  groupName: "Verbosity"
  verbosity: "INFO"
  listVerbosity: false
versionFlags:
  groupName: "Version"
  version: false
inputFlags:
  groupName: "Inputs"
  testScript: "predict.sh"
  inputFile: "Main.java"
  deps: []
  sourceFile: "Main.java"
resultOutputFlags:
  groupName: "Outputs"
  outputDir: null
reductionControlFlags:
  groupName: "General Reduction Control"
  fixpoint: true
  numOfThreads: 32
  codeFormat: "COMPACT_ORIG_FORMAT"
  testScriptExecutionTimeoutInSeconds: 600
  testScriptExecutionKeepWaitingAfterTimeout: true
outputRefiningFlags:
  groupName: "Output Refining Control"
  callFormatter: false
  formatCmd: ""
  callCReduce: false
  creduceCmd: "creduce"
algorithmControlFlags:
  groupName: "Reduction Algorithm Control"
  reductionAlgorithm: null
  listAllReductionAlgorithms: false
  rebuildParseTreeEachIteration: true
  enableTokenSlicer: false
  enableTreeSlicer: false
  enableLineSlicer: false
  defaultDeltaDebuggerTypeForKleene: "DFS"
  maxEditCountForRegularRuleNode: 100
  maxBfsDepthForRegularRuleNode: 5
  stopAtFirstCompatibleChildForRegularRuleNode: false
languageControlFlags:
  groupName: "Language Control"
  languageName: ""
  listLangs: false
  designatedParserFacadeClassName: ""
  listParserFacades: false
  languageJarFiles: []
vulcanFlags:
  groupName: "Vulcan Reducer Control"
  enableVulcan: false
  nonDeletionIterationLimit: 10
  windowSize: 4
  vulcanFixpoint: false
trecFlags:
  groupName: "T-Rec Reducer Control"
  enableTrec: false
profilingFlags:
  groupName: "Profiling"
  progressDumpFile: "perses_progress.txt"
  appendToProgressDumpFile: true
  statDumpFile: null
  profileQueryCacheTime: null
  profileQueryCacheTimeCSV: null
  profileQueryCacheMemory: null
  actionSetProfiler: null
  profileDeltaDebugger: null
cacheControlFlags:
  groupName: "Cache Control"
  queryCaching: "AUTO"
  cacheType: "CONTENT_SHA512"
  nodeActionSetCaching: true
  queryCacheRefreshThreshold: 0
  enableLightweightRefreshing: true
  enablePassCache: false
  globalCacheFile: null
  pathToSaveUpdatedGlobalCache: null
experimentFlags:
  groupName: "Experiment Control"
  onDemandFineGritReducerAnnotationClasses: []
  onDemandMediumGritReducerAnnotationClasses: []
  onDemandCoarseGritReducerAnnotationClasses: []
lprFlags:
  groupName: "LPR Reducer Control"
  enableLPR: false
  lprFixpoint: false
  llmClientPath: null

Parser Facade: class org.perses.grammar.java.Java8ParserFacade
------------------------------------------------------------


The sanity check has been performed. The result is PassingSanityCheckResult
The reducer execution plan is listed below.
--- !<fixpoint>
body: !<atomic>
  reducer: perses_node_priority_with_dfs_delta
condition: smaller


Rebuilding spar-tree: The spartree is not dirty, and thus the rebuilding is skipped.

=Fixpoint iteration 1. Reducer: perses_node_priority_with_dfs_delta

The spar-tree is the following.
compilationUnit {id=1,slot_type=compilationUnit}
|___(*) {id=2,slot_type=kleene_star__compilationUnit_2}
|   |___typeImportOnDemandDeclaration {id=2143,slot_type=[importDeclaration,typeImportOnDemandDeclaration]}
|   |   |___Token:import {id=2144,slot_type=IMPORT}
|   |   |___packageName {id=2145,slot_type=packageName}
|   |   |   |___Token:java {id=2149,slot_type=Identifier}
|   |   |   |___(*) {id=2150,slot_type=kleene_star__packageName_1}
|   |   |       |___aux_rule__packageName_2 {id=2151,slot_type=aux_rule__packageName_2}
|   |   |       |   |___Token:. {id=2155,slot_type=DOT}
|   |   |       |   |___Token:util {id=2156,slot_type=Identifier}
|   |   |       |___aux_rule__packageName_2 {id=2152,slot_type=aux_rule__packageName_2}
|   |   |           |___Token:. {id=2153,slot_type=DOT}
|   |   |           |___Token:stream {id=2154,slot_type=Identifier}
|   |   |___Token:. {id=2146,slot_type=DOT}
|   |   |___Token:* {id=2147,slot_type=MUL}
|   |   |___Token:; {id=2148,slot_type=SEMI}
|   |___typeImportOnDemandDeclaration {id=2129,slot_type=[importDeclaration,typeImportOnDemandDeclaration]}
|   |   |___Token:import {id=2130,slot_type=IMPORT}
|   |   |___packageName {id=2131,slot_type=packageName}
|   |   |   |___Token:java {id=2135,slot_type=Identifier}
|   |   |   |___(*) {id=2136,slot_type=kleene_star__packageName_1}
|   |   |       |___aux_rule__packageName_2 {id=2137,slot_type=aux_rule__packageName_2}
|   |   |       |   |___Token:. {id=2141,slot_type=DOT}
|   |   |       |   |___Token:util {id=2142,slot_type=Identifier}
|   |   |       |___aux_rule__packageName_2 {id=2138,slot_type=aux_rule__packageName_2}
|   |   |           |___Token:. {id=2139,slot_type=DOT}
|   |   |           |___Token:function {id=2140,slot_type=Identifier}
|   |   |___Token:. {id=2132,slot_type=DOT}
|   |   |___Token:* {id=2133,slot_type=MUL}
|   |   |___Token:; {id=2134,slot_type=SEMI}
|   |___typeImportOnDemandDeclaration {id=2118,slot_type=[importDeclaration,typeImportOnDemandDeclaration]}
|       |___Token:import {id=2119,slot_type=IMPORT}
|       |___packageName {id=2120,slot_type=packageName}
|       |   |___Token:java {id=2124,slot_type=Identifier}
|       |   |___(*) {id=2125,slot_type=kleene_star__packageName_1}
|       |       |___aux_rule__packageName_2 {id=2126,slot_type=aux_rule__packageName_2}
|       |           |___Token:. {id=2127,slot_type=DOT}
|       |           |___Token:awt {id=2128,slot_type=Identifier}
|       |___Token:. {id=2121,slot_type=DOT}
|       |___Token:* {id=2122,slot_type=MUL}
|       |___Token:; {id=2123,slot_type=SEMI}
|___(*) {id=3,slot_type=kleene_star__compilationUnit_3}
    |___normalClassDeclaration {id=6,slot_type=[typeDeclaration,classDeclaration,normalClassDeclaration]}
        |___(*) {id=7,slot_type=kleene_star__normalClassDeclaration_1}
        |   |___Token:public {id=2114,slot_type=[classModifier,PUBLIC]}
        |___Token:class {id=8,slot_type=CLASS}
        |___Token:Main {id=9,slot_type=Identifier}
        |___classBody {id=10,slot_type=classBody}
            |___Token:{ {id=11,slot_type=LBRACE}
            |___(*) {id=12,slot_type=kleene_star__classBody_1}
            |   |___normalClassDeclaration {id=2013,slot_type=[classBodyDeclaration,classDeclaration,normalClassDeclaration]}
            |   |   |___(*) {id=2014,slot_type=kleene_star__normalClassDeclaration_1}
            |   |   |   |___Token:public {id=2112,slot_type=[classModifier,PUBLIC]}
            |   |   |   |___Token:static {id=2111,slot_type=[classModifier,STATIC]}
            |   |   |___Token:class {id=2015,slot_type=CLASS}
            |   |   |___Token:Spectrum {id=2016,slot_type=Identifier}
            |   |   |___classBody {id=2017,slot_type=classBody}
            |   |       |___Token:{ {id=2018,slot_type=LBRACE}
            |   |       |___(*) {id=2019,slot_type=kleene_star__classBody_1}
            |   |       |   |___methodDeclaration {id=2022,slot_type=[classBodyDeclaration,methodDeclaration]}
            |   |       |       |___(*) {id=2023,slot_type=kleene_star__methodDeclaration_1}
            |   |       |       |   |___Token:public {id=2108,slot_type=[methodModifier,PUBLIC]}
            |   |       |       |___methodHeader {id=2024,slot_type=methodHeader}
            |   |       |       |   |___unannArrayType {id=2098,slot_type=[result,unannType,unannArrayType]}
            |   |       |       |   |   |___Token:double {id=2106,slot_type=[altnt_block__unannArrayType_1,altnt_block__primitiveType_3,DOUBLE]}
            |   |       |       |   |   |___(+) {id=2101,slot_type=[dims,kleene_plus__dims_5]}
            |   |       |       |   |       |___aux_rule__dims_3 {id=2102,slot_type=aux_rule__dims_3}
            |   |       |       |   |           |___Token:[ {id=2103,slot_type=LBRACK}
            |   |       |       |   |           |___Token:] {id=2104,slot_type=RBRACK}
            |   |       |       |   |___methodDeclarator {id=2093,slot_type=methodDeclarator}
            |   |       |       |       |___Token:getEnergy {id=2094,slot_type=Identifier}
            |   |       |       |       |___Token:( {id=2095,slot_type=LPAREN}
            |   |       |       |       |___Token:) {id=2096,slot_type=RPAREN}
            |   |       |       |___block {id=2026,slot_type=[methodBody,block]}
            |   |       |           |___Token:{ {id=2027,slot_type=LBRACE}
            |   |       |           |___(?) {id=2028,slot_type=optional__block_1}
            |   |       |           |   |___(+) {id=2030,slot_type=blockStatements}
            |   |       |           |       |___returnStatement {id=2034,slot_type=[blockStatement,statement,statementWithoutTrailingSubstatement,returnStatement]}
            |   |       |           |           |___Token:return {id=2035,slot_type=RETURN}
            |   |       |           |           |___(?) {id=2036,slot_type=optional__returnStatement_1}
            |   |       |           |           |   |___arrayCreationExpression {id=2057,slot_type=[expression,conditionalExpression,conditionalOrExpression,conditionalAndExpression,inclusiveOrExpression,exclusiveOrExpression,andExpression,equalityExpression,relationalExpression,shiftExpression,additiveExpression,multiplicativeExpression,unaryExpression,aux_rule__unaryExpression_3,unaryExpressionNotPlusMinus,postfixExpression,altnt_block__postfixExpression_3,primary,altnt_block__primary_2,arrayCreationExpression]}
            |   |       |           |           |       |___Token:new {id=2058,slot_type=NEW}
            |   |       |           |           |       |___altnt_block__arrayCreationExpression_3 {id=2059,slot_type=altnt_block__arrayCreationExpression_3}
            |   |       |           |           |           |___Token:double {id=2091,slot_type=[altnt_block__arrayCreationExpression_6,primitiveType,altnt_block__primitiveType_3,DOUBLE]}
            |   |       |           |           |           |___(+) {id=2064,slot_type=[altnt_block__arrayCreationExpression_4,aux_rule__arrayCreationExpression_7,dimExprs,kleene_plus__dimExprs_2]}
            |   |       |           |           |               |___dimExpr {id=2065,slot_type=dimExpr}
            |   |       |           |           |                   |___Token:[ {id=2066,slot_type=LBRACK}
            |   |       |           |           |                   |___Token:0 {id=2088,slot_type=[expression,conditionalExpression,conditionalOrExpression,conditionalAndExpression,inclusiveOrExpression,exclusiveOrExpression,andExpression,equalityExpression,relationalExpression,shiftExpression,additiveExpression,multiplicativeExpression,unaryExpression,aux_rule__unaryExpression_3,unaryExpressionNotPlusMinus,postfixExpression,altnt_block__postfixExpression_3,primary,altnt_block__primary_2,literal,IntegerLiteral]}
            |   |       |           |           |                   |___Token:] {id=2068,slot_type=RBRACK}
            |   |       |           |           |___Token:; {id=2037,slot_type=SEMI}
            |   |       |           |___Token:} {id=2029,slot_type=RBRACE}
            |   |       |___Token:} {id=2020,slot_type=RBRACE}
            |   |___fieldDeclaration {id=1998,slot_type=[classBodyDeclaration,fieldDeclaration]}
            |   |   |___(*) {id=1999,slot_type=kleene_star__fieldDeclaration_1}
            |   |   |   |___Token:protected {id=2011,slot_type=[fieldModifier,PROTECTED]}
            |   |   |___Token:Spectrum {id=2009,slot_type=[unannType,unannClassOrInterfaceType,altnt_block__unannClassOrInterfaceType_3,unannClassType_lfno_unannClassOrInterfaceType,Identifier]}
            |   |   |___Token:spectrum {id=2005,slot_type=[variableDeclaratorList,variableDeclarator,variableDeclaratorId,Identifier]}
            |   |   |___Token:; {id=2002,slot_type=SEMI}
            |   |___normalClassDeclaration {id=1712,slot_type=[classBodyDeclaration,classDeclaration,normalClassDeclaration]}
            |   |   |___(*) {id=1713,slot_type=kleene_star__normalClassDeclaration_1}
            |   |   |   |___Token:public {id=1997,slot_type=[classModifier,PUBLIC]}
            |   |   |   |___Token:static {id=1996,slot_type=[classModifier,STATIC]}
            |   |   |___Token:class {id=1714,slot_type=CLASS}
            |   |   |___Token:Ref {id=1715,slot_type=Identifier}
            |   |   |___(?) {id=1716,slot_type=optional__normalClassDeclaration_2}
            |   |   |   |___typeParameters {id=1988,slot_type=typeParameters}
            |   |   |       |___Token:< {id=1989,slot_type=LT}
            |   |   |       |___Token:T {id=1993,slot_type=[typeParameterList,typeParameter,Identifier]}
            |   |   |       |___Token:> {id=1991,slot_type=GT}
            |   |   |___classBody {id=1717,slot_type=classBody}
            |   |       |___Token:{ {id=1718,slot_type=LBRACE}
            |   |       |___(*) {id=1719,slot_type=kleene_star__classBody_1}
            |   |       |   |___fieldDeclaration {id=1977,slot_type=[classBodyDeclaration,fieldDeclaration]}
            |   |       |   |   |___Token:T {id=1987,slot_type=[unannType,unannClassOrInterfaceType,altnt_block__unannClassOrInterfaceType_3,unannClassType_lfno_unannClassOrInterfaceType,Identifier]}
            |   |       |   |   |___Token:value {id=1983,slot_type=[variableDeclaratorList,variableDeclarator,variableDeclaratorId,Identifier]}
            |   |       |   |   |___Token:; {id=1980,slot_type=SEMI}
            |   |       |   |___constructorDeclaration {id=1965,slot_type=[classBodyDeclaration,constructorDeclaration]}
            |   |       |   |   |___(*) {id=1966,slot_type=kleene_star__constructorDeclaration_1}
            |   |       |   |   |   |___Token:public {id=1976,slot_type=[constructorModifier,PUBLIC]}
            |   |       |   |   |___constructorDeclarator {id=1967,slot_type=constructorDeclarator}
            |   |       |   |   |   |___Token:Ref {id=1974,slot_type=[methodName,Identifier]}
            |   |       |   |   |   |___Token:( {id=1972,slot_type=LPAREN}
            |   |       |   |   |   |___Token:) {id=1973,slot_type=RPAREN}
            |   |       |   |   |___constructorBody {id=1968,slot_type=constructorBody}
            |   |       |   |       |___Token:{ {id=1969,slot_type=LBRACE}
            |   |       |   |       |___Token:} {id=1970,slot_type=RBRACE}
            |   |       |   |___constructorDeclaration {id=1902,slot_type=[classBodyDeclaration,constructorDeclaration]}
            |   |       |   |   |___(*) {id=1903,slot_type=kleene_star__constructorDeclaration_1}
            |   |       |   |   |   |___Token:public {id=1964,slot_type=[constructorModifier,PUBLIC]}
            |   |       |   |   |___constructorDeclarator {id=1904,slot_type=constructorDeclarator}
            |   |       |   |   |   |___Token:Ref {id=1962,slot_type=[methodName,Identifier]}
            |   |       |   |   |   |___Token:( {id=1948,slot_type=LPAREN}
            |   |       |   |   |   |___(?) {id=1949,slot_type=optional__methodDeclarator_1}
            |   |       |   |   |   |   |___formalParameter {id=1954,slot_type=[formalParameterList,aux_rule__formalParameterList_3,lastFormalParameter,formalParameter]}
            |   |       |   |   |   |       |___Token:T {id=1961,slot_type=[unannType,unannClassOrInterfaceType,altnt_block__unannClassOrInterfaceType_3,unannClassType_lfno_unannClassOrInterfaceType,Identifier]}
            |   |       |   |   |   |       |___Token:value {id=1957,slot_type=[variableDeclaratorId,Identifier]}
            |   |       |   |   |   |___Token:) {id=1950,slot_type=RPAREN}
            |   |       |   |   |___constructorBody {id=1905,slot_type=constructorBody}
            |   |       |   |       |___Token:{ {id=1906,slot_type=LBRACE}
            |   |       |   |       |___(?) {id=1907,slot_type=optional__block_1}
            |   |       |   |       |   |___(+) {id=1909,slot_type=blockStatements}
            |   |       |   |       |       |___expressionStatement {id=1913,slot_type=[blockStatement,statement,statementWithoutTrailingSubstatement,expressionStatement]}
            |   |       |   |       |           |___assignment {id=1916,slot_type=[statementExpression,assignment]}
            |   |       |   |       |           |   |___fieldAccess {id=1939,slot_type=[leftHandSide,fieldAccess]}
            |   |       |   |       |           |   |   |___Token:this {id=1946,slot_type=[altnt_block__fieldAccess_3,primary,altnt_block__primary_2,aux_rule__primary_3,THIS]}
            |   |       |   |       |           |   |   |___Token:. {id=1941,slot_type=DOT}
            |   |       |   |       |           |   |   |___Token:value {id=1942,slot_type=Identifier}
            |   |       |   |       |           |   |___Token:= {id=1938,slot_type=[assignmentOperator,ASSIGN]}
            |   |       |   |       |           |   |___Token:value {id=1937,slot_type=[expression,conditionalExpression,conditionalOrExpression,conditionalAndExpression,inclusiveOrExpression,exclusiveOrExpression,andExpression,equalityExpression,relationalExpression,shiftExpression,additiveExpression,multiplicativeExpression,unaryExpression,aux_rule__unaryExpression_3,unaryExpressionNotPlusMinus,postfixExpression,altnt_block__postfixExpression_3,typeName,Identifier]}
            |   |       |   |       |           |___Token:; {id=1915,slot_type=SEMI}
            |   |       |   |       |___Token:} {id=1908,slot_type=RBRACE}
            |   |       |   |___methodDeclaration {id=1840,slot_type=[classBodyDeclaration,methodDeclaration]}
            |   |       |   |   |___(*) {id=1841,slot_type=kleene_star__methodDeclaration_1}
            |   |       |   |   |   |___Token:public {id=1901,slot_type=[methodModifier,PUBLIC]}
            |   |       |   |   |___methodHeader {id=1842,slot_type=methodHeader}
            |   |       |   |   |   |___Token:boolean {id=1899,slot_type=[result,unannType,altnt_block__primitiveType_3,BOOLEAN]}
            |   |       |   |   |   |___methodDeclarator {id=1893,slot_type=methodDeclarator}
            |   |       |   |   |       |___Token:isNull {id=1894,slot_type=Identifier}
            |   |       |   |   |       |___Token:( {id=1895,slot_type=LPAREN}
            |   |       |   |   |       |___Token:) {id=1896,slot_type=RPAREN}
            |   |       |   |   |___block {id=1844,slot_type=[methodBody,block]}
            |   |       |   |       |___Token:{ {id=1845,slot_type=LBRACE}
            |   |       |   |       |___(?) {id=1846,slot_type=optional__block_1}
            |   |       |   |       |   |___(+) {id=1848,slot_type=blockStatements}
            |   |       |   |       |       |___returnStatement {id=1852,slot_type=[blockStatement,statement,statementWithoutTrailingSubstatement,returnStatement]}
            |   |       |   |       |           |___Token:return {id=1853,slot_type=RETURN}
            |   |       |   |       |           |___(?) {id=1854,slot_type=optional__returnStatement_1}
            |   |       |   |       |           |   |___equalityExpression {id=1863,slot_type=[expression,conditionalExpression,conditionalOrExpression,conditionalAndExpression,inclusiveOrExpression,exclusiveOrExpression,andExpression,equalityExpression]}
            |   |       |   |       |           |       |___Token:value {id=1891,slot_type=[relationalExpression,shiftExpression,additiveExpression,multiplicativeExpression,unaryExpression,aux_rule__unaryExpression_3,unaryExpressionNotPlusMinus,postfixExpression,altnt_block__postfixExpression_3,typeName,Identifier]}
            |   |       |   |       |           |       |___(*) {id=1865,slot_type=kleene_star__equalityExpression_1}
            |   |       |   |       |           |           |___aux_rule__equalityExpression_2 {id=1866,slot_type=aux_rule__equalityExpression_2}
            |   |       |   |       |           |               |___Token:== {id=1881,slot_type=[altnt_block__equalityExpression_3,EQUAL]}
            |   |       |   |       |           |               |___Token:null {id=1880,slot_type=[relationalExpression,shiftExpression,additiveExpression,multiplicativeExpression,unaryExpression,aux_rule__unaryExpression_3,unaryExpressionNotPlusMinus,postfixExpression,altnt_block__postfixExpression_3,primary,altnt_block__primary_2,literal,NullLiteral]}
            |   |       |   |       |           |___Token:; {id=1855,slot_type=SEMI}
            |   |       |   |       |___Token:} {id=1847,slot_type=RBRACE}
            |   |       |   |___methodDeclaration {id=1793,slot_type=[classBodyDeclaration,methodDeclaration]}
            |   |       |   |   |___(*) {id=1794,slot_type=kleene_star__methodDeclaration_1}
            |   |       |   |   |   |___Token:public {id=1839,slot_type=[methodModifier,PUBLIC]}
            |   |       |   |   |___methodHeader {id=1795,slot_type=methodHeader}
            |   |       |   |   |   |___Token:T {id=1837,slot_type=[result,unannType,unannClassOrInterfaceType,altnt_block__unannClassOrInterfaceType_3,unannClassType_lfno_unannClassOrInterfaceType,Identifier]}
            |   |       |   |   |   |___methodDeclarator {id=1829,slot_type=methodDeclarator}
            |   |       |   |   |       |___Token:get {id=1830,slot_type=Identifier}
            |   |       |   |   |       |___Token:( {id=1831,slot_type=LPAREN}
            |   |       |   |   |       |___Token:) {id=1832,slot_type=RPAREN}
            |   |       |   |   |___block {id=1797,slot_type=[methodBody,block]}
            |   |       |   |       |___Token:{ {id=1798,slot_type=LBRACE}
            |   |       |   |       |___(?) {id=1799,slot_type=optional__block_1}
            |   |       |   |       |   |___(+) {id=1801,slot_type=blockStatements}
            |   |       |   |       |       |___returnStatement {id=1805,slot_type=[blockStatement,statement,statementWithoutTrailingSubstatement,returnStatement]}
            |   |       |   |       |           |___Token:return {id=1806,slot_type=RETURN}
            |   |       |   |       |           |___(?) {id=1807,slot_type=optional__returnStatement_1}
            |   |       |   |       |           |   |___Token:value {id=1827,slot_type=[expression,conditionalExpression,conditionalOrExpression,conditionalAndExpression,inclusiveOrExpression,exclusiveOrExpression,andExpression,equalityExpression,relationalExpression,shiftExpression,additiveExpression,multiplicativeExpression,unaryExpression,aux_rule__unaryExpression_3,unaryExpressionNotPlusMinus,postfixExpression,altnt_block__postfixExpression_3,typeName,Identifier]}
            |   |       |   |       |           |___Token:; {id=1808,slot_type=SEMI}
            |   |       |   |       |___Token:} {id=1800,slot_type=RBRACE}
            |   |       |   |___methodDeclaration {id=1727,slot_type=[classBodyDeclaration,methodDeclaration]}
            |   |       |       |___(*) {id=1728,slot_type=kleene_star__methodDeclaration_1}
            |   |       |       |   |___Token:public {id=1792,slot_type=[methodModifier,PUBLIC]}
            |   |       |       |___methodHeader {id=1729,slot_type=methodHeader}
            |   |       |       |   |___Token:void {id=1790,slot_type=[result,VOID]}
            |   |       |       |   |___methodDeclarator {id=1774,slot_type=methodDeclarator}
            |   |       |       |       |___Token:set {id=1775,slot_type=Identifier}
            |   |       |       |       |___Token:( {id=1776,slot_type=LPAREN}
            |   |       |       |       |___(?) {id=1777,slot_type=optional__methodDeclarator_1}
            |   |       |       |       |   |___formalParameter {id=1782,slot_type=[formalParameterList,aux_rule__formalParameterList_3,lastFormalParameter,formalParameter]}
            |   |       |       |       |       |___Token:T {id=1789,slot_type=[unannType,unannClassOrInterfaceType,altnt_block__unannClassOrInterfaceType_3,unannClassType_lfno_unannClassOrInterfaceType,Identifier]}
            |   |       |       |       |       |___Token:value {id=1785,slot_type=[variableDeclaratorId,Identifier]}
            |   |       |       |       |___Token:) {id=1778,slot_type=RPAREN}
            |   |       |       |___block {id=1731,slot_type=[methodBody,block]}
            |   |       |           |___Token:{ {id=1732,slot_type=LBRACE}
            |   |       |           |___(?) {id=1733,slot_type=optional__block_1}
            |   |       |           |   |___(+) {id=1735,slot_type=blockStatements}
            |   |       |           |       |___expressionStatement {id=1739,slot_type=[blockStatement,statement,statementWithoutTrailingSubstatement,expressionStatement]}
            |   |       |           |           |___assignment {id=1742,slot_type=[statementExpression,assignment]}
            |   |       |           |           |   |___fieldAccess {id=1765,slot_type=[leftHandSide,fieldAccess]}
            |   |       |           |           |   |   |___Token:this {id=1772,slot_type=[altnt_block__fieldAccess_3,primary,altnt_block__primary_2,aux_rule__primary_3,THIS]}
            |   |       |           |           |   |   |___Token:. {id=1767,slot_type=DOT}
            |   |       |           |           |   |   |___Token:value {id=1768,slot_type=Identifier}
            |   |       |           |           |   |___Token:= {id=1764,slot_type=[assignmentOperator,ASSIGN]}
            |   |       |           |           |   |___Token:value {id=1763,slot_type=[expression,conditionalExpression,conditionalOrExpression,conditionalAndExpression,inclusiveOrExpression,exclusiveOrExpression,andExpression,equalityExpression,relationalExpression,shiftExpression,additiveExpression,multiplicativeExpression,unaryExpression,aux_rule__unaryExpression_3,unaryExpressionNotPlusMinus,postfixExpression,altnt_block__postfixExpression_3,typeName,Identifier]}
            |   |       |           |           |___Token:; {id=1741,slot_type=SEMI}
            |   |       |           |___Token:} {id=1734,slot_type=RBRACE}
            |   |       |___Token:} {id=1720,slot_type=RBRACE}
            |   |___methodDeclaration {id=1214,slot_type=[classBodyDeclaration,methodDeclaration]}
            |   |   |___(*) {id=1215,slot_type=kleene_star__methodDeclaration_1}
            |   |   |   |___Token:public {id=1710,slot_type=[methodModifier,PUBLIC]}
            |   |   |   |___Token:static {id=1709,slot_type=[methodModifier,STATIC]}
            |   |   |___methodHeader {id=1216,slot_type=methodHeader}
            |   |   |   |___(?) {id=1633,slot_type=optional__methodHeader_5}
            |   |   |   |   |___typeParameters {id=1701,slot_type=[aux_rule__methodHeader_4,typeParameters]}
            |   |   |   |       |___Token:< {id=1702,slot_type=LT}
            |   |   |   |       |___Token:T {id=1706,slot_type=[typeParameterList,typeParameter,Identifier]}
            |   |   |   |       |___Token:> {id=1704,slot_type=GT}
            |   |   |   |___Token:T {id=1699,slot_type=[result,unannType,unannClassOrInterfaceType,altnt_block__unannClassOrInterfaceType_3,unannClassType_lfno_unannClassOrInterfaceType,Identifier]}
            |   |   |   |___methodDeclarator {id=1635,slot_type=methodDeclarator}
            |   |   |       |___Token:maxKey {id=1636,slot_type=Identifier}
            |   |   |       |___Token:( {id=1637,slot_type=LPAREN}
            |   |   |       |___(?) {id=1638,slot_type=optional__methodDeclarator_1}
            |   |   |       |   |___aux_rule__formalParameterList_3 {id=1641,slot_type=[formalParameterList,aux_rule__formalParameterList_3]}
            |   |   |       |       |___(?) {id=1642,slot_type=optional__formalParameterList_2}
            |   |   |       |       |   |___aux_rule__formalParameterList_1 {id=1672,slot_type=aux_rule__formalParameterList_1}
            |   |   |       |       |       |___formalParameter {id=1676,slot_type=[formalParameters,altnt_block__formalParameters_5,formalParameter]}
            |   |   |       |       |       |   |___unannClassType_lfno_unannClassOrInterfaceType {id=1682,slot_type=[unannType,unannClassOrInterfaceType,altnt_block__unannClassOrInterfaceType_3,unannClassType_lfno_unannClassOrInterfaceType]}
            |   |   |       |       |       |   |   |___Token:Stream {id=1683,slot_type=Identifier}
            |   |   |       |       |       |   |   |___(?) {id=1684,slot_type=optional__classType_lfno_classOrInterfaceType_2}
            |   |   |       |       |       |   |       |___typeArguments {id=1685,slot_type=typeArguments}
            |   |   |       |       |       |   |           |___Token:< {id=1686,slot_type=LT}
            |   |   |       |       |       |   |           |___Token:T {id=1694,slot_type=[typeArgumentList,typeArgument,referenceType,classOrInterfaceType,altnt_block__classOrInterfaceType_3,classType_lfno_classOrInterfaceType,Identifier]}
            |   |   |       |       |       |   |           |___Token:> {id=1688,slot_type=GT}
            |   |   |       |       |       |   |___Token:stream {id=1679,slot_type=[variableDeclaratorId,Identifier]}
            |   |   |       |       |       |___Token:, {id=1674,slot_type=COMMA}
            |   |   |       |       |___formalParameter {id=1644,slot_type=[lastFormalParameter,formalParameter]}
            |   |   |       |           |___unannClassType_lfno_unannClassOrInterfaceType {id=1650,slot_type=[unannType,unannClassOrInterfaceType,altnt_block__unannClassOrInterfaceType_3,unannClassType_lfno_unannClassOrInterfaceType]}
            |   |   |       |           |   |___Token:Function {id=1651,slot_type=Identifier}
            |   |   |       |           |   |___(?) {id=1652,slot_type=optional__classType_lfno_classOrInterfaceType_2}
            |   |   |       |           |       |___typeArguments {id=1653,slot_type=typeArguments}
            |   |   |       |           |           |___Token:< {id=1654,slot_type=LT}
            |   |   |       |           |           |___typeArgumentList {id=1655,slot_type=typeArgumentList}
            |   |   |       |           |           |   |___Token:T {id=1671,slot_type=[typeArgument,referenceType,classOrInterfaceType,altnt_block__classOrInterfaceType_3,classType_lfno_classOrInterfaceType,Identifier]}
            |   |   |       |           |           |   |___(*) {id=1658,slot_type=kleene_star__typeArgumentList_2}
            |   |   |       |           |           |       |___aux_rule__typeArgumentList_1 {id=1659,slot_type=aux_rule__typeArgumentList_1}
            |   |   |       |           |           |           |___Token:, {id=1660,slot_type=COMMA}
            |   |   |       |           |           |           |___Token:Double {id=1666,slot_type=[typeArgument,referenceType,classOrInterfaceType,altnt_block__classOrInterfaceType_3,classType_lfno_classOrInterfaceType,Identifier]}
            |   |   |       |           |           |___Token:> {id=1656,slot_type=GT}
            |   |   |       |           |___Token:function {id=1647,slot_type=[variableDeclaratorId,Identifier]}
            |   |   |       |___Token:) {id=1639,slot_type=RPAREN}
            |   |   |___block {id=1218,slot_type=[methodBody,block]}
            |   |       |___Token:{ {id=1219,slot_type=LBRACE}
            |   |       |___(?) {id=1220,slot_type=optional__block_1}
            |   |       |   |___(+) {id=1222,slot_type=blockStatements}
            |   |       |       |___localVariableDeclarationStatement {id=1575,slot_type=[blockStatement,localVariableDeclarationStatement]}
            |   |       |       |   |___localVariableDeclaration {id=1576,slot_type=localVariableDeclaration}
            |   |       |       |   |   |___unannClassType_lfno_unannClassOrInterfaceType {id=1620,slot_type=[unannType,unannClassOrInterfaceType,altnt_block__unannClassOrInterfaceType_3,unannClassType_lfno_unannClassOrInterfaceType]}
            |   |       |       |   |   |   |___Token:Ref {id=1621,slot_type=Identifier}
            |   |       |       |   |   |   |___(?) {id=1622,slot_type=optional__classType_lfno_classOrInterfaceType_2}
            |   |       |       |   |   |       |___typeArguments {id=1623,slot_type=typeArguments}
            |   |       |       |   |   |           |___Token:< {id=1624,slot_type=LT}
            |   |       |       |   |   |           |___Token:Double {id=1632,slot_type=[typeArgumentList,typeArgument,referenceType,classOrInterfaceType,altnt_block__classOrInterfaceType_3,classType_lfno_classOrInterfaceType,Identifier]}
            |   |       |       |   |   |           |___Token:> {id=1626,slot_type=GT}
            |   |       |       |   |   |___variableDeclarator {id=1580,slot_type=[variableDeclaratorList,variableDeclarator]}
            |   |       |       |   |       |___Token:max {id=1617,slot_type=[variableDeclaratorId,Identifier]}
            |   |       |       |   |       |___(?) {id=1582,slot_type=optional__variableDeclarator_2}
            |   |       |       |   |           |___aux_rule__variableDeclarator_1 {id=1583,slot_type=aux_rule__variableDeclarator_1}
            |   |       |       |   |               |___Token:= {id=1584,slot_type=ASSIGN}
            |   |       |       |   |               |___classInstanceCreationExpression_lfno_primary {id=1605,slot_type=[variableInitializer,expression,conditionalExpression,conditionalOrExpression,conditionalAndExpression,inclusiveOrExpression,exclusiveOrExpression,andExpression,equalityExpression,relationalExpression,shiftExpression,additiveExpression,multiplicativeExpression,unaryExpression,aux_rule__unaryExpression_3,unaryExpressionNotPlusMinus,postfixExpression,altnt_block__postfixExpression_3,primary,altnt_block__primary_2,classInstanceCreationExpression_lfno_primary]}
            |   |       |       |   |                   |___aux_rule__classInstanceCreationExpression_lfno_primary_15 {id=1614,slot_type=[altnt_block__classInstanceCreationExpression_lfno_primary_14,aux_rule__classInstanceCreationExpression_lfno_primary_15]}
            |   |       |       |   |                   |   |___Token:new {id=1615,slot_type=NEW}
            |   |       |       |   |                   |   |___Token:Ref {id=1616,slot_type=Identifier}
            |   |       |       |   |                   |___(?) {id=1607,slot_type=optional__classInstanceCreationExpression_6}
            |   |       |       |   |                   |   |___aux_rule__typeArgumentsOrDiamond_1 {id=1611,slot_type=[typeArgumentsOrDiamond,aux_rule__typeArgumentsOrDiamond_1]}
            |   |       |       |   |                   |       |___Token:< {id=1612,slot_type=LT}
            |   |       |       |   |                   |       |___Token:> {id=1613,slot_type=GT}
            |   |       |       |   |                   |___Token:( {id=1608,slot_type=LPAREN}
            |   |       |       |   |                   |___Token:) {id=1609,slot_type=RPAREN}
            |   |       |       |   |___Token:; {id=1577,slot_type=SEMI}
            |   |       |       |___localVariableDeclarationStatement {id=1517,slot_type=[blockStatement,localVariableDeclarationStatement]}
            |   |       |       |   |___localVariableDeclaration {id=1518,slot_type=localVariableDeclaration}
            |   |       |       |   |   |___unannClassType_lfno_unannClassOrInterfaceType {id=1562,slot_type=[unannType,unannClassOrInterfaceType,altnt_block__unannClassOrInterfaceType_3,unannClassType_lfno_unannClassOrInterfaceType]}
            |   |       |       |   |   |   |___Token:Ref {id=1563,slot_type=Identifier}
            |   |       |       |   |   |   |___(?) {id=1564,slot_type=optional__classType_lfno_classOrInterfaceType_2}
            |   |       |       |   |   |       |___typeArguments {id=1565,slot_type=typeArguments}
            |   |       |       |   |   |           |___Token:< {id=1566,slot_type=LT}
            |   |       |       |   |   |           |___Token:T {id=1574,slot_type=[typeArgumentList,typeArgument,referenceType,classOrInterfaceType,altnt_block__classOrInterfaceType_3,classType_lfno_classOrInterfaceType,Identifier]}
            |   |       |       |   |   |           |___Token:> {id=1568,slot_type=GT}
            |   |       |       |   |   |___variableDeclarator {id=1522,slot_type=[variableDeclaratorList,variableDeclarator]}
            |   |       |       |   |       |___Token:index {id=1559,slot_type=[variableDeclaratorId,Identifier]}
            |   |       |       |   |       |___(?) {id=1524,slot_type=optional__variableDeclarator_2}
            |   |       |       |   |           |___aux_rule__variableDeclarator_1 {id=1525,slot_type=aux_rule__variableDeclarator_1}
            |   |       |       |   |               |___Token:= {id=1526,slot_type=ASSIGN}
            |   |       |       |   |               |___classInstanceCreationExpression_lfno_primary {id=1547,slot_type=[variableInitializer,expression,conditionalExpression,conditionalOrExpression,conditionalAndExpression,inclusiveOrExpression,exclusiveOrExpression,andExpression,equalityExpression,relationalExpression,shiftExpression,additiveExpression,multiplicativeExpression,unaryExpression,aux_rule__unaryExpression_3,unaryExpressionNotPlusMinus,postfixExpression,altnt_block__postfixExpression_3,primary,altnt_block__primary_2,classInstanceCreationExpression_lfno_primary]}
            |   |       |       |   |                   |___aux_rule__classInstanceCreationExpression_lfno_primary_15 {id=1556,slot_type=[altnt_block__classInstanceCreationExpression_lfno_primary_14,aux_rule__classInstanceCreationExpression_lfno_primary_15]}
            |   |       |       |   |                   |   |___Token:new {id=1557,slot_type=NEW}
            |   |       |       |   |                   |   |___Token:Ref {id=1558,slot_type=Identifier}
            |   |       |       |   |                   |___(?) {id=1549,slot_type=optional__classInstanceCreationExpression_6}
            |   |       |       |   |                   |   |___aux_rule__typeArgumentsOrDiamond_1 {id=1553,slot_type=[typeArgumentsOrDiamond,aux_rule__typeArgumentsOrDiamond_1]}
            |   |       |       |   |                   |       |___Token:< {id=1554,slot_type=LT}
            |   |       |       |   |                   |       |___Token:> {id=1555,slot_type=GT}
            |   |       |       |   |                   |___Token:( {id=1550,slot_type=LPAREN}
            |   |       |       |   |                   |___Token:) {id=1551,slot_type=RPAREN}
            |   |       |       |   |___Token:; {id=1519,slot_type=SEMI}
            |   |       |       |___expressionStatement {id=1264,slot_type=[blockStatement,statement,statementWithoutTrailingSubstatement,expressionStatement]}
            |   |       |       |   |___methodInvocation {id=1267,slot_type=[statementExpression,methodInvocation]}
            |   |       |       |   |   |___aux_rule__methodInvocation_18 {id=1270,slot_type=[altnt_block__methodInvocation_12,aux_rule__methodInvocation_18]}
            |   |       |       |   |   |   |___Token:stream {id=1516,slot_type=[typeName,Identifier]}
            |   |       |       |   |   |   |___Token:. {id=1272,slot_type=DOT}
            |   |       |       |   |   |   |___altnt_block__methodInvocation_13 {id=1273,slot_type=altnt_block__methodInvocation_13}
            |   |       |       |   |   |       |___Token:forEach {id=1274,slot_type=Identifier}
            |   |       |       |   |   |       |___Token:( {id=1275,slot_type=LPAREN}
            |   |       |       |   |   |       |___(?) {id=1276,slot_type=optional__explicitConstructorInvocation_2}
            |   |       |       |   |   |           |___lambdaExpression {id=1279,slot_type=[argumentList,expression,lambdaExpression]}
            |   |       |       |   |   |               |___Token:v {id=1515,slot_type=[lambdaParameters,Identifier]}
            |   |       |       |   |   |               |___Token:-> {id=1281,slot_type=ARROW}
            |   |       |       |   |   |               |___block {id=1283,slot_type=[lambdaBody,block]}
            |   |       |       |   |   |                   |___Token:{ {id=1284,slot_type=LBRACE}
            |   |       |       |   |   |                   |___(?) {id=1285,slot_type=optional__block_1}
            |   |       |       |   |   |                   |   |___(+) {id=1287,slot_type=blockStatements}
            |   |       |       |   |   |                   |       |___localVariableDeclarationStatement {id=1449,slot_type=[blockStatement,localVariableDeclarationStatement]}
            |   |       |       |   |   |                   |       |   |___localVariableDeclaration {id=1450,slot_type=localVariableDeclaration}
            |   |       |       |   |   |                   |       |   |   |___Token:Double {id=1514,slot_type=[unannType,unannClassOrInterfaceType,altnt_block__unannClassOrInterfaceType_3,unannClassType_lfno_unannClassOrInterfaceType,Identifier]}
            |   |       |       |   |   |                   |       |   |   |___variableDeclarator {id=1454,slot_type=[variableDeclaratorList,variableDeclarator]}
            |   |       |       |   |   |                   |       |   |       |___Token:value {id=1510,slot_type=[variableDeclaratorId,Identifier]}
            |   |       |       |   |   |                   |       |   |       |___(?) {id=1456,slot_type=optional__variableDeclarator_2}
            |   |       |       |   |   |                   |       |   |           |___aux_rule__variableDeclarator_1 {id=1457,slot_type=aux_rule__variableDeclarator_1}
            |   |       |       |   |   |                   |       |   |               |___Token:= {id=1458,slot_type=ASSIGN}
            |   |       |       |   |   |                   |       |   |               |___methodInvocation_lfno_primary {id=1479,slot_type=[variableInitializer,expression,conditionalExpression,conditionalOrExpression,conditionalAndExpression,inclusiveOrExpression,exclusiveOrExpression,andExpression,equalityExpression,relationalExpression,shiftExpression,additiveExpression,multiplicativeExpression,unaryExpression,aux_rule__unaryExpression_3,unaryExpressionNotPlusMinus,postfixExpression,altnt_block__postfixExpression_3,primary,altnt_block__primary_2,methodInvocation_lfno_primary]}
            |   |       |       |   |   |                   |       |   |                   |___aux_rule__methodInvocation_lfno_primary_14 {id=1482,slot_type=[altnt_block__methodInvocation_lfno_primary_10,aux_rule__methodInvocation_lfno_primary_14]}
            |   |       |       |   |   |                   |       |   |                   |   |___Token:function {id=1509,slot_type=[typeName,Identifier]}
            |   |       |       |   |   |                   |       |   |                   |   |___Token:. {id=1484,slot_type=DOT}
            |   |       |       |   |   |                   |       |   |                   |   |___altnt_block__methodInvocation_13 {id=1485,slot_type=altnt_block__methodInvocation_13}
            |   |       |       |   |   |                   |       |   |                   |       |___Token:apply {id=1486,slot_type=Identifier}
            |   |       |       |   |   |                   |       |   |                   |       |___Token:( {id=1487,slot_type=LPAREN}
            |   |       |       |   |   |                   |       |   |                   |       |___(?) {id=1488,slot_type=optional__explicitConstructorInvocation_2}
            |   |       |       |   |   |                   |       |   |                   |           |___Token:v {id=1508,slot_type=[argumentList,expression,conditionalExpression,conditionalOrExpression,conditionalAndExpression,inclusiveOrExpression,exclusiveOrExpression,andExpression,equalityExpression,relationalExpression,shiftExpression,additiveExpression,multiplicativeExpression,unaryExpression,aux_rule__unaryExpression_3,unaryExpressionNotPlusMinus,postfixExpression,altnt_block__postfixExpression_3,typeName,Identifier]}
            |   |       |       |   |   |                   |       |   |                   |___Token:) {id=1481,slot_type=RPAREN}
            |   |       |       |   |   |                   |       |   |___Token:; {id=1451,slot_type=SEMI}
            |   |       |       |   |   |                   |       |___ifThenStatement {id=1291,slot_type=[blockStatement,statement,ifThenStatement]}
            |   |       |       |   |   |                   |           |___Token:if {id=1292,slot_type=IF}
            |   |       |       |   |   |                   |           |___Token:( {id=1293,slot_type=LPAREN}
            |   |       |       |   |   |                   |           |___conditionalOrExpression {id=1378,slot_type=[expression,conditionalExpression,conditionalOrExpression]}
            |   |       |       |   |   |                   |           |   |___methodInvocation_lfno_primary {id=1439,slot_type=[conditionalAndExpression,inclusiveOrExpression,exclusiveOrExpression,andExpression,equalityExpression,relationalExpression,shiftExpression,additiveExpression,multiplicativeExpression,unaryExpression,aux_rule__unaryExpression_3,unaryExpressionNotPlusMinus,postfixExpression,altnt_block__postfixExpression_3,primary,altnt_block__primary_2,methodInvocation_lfno_primary]}
            |   |       |       |   |   |                   |           |   |   |___aux_rule__methodInvocation_lfno_primary_14 {id=1442,slot_type=[altnt_block__methodInvocation_lfno_primary_10,aux_rule__methodInvocation_lfno_primary_14]}
            |   |       |       |   |   |                   |           |   |   |   |___Token:max {id=1448,slot_type=[typeName,Identifier]}
            |   |       |       |   |   |                   |           |   |   |   |___Token:. {id=1444,slot_type=DOT}
            |   |       |       |   |   |                   |           |   |   |   |___altnt_block__methodInvocation_13 {id=1445,slot_type=altnt_block__methodInvocation_13}
            |   |       |       |   |   |                   |           |   |   |       |___Token:isNull {id=1446,slot_type=Identifier}
            |   |       |       |   |   |                   |           |   |   |       |___Token:( {id=1447,slot_type=LPAREN}
            |   |       |       |   |   |                   |           |   |   |___Token:) {id=1441,slot_type=RPAREN}
            |   |       |       |   |   |                   |           |   |___(*) {id=1380,slot_type=kleene_star__conditionalOrExpression_1}
            |   |       |       |   |   |                   |           |       |___aux_rule__conditionalOrExpression_2 {id=1381,slot_type=aux_rule__conditionalOrExpression_2}
            |   |       |       |   |   |                   |           |           |___Token:|| {id=1382,slot_type=OR}
            |   |       |       |   |   |                   |           |           |___relationalExpression {id=1388,slot_type=[conditionalAndExpression,inclusiveOrExpression,exclusiveOrExpression,andExpression,equalityExpression,relationalExpression]}
            |   |       |       |   |   |                   |           |               |___Token:value {id=1423,slot_type=[shiftExpression,additiveExpression,multiplicativeExpression,unaryExpression,aux_rule__unaryExpression_3,unaryExpressionNotPlusMinus,postfixExpression,altnt_block__postfixExpression_3,typeName,Identifier]}
            |   |       |       |   |   |                   |           |               |___(*) {id=1390,slot_type=kleene_star__relationalExpression_1}
            |   |       |       |   |   |                   |           |                   |___aux_rule__relationalExpression_5 {id=1392,slot_type=[aux_rule__relationalExpression_2,aux_rule__relationalExpression_5]}
            |   |       |       |   |   |                   |           |                       |___Token:> {id=1414,slot_type=[altnt_block__relationalExpression_3,GT]}
            |   |       |       |   |   |                   |           |                       |___methodInvocation_lfno_primary {id=1404,slot_type=[shiftExpression,additiveExpression,multiplicativeExpression,unaryExpression,aux_rule__unaryExpression_3,unaryExpressionNotPlusMinus,postfixExpression,altnt_block__postfixExpression_3,primary,altnt_block__primary_2,methodInvocation_lfno_primary]}
            |   |       |       |   |   |                   |           |                           |___aux_rule__methodInvocation_lfno_primary_14 {id=1407,slot_type=[altnt_block__methodInvocation_lfno_primary_10,aux_rule__methodInvocation_lfno_primary_14]}
            |   |       |       |   |   |                   |           |                           |   |___Token:max {id=1413,slot_type=[typeName,Identifier]}
            |   |       |       |   |   |                   |           |                           |   |___Token:. {id=1409,slot_type=DOT}
            |   |       |       |   |   |                   |           |                           |   |___altnt_block__methodInvocation_13 {id=1410,slot_type=altnt_block__methodInvocation_13}
            |   |       |       |   |   |                   |           |                           |       |___Token:get {id=1411,slot_type=Identifier}
            |   |       |       |   |   |                   |           |                           |       |___Token:( {id=1412,slot_type=LPAREN}
            |   |       |       |   |   |                   |           |                           |___Token:) {id=1406,slot_type=RPAREN}
            |   |       |       |   |   |                   |           |___Token:) {id=1295,slot_type=RPAREN}
            |   |       |       |   |   |                   |           |___block {id=1298,slot_type=[statement,statementWithoutTrailingSubstatement,block]}
            |   |       |       |   |   |                   |               |___Token:{ {id=1299,slot_type=LBRACE}
            |   |       |       |   |   |                   |               |___(?) {id=1300,slot_type=optional__block_1}
            |   |       |       |   |   |                   |               |   |___(+) {id=1302,slot_type=blockStatements}
            |   |       |       |   |   |                   |               |       |___expressionStatement {id=1343,slot_type=[blockStatement,statement,statementWithoutTrailingSubstatement,expressionStatement]}
            |   |       |       |   |   |                   |               |       |   |___methodInvocation {id=1346,slot_type=[statementExpression,methodInvocation]}
            |   |       |       |   |   |                   |               |       |   |   |___aux_rule__methodInvocation_18 {id=1349,slot_type=[altnt_block__methodInvocation_12,aux_rule__methodInvocation_18]}
            |   |       |       |   |   |                   |               |       |   |   |   |___Token:max {id=1376,slot_type=[typeName,Identifier]}
            |   |       |       |   |   |                   |               |       |   |   |   |___Token:. {id=1351,slot_type=DOT}
            |   |       |       |   |   |                   |               |       |   |   |   |___altnt_block__methodInvocation_13 {id=1352,slot_type=altnt_block__methodInvocation_13}
            |   |       |       |   |   |                   |               |       |   |   |       |___Token:set {id=1353,slot_type=Identifier}
            |   |       |       |   |   |                   |               |       |   |   |       |___Token:( {id=1354,slot_type=LPAREN}
            |   |       |       |   |   |                   |               |       |   |   |       |___(?) {id=1355,slot_type=optional__explicitConstructorInvocation_2}
            |   |       |       |   |   |                   |               |       |   |   |           |___Token:value {id=1375,slot_type=[argumentList,expression,conditionalExpression,conditionalOrExpression,conditionalAndExpression,inclusiveOrExpression,exclusiveOrExpression,andExpression,equalityExpression,relationalExpression,shiftExpression,additiveExpression,multiplicativeExpression,unaryExpression,aux_rule__unaryExpression_3,unaryExpressionNotPlusMinus,postfixExpression,altnt_block__postfixExpression_3,typeName,Identifier]}
            |   |       |       |   |   |                   |               |       |   |   |___Token:) {id=1348,slot_type=RPAREN}
            |   |       |       |   |   |                   |               |       |   |___Token:; {id=1345,slot_type=SEMI}
            |   |       |       |   |   |                   |               |       |___expressionStatement {id=1307,slot_type=[blockStatement,statement,statementWithoutTrailingSubstatement,expressionStatement]}
            |   |       |       |   |   |                   |               |           |___methodInvocation {id=1310,slot_type=[statementExpression,methodInvocation]}
            |   |       |       |   |   |                   |               |           |   |___aux_rule__methodInvocation_18 {id=1313,slot_type=[altnt_block__methodInvocation_12,aux_rule__methodInvocation_18]}
            |   |       |       |   |   |                   |               |           |   |   |___Token:index {id=1340,slot_type=[typeName,Identifier]}
            |   |       |       |   |   |                   |               |           |   |   |___Token:. {id=1315,slot_type=DOT}
            |   |       |       |   |   |                   |               |           |   |   |___altnt_block__methodInvocation_13 {id=1316,slot_type=altnt_block__methodInvocation_13}
            |   |       |       |   |   |                   |               |           |   |       |___Token:set {id=1317,slot_type=Identifier}
            |   |       |       |   |   |                   |               |           |   |       |___Token:( {id=1318,slot_type=LPAREN}
            |   |       |       |   |   |                   |               |           |   |       |___(?) {id=1319,slot_type=optional__explicitConstructorInvocation_2}
            |   |       |       |   |   |                   |               |           |   |           |___Token:v {id=1339,slot_type=[argumentList,expression,conditionalExpression,conditionalOrExpression,conditionalAndExpression,inclusiveOrExpression,exclusiveOrExpression,andExpression,equalityExpression,relationalExpression,shiftExpression,additiveExpression,multiplicativeExpression,unaryExpression,aux_rule__unaryExpression_3,unaryExpressionNotPlusMinus,postfixExpression,altnt_block__postfixExpression_3,typeName,Identifier]}
            |   |       |       |   |   |                   |               |           |   |___Token:) {id=1312,slot_type=RPAREN}
            |   |       |       |   |   |                   |               |           |___Token:; {id=1309,slot_type=SEMI}
            |   |       |       |   |   |                   |               |___Token:} {id=1301,slot_type=RBRACE}
            |   |       |       |   |   |                   |___Token:} {id=1286,slot_type=RBRACE}
            |   |       |       |   |   |___Token:) {id=1269,slot_type=RPAREN}
            |   |       |       |   |___Token:; {id=1266,slot_type=SEMI}
            |   |       |       |___returnStatement {id=1229,slot_type=[blockStatement,statement,statementWithoutTrailingSubstatement,returnStatement]}
            |   |       |           |___Token:return {id=1230,slot_type=RETURN}
            |   |       |           |___(?) {id=1231,slot_type=optional__returnStatement_1}
            |   |       |           |   |___methodInvocation_lfno_primary {id=1252,slot_type=[expression,conditionalExpression,conditionalOrExpression,conditionalAndExpression,inclusiveOrExpression,exclusiveOrExpression,andExpression,equalityExpression,relationalExpression,shiftExpression,additiveExpression,multiplicativeExpression,unaryExpression,aux_rule__unaryExpression_3,unaryExpressionNotPlusMinus,postfixExpression,altnt_block__postfixExpression_3,primary,altnt_block__primary_2,methodInvocation_lfno_primary]}
            |   |       |           |       |___aux_rule__methodInvocation_lfno_primary_14 {id=1255,slot_type=[altnt_block__methodInvocation_lfno_primary_10,aux_rule__methodInvocation_lfno_primary_14]}
            |   |       |           |       |   |___Token:index {id=1261,slot_type=[typeName,Identifier]}
            |   |       |           |       |   |___Token:. {id=1257,slot_type=DOT}
            |   |       |           |       |   |___altnt_block__methodInvocation_13 {id=1258,slot_type=altnt_block__methodInvocation_13}
            |   |       |           |       |       |___Token:get {id=1259,slot_type=Identifier}
            |   |       |           |       |       |___Token:( {id=1260,slot_type=LPAREN}
            |   |       |           |       |___Token:) {id=1254,slot_type=RPAREN}
            |   |       |           |___Token:; {id=1232,slot_type=SEMI}
            |   |       |___Token:} {id=1221,slot_type=RBRACE}
            |   |___methodDeclaration {id=986,slot_type=[classBodyDeclaration,methodDeclaration]}
            |   |   |___(*) {id=987,slot_type=kleene_star__methodDeclaration_1}
            |   |   |   |___Token:public {id=1213,slot_type=[methodModifier,PUBLIC]}
            |   |   |   |___Token:static {id=1212,slot_type=[methodModifier,STATIC]}
            |   |   |___methodHeader {id=988,slot_type=methodHeader}
            |   |   |   |___Token:int {id=1209,slot_type=[result,unannType,altnt_block__primitiveType_3,INT]}
            |   |   |   |___methodDeclarator {id=1157,slot_type=methodDeclarator}
            |   |   |       |___Token:interpolate {id=1158,slot_type=Identifier}
            |   |   |       |___Token:( {id=1159,slot_type=LPAREN}
            |   |   |       |___(?) {id=1160,slot_type=optional__methodDeclarator_1}
            |   |   |       |   |___aux_rule__formalParameterList_3 {id=1163,slot_type=[formalParameterList,aux_rule__formalParameterList_3]}
            |   |   |       |       |___(?) {id=1164,slot_type=optional__formalParameterList_2}
            |   |   |       |       |   |___aux_rule__formalParameterList_1 {id=1172,slot_type=aux_rule__formalParameterList_1}
            |   |   |       |       |       |___formalParameters {id=1173,slot_type=formalParameters}
            |   |   |       |       |       |   |___formalParameter {id=1201,slot_type=[altnt_block__formalParameters_5,formalParameter]}
            |   |   |       |       |       |   |   |___Token:int {id=1206,slot_type=[unannType,altnt_block__primitiveType_3,INT]}
            |   |   |       |       |       |   |   |___Token:x {id=1204,slot_type=[variableDeclaratorId,Identifier]}
            |   |   |       |       |       |   |___(*) {id=1176,slot_type=kleene_star__formalParameters_2}
            |   |   |       |       |       |       |___aux_rule__formalParameters_1 {id=1177,slot_type=aux_rule__formalParameters_1}
            |   |   |       |       |       |       |   |___Token:, {id=1194,slot_type=COMMA}
            |   |   |       |       |       |       |   |___formalParameter {id=1195,slot_type=formalParameter}
            |   |   |       |       |       |       |       |___Token:int {id=1200,slot_type=[unannType,altnt_block__primitiveType_3,INT]}
            |   |   |       |       |       |       |       |___Token:x0 {id=1198,slot_type=[variableDeclaratorId,Identifier]}
            |   |   |       |       |       |       |___aux_rule__formalParameters_1 {id=1178,slot_type=aux_rule__formalParameters_1}
            |   |   |       |       |       |       |   |___Token:, {id=1187,slot_type=COMMA}
            |   |   |       |       |       |       |   |___formalParameter {id=1188,slot_type=formalParameter}
            |   |   |       |       |       |       |       |___Token:int {id=1193,slot_type=[unannType,altnt_block__primitiveType_3,INT]}
            |   |   |       |       |       |       |       |___Token:x1 {id=1191,slot_type=[variableDeclaratorId,Identifier]}
            |   |   |       |       |       |       |___aux_rule__formalParameters_1 {id=1179,slot_type=aux_rule__formalParameters_1}
            |   |   |       |       |       |           |___Token:, {id=1180,slot_type=COMMA}
            |   |   |       |       |       |           |___formalParameter {id=1181,slot_type=formalParameter}
            |   |   |       |       |       |               |___Token:int {id=1186,slot_type=[unannType,altnt_block__primitiveType_3,INT]}
            |   |   |       |       |       |               |___Token:y0 {id=1184,slot_type=[variableDeclaratorId,Identifier]}
            |   |   |       |       |       |___Token:, {id=1174,slot_type=COMMA}
            |   |   |       |       |___formalParameter {id=1166,slot_type=[lastFormalParameter,formalParameter]}
            |   |   |       |           |___Token:int {id=1171,slot_type=[unannType,altnt_block__primitiveType_3,INT]}
            |   |   |       |           |___Token:y1 {id=1169,slot_type=[variableDeclaratorId,Identifier]}
            |   |   |       |___Token:) {id=1161,slot_type=RPAREN}
            |   |   |___block {id=990,slot_type=[methodBody,block]}
            |   |       |___Token:{ {id=991,slot_type=LBRACE}
            |   |       |___(?) {id=992,slot_type=optional__block_1}
            |   |       |   |___(+) {id=994,slot_type=blockStatements}
            |   |       |       |___returnStatement {id=998,slot_type=[blockStatement,statement,statementWithoutTrailingSubstatement,returnStatement]}
            |   |       |           |___Token:return {id=999,slot_type=RETURN}
            |   |       |           |___(?) {id=1000,slot_type=optional__returnStatement_1}
            |   |       |           |   |___additiveExpression {id=1012,slot_type=[expression,conditionalExpression,conditionalOrExpression,conditionalAndExpression,inclusiveOrExpression,exclusiveOrExpression,andExpression,equalityExpression,relationalExpression,shiftExpression,additiveExpression]}
            |   |       |           |       |___Token:y0 {id=1155,slot_type=[multiplicativeExpression,unaryExpression,aux_rule__unaryExpression_3,unaryExpressionNotPlusMinus,postfixExpression,altnt_block__postfixExpression_3,typeName,Identifier]}
            |   |       |           |       |___(*) {id=1014,slot_type=kleene_star__additiveExpression_1}
            |   |       |           |           |___aux_rule__additiveExpression_2 {id=1015,slot_type=aux_rule__additiveExpression_2}
            |   |       |           |               |___Token:+ {id=1148,slot_type=[altnt_block__additiveExpression_3,ADD]}
            |   |       |           |               |___multiplicativeExpression {id=1017,slot_type=multiplicativeExpression}
            |   |       |           |                   |___aux_rule__primary_4 {id=1114,slot_type=[unaryExpression,aux_rule__unaryExpression_3,unaryExpressionNotPlusMinus,postfixExpression,altnt_block__postfixExpression_3,primary,altnt_block__primary_2,aux_rule__primary_4]}
            |   |       |           |                   |   |___Token:( {id=1115,slot_type=LPAREN}
            |   |       |           |                   |   |___additiveExpression {id=1127,slot_type=[expression,conditionalExpression,conditionalOrExpression,conditionalAndExpression,inclusiveOrExpression,exclusiveOrExpression,andExpression,equalityExpression,relationalExpression,shiftExpression,additiveExpression]}
            |   |       |           |                   |   |   |___Token:x {id=1147,slot_type=[multiplicativeExpression,unaryExpression,aux_rule__unaryExpression_3,unaryExpressionNotPlusMinus,postfixExpression,altnt_block__postfixExpression_3,typeName,Identifier]}
            |   |       |           |                   |   |   |___(*) {id=1129,slot_type=kleene_star__additiveExpression_1}
            |   |       |           |                   |   |       |___aux_rule__additiveExpression_2 {id=1130,slot_type=aux_rule__additiveExpression_2}
            |   |       |           |                   |   |           |___Token:- {id=1140,slot_type=[altnt_block__additiveExpression_3,SUB]}
            |   |       |           |                   |   |           |___Token:x0 {id=1139,slot_type=[multiplicativeExpression,unaryExpression,aux_rule__unaryExpression_3,unaryExpressionNotPlusMinus,postfixExpression,altnt_block__postfixExpression_3,typeName,Identifier]}
            |   |       |           |                   |   |___Token:) {id=1117,slot_type=RPAREN}
            |   |       |           |                   |___(*) {id=1019,slot_type=kleene_star__multiplicativeExpression_1}
            |   |       |           |                       |___aux_rule__multiplicativeExpression_2 {id=1020,slot_type=aux_rule__multiplicativeExpression_2}
            |   |       |           |                       |   |___Token:* {id=1107,slot_type=[altnt_block__multiplicativeExpression_3,MUL]}
            |   |       |           |                       |   |___aux_rule__primary_4 {id=1073,slot_type=[unaryExpression,aux_rule__unaryExpression_3,unaryExpressionNotPlusMinus,postfixExpression,altnt_block__postfixExpression_3,primary,altnt_block__primary_2,aux_rule__primary_4]}
            |   |       |           |                       |       |___Token:( {id=1074,slot_type=LPAREN}
            |   |       |           |                       |       |___additiveExpression {id=1086,slot_type=[expression,conditionalExpression,conditionalOrExpression,conditionalAndExpression,inclusiveOrExpression,exclusiveOrExpression,andExpression,equalityExpression,relationalExpression,shiftExpression,additiveExpression]}
            |   |       |           |                       |       |   |___Token:y1 {id=1106,slot_type=[multiplicativeExpression,unaryExpression,aux_rule__unaryExpression_3,unaryExpressionNotPlusMinus,postfixExpression,altnt_block__postfixExpression_3,typeName,Identifier]}
            |   |       |           |                       |       |   |___(*) {id=1088,slot_type=kleene_star__additiveExpression_1}
            |   |       |           |                       |       |       |___aux_rule__additiveExpression_2 {id=1089,slot_type=aux_rule__additiveExpression_2}
            |   |       |           |                       |       |           |___Token:- {id=1099,slot_type=[altnt_block__additiveExpression_3,SUB]}
            |   |       |           |                       |       |           |___Token:y0 {id=1098,slot_type=[multiplicativeExpression,unaryExpression,aux_rule__unaryExpression_3,unaryExpressionNotPlusMinus,postfixExpression,altnt_block__postfixExpression_3,typeName,Identifier]}
            |   |       |           |                       |       |___Token:) {id=1076,slot_type=RPAREN}
            |   |       |           |                       |___aux_rule__multiplicativeExpression_2 {id=1021,slot_type=aux_rule__multiplicativeExpression_2}
            |   |       |           |                           |___Token:/ {id=1064,slot_type=[altnt_block__multiplicativeExpression_3,DIV]}
            |   |       |           |                           |___aux_rule__primary_4 {id=1030,slot_type=[unaryExpression,aux_rule__unaryExpression_3,unaryExpressionNotPlusMinus,postfixExpression,altnt_block__postfixExpression_3,primary,altnt_block__primary_2,aux_rule__primary_4]}
            |   |       |           |                               |___Token:( {id=1031,slot_type=LPAREN}
            |   |       |           |                               |___additiveExpression {id=1043,slot_type=[expression,conditionalExpression,conditionalOrExpression,conditionalAndExpression,inclusiveOrExpression,exclusiveOrExpression,andExpression,equalityExpression,relationalExpression,shiftExpression,additiveExpression]}
            |   |       |           |                               |   |___Token:x1 {id=1063,slot_type=[multiplicativeExpression,unaryExpression,aux_rule__unaryExpression_3,unaryExpressionNotPlusMinus,postfixExpression,altnt_block__postfixExpression_3,typeName,Identifier]}
            |   |       |           |                               |   |___(*) {id=1045,slot_type=kleene_star__additiveExpression_1}
            |   |       |           |                               |       |___aux_rule__additiveExpression_2 {id=1046,slot_type=aux_rule__additiveExpression_2}
            |   |       |           |                               |           |___Token:- {id=1056,slot_type=[altnt_block__additiveExpression_3,SUB]}
            |   |       |           |                               |           |___Token:x0 {id=1055,slot_type=[multiplicativeExpression,unaryExpression,aux_rule__unaryExpression_3,unaryExpressionNotPlusMinus,postfixExpression,altnt_block__postfixExpression_3,typeName,Identifier]}
            |   |       |           |                               |___Token:) {id=1033,slot_type=RPAREN}
            |   |       |           |___Token:; {id=1001,slot_type=SEMI}
            |   |       |___Token:} {id=993,slot_type=RBRACE}
            |   |___methodDeclaration {id=758,slot_type=[classBodyDeclaration,methodDeclaration]}
            |   |   |___(*) {id=759,slot_type=kleene_star__methodDeclaration_1}
            |   |   |   |___Token:public {id=985,slot_type=[methodModifier,PUBLIC]}
            |   |   |   |___Token:static {id=984,slot_type=[methodModifier,STATIC]}
            |   |   |___methodHeader {id=760,slot_type=methodHeader}
            |   |   |   |___Token:double {id=981,slot_type=[result,unannType,altnt_block__primitiveType_3,DOUBLE]}
            |   |   |   |___methodDeclarator {id=929,slot_type=methodDeclarator}
            |   |   |       |___Token:interpolate {id=930,slot_type=Identifier}
            |   |   |       |___Token:( {id=931,slot_type=LPAREN}
            |   |   |       |___(?) {id=932,slot_type=optional__methodDeclarator_1}
            |   |   |       |   |___aux_rule__formalParameterList_3 {id=935,slot_type=[formalParameterList,aux_rule__formalParameterList_3]}
            |   |   |       |       |___(?) {id=936,slot_type=optional__formalParameterList_2}
            |   |   |       |       |   |___aux_rule__formalParameterList_1 {id=944,slot_type=aux_rule__formalParameterList_1}
            |   |   |       |       |       |___formalParameters {id=945,slot_type=formalParameters}
            |   |   |       |       |       |   |___formalParameter {id=973,slot_type=[altnt_block__formalParameters_5,formalParameter]}
            |   |   |       |       |       |   |   |___Token:double {id=978,slot_type=[unannType,altnt_block__primitiveType_3,DOUBLE]}
            |   |   |       |       |       |   |   |___Token:x {id=976,slot_type=[variableDeclaratorId,Identifier]}
            |   |   |       |       |       |   |___(*) {id=948,slot_type=kleene_star__formalParameters_2}
            |   |   |       |       |       |       |___aux_rule__formalParameters_1 {id=949,slot_type=aux_rule__formalParameters_1}
            |   |   |       |       |       |       |   |___Token:, {id=966,slot_type=COMMA}
            |   |   |       |       |       |       |   |___formalParameter {id=967,slot_type=formalParameter}
            |   |   |       |       |       |       |       |___Token:double {id=972,slot_type=[unannType,altnt_block__primitiveType_3,DOUBLE]}
            |   |   |       |       |       |       |       |___Token:x0 {id=970,slot_type=[variableDeclaratorId,Identifier]}
            |   |   |       |       |       |       |___aux_rule__formalParameters_1 {id=950,slot_type=aux_rule__formalParameters_1}
            |   |   |       |       |       |       |   |___Token:, {id=959,slot_type=COMMA}
            |   |   |       |       |       |       |   |___formalParameter {id=960,slot_type=formalParameter}
            |   |   |       |       |       |       |       |___Token:double {id=965,slot_type=[unannType,altnt_block__primitiveType_3,DOUBLE]}
            |   |   |       |       |       |       |       |___Token:x1 {id=963,slot_type=[variableDeclaratorId,Identifier]}
            |   |   |       |       |       |       |___aux_rule__formalParameters_1 {id=951,slot_type=aux_rule__formalParameters_1}
            |   |   |       |       |       |           |___Token:, {id=952,slot_type=COMMA}
            |   |   |       |       |       |           |___formalParameter {id=953,slot_type=formalParameter}
            |   |   |       |       |       |               |___Token:double {id=958,slot_type=[unannType,altnt_block__primitiveType_3,DOUBLE]}
            |   |   |       |       |       |               |___Token:y0 {id=956,slot_type=[variableDeclaratorId,Identifier]}
            |   |   |       |       |       |___Token:, {id=946,slot_type=COMMA}
            |   |   |       |       |___formalParameter {id=938,slot_type=[lastFormalParameter,formalParameter]}
            |   |   |       |           |___Token:double {id=943,slot_type=[unannType,altnt_block__primitiveType_3,DOUBLE]}
            |   |   |       |           |___Token:y1 {id=941,slot_type=[variableDeclaratorId,Identifier]}
            |   |   |       |___Token:) {id=933,slot_type=RPAREN}
            |   |   |___block {id=762,slot_type=[methodBody,block]}
            |   |       |___Token:{ {id=763,slot_type=LBRACE}
            |   |       |___(?) {id=764,slot_type=optional__block_1}
            |   |       |   |___(+) {id=766,slot_type=blockStatements}
            |   |       |       |___returnStatement {id=770,slot_type=[blockStatement,statement,statementWithoutTrailingSubstatement,returnStatement]}
            |   |       |           |___Token:return {id=771,slot_type=RETURN}
            |   |       |           |___(?) {id=772,slot_type=optional__returnStatement_1}
            |   |       |           |   |___additiveExpression {id=784,slot_type=[expression,conditionalExpression,conditionalOrExpression,conditionalAndExpression,inclusiveOrExpression,exclusiveOrExpression,andExpression,equalityExpression,relationalExpression,shiftExpression,additiveExpression]}
            |   |       |           |       |___Token:y0 {id=927,slot_type=[multiplicativeExpression,unaryExpression,aux_rule__unaryExpression_3,unaryExpressionNotPlusMinus,postfixExpression,altnt_block__postfixExpression_3,typeName,Identifier]}
            |   |       |           |       |___(*) {id=786,slot_type=kleene_star__additiveExpression_1}
            |   |       |           |           |___aux_rule__additiveExpression_2 {id=787,slot_type=aux_rule__additiveExpression_2}
            |   |       |           |               |___Token:+ {id=920,slot_type=[altnt_block__additiveExpression_3,ADD]}
            |   |       |           |               |___multiplicativeExpression {id=789,slot_type=multiplicativeExpression}
            |   |       |           |                   |___aux_rule__primary_4 {id=886,slot_type=[unaryExpression,aux_rule__unaryExpression_3,unaryExpressionNotPlusMinus,postfixExpression,altnt_block__postfixExpression_3,primary,altnt_block__primary_2,aux_rule__primary_4]}
            |   |       |           |                   |   |___Token:( {id=887,slot_type=LPAREN}
            |   |       |           |                   |   |___additiveExpression {id=899,slot_type=[expression,conditionalExpression,conditionalOrExpression,conditionalAndExpression,inclusiveOrExpression,exclusiveOrExpression,andExpression,equalityExpression,relationalExpression,shiftExpression,additiveExpression]}
            |   |       |           |                   |   |   |___Token:x {id=919,slot_type=[multiplicativeExpression,unaryExpression,aux_rule__unaryExpression_3,unaryExpressionNotPlusMinus,postfixExpression,altnt_block__postfixExpression_3,typeName,Identifier]}
            |   |       |           |                   |   |   |___(*) {id=901,slot_type=kleene_star__additiveExpression_1}
            |   |       |           |                   |   |       |___aux_rule__additiveExpression_2 {id=902,slot_type=aux_rule__additiveExpression_2}
            |   |       |           |                   |   |           |___Token:- {id=912,slot_type=[altnt_block__additiveExpression_3,SUB]}
            |   |       |           |                   |   |           |___Token:x0 {id=911,slot_type=[multiplicativeExpression,unaryExpression,aux_rule__unaryExpression_3,unaryExpressionNotPlusMinus,postfixExpression,altnt_block__postfixExpression_3,typeName,Identifier]}
            |   |       |           |                   |   |___Token:) {id=889,slot_type=RPAREN}
            |   |       |           |                   |___(*) {id=791,slot_type=kleene_star__multiplicativeExpression_1}
            |   |       |           |                       |___aux_rule__multiplicativeExpression_2 {id=792,slot_type=aux_rule__multiplicativeExpression_2}
            |   |       |           |                       |   |___Token:* {id=879,slot_type=[altnt_block__multiplicativeExpression_3,MUL]}
            |   |       |           |                       |   |___aux_rule__primary_4 {id=845,slot_type=[unaryExpression,aux_rule__unaryExpression_3,unaryExpressionNotPlusMinus,postfixExpression,altnt_block__postfixExpression_3,primary,altnt_block__primary_2,aux_rule__primary_4]}
            |   |       |           |                       |       |___Token:( {id=846,slot_type=LPAREN}
            |   |       |           |                       |       |___additiveExpression {id=858,slot_type=[expression,conditionalExpression,conditionalOrExpression,conditionalAndExpression,inclusiveOrExpression,exclusiveOrExpression,andExpression,equalityExpression,relationalExpression,shiftExpression,additiveExpression]}
            |   |       |           |                       |       |   |___Token:y1 {id=878,slot_type=[multiplicativeExpression,unaryExpression,aux_rule__unaryExpression_3,unaryExpressionNotPlusMinus,postfixExpression,altnt_block__postfixExpression_3,typeName,Identifier]}
            |   |       |           |                       |       |   |___(*) {id=860,slot_type=kleene_star__additiveExpression_1}
            |   |       |           |                       |       |       |___aux_rule__additiveExpression_2 {id=861,slot_type=aux_rule__additiveExpression_2}
            |   |       |           |                       |       |           |___Token:- {id=871,slot_type=[altnt_block__additiveExpression_3,SUB]}
            |   |       |           |                       |       |           |___Token:y0 {id=870,slot_type=[multiplicativeExpression,unaryExpression,aux_rule__unaryExpression_3,unaryExpressionNotPlusMinus,postfixExpression,altnt_block__postfixExpression_3,typeName,Identifier]}
            |   |       |           |                       |       |___Token:) {id=848,slot_type=RPAREN}
            |   |       |           |                       |___aux_rule__multiplicativeExpression_2 {id=793,slot_type=aux_rule__multiplicativeExpression_2}
            |   |       |           |                           |___Token:/ {id=836,slot_type=[altnt_block__multiplicativeExpression_3,DIV]}
            |   |       |           |                           |___aux_rule__primary_4 {id=802,slot_type=[unaryExpression,aux_rule__unaryExpression_3,unaryExpressionNotPlusMinus,postfixExpression,altnt_block__postfixExpression_3,primary,altnt_block__primary_2,aux_rule__primary_4]}
            |   |       |           |                               |___Token:( {id=803,slot_type=LPAREN}
            |   |       |           |                               |___additiveExpression {id=815,slot_type=[expression,conditionalExpression,conditionalOrExpression,conditionalAndExpression,inclusiveOrExpression,exclusiveOrExpression,andExpression,equalityExpression,relationalExpression,shiftExpression,additiveExpression]}
            |   |       |           |                               |   |___Token:x1 {id=835,slot_type=[multiplicativeExpression,unaryExpression,aux_rule__unaryExpression_3,unaryExpressionNotPlusMinus,postfixExpression,altnt_block__postfixExpression_3,typeName,Identifier]}
            |   |       |           |                               |   |___(*) {id=817,slot_type=kleene_star__additiveExpression_1}
            |   |       |           |                               |       |___aux_rule__additiveExpression_2 {id=818,slot_type=aux_rule__additiveExpression_2}
            |   |       |           |                               |           |___Token:- {id=828,slot_type=[altnt_block__additiveExpression_3,SUB]}
            |   |       |           |                               |           |___Token:x0 {id=827,slot_type=[multiplicativeExpression,unaryExpression,aux_rule__unaryExpression_3,unaryExpressionNotPlusMinus,postfixExpression,altnt_block__postfixExpression_3,typeName,Identifier]}
            |   |       |           |                               |___Token:) {id=805,slot_type=RPAREN}
            |   |       |           |___Token:; {id=773,slot_type=SEMI}
            |   |       |___Token:} {id=765,slot_type=RBRACE}
            |   |___methodDeclaration {id=463,slot_type=[classBodyDeclaration,methodDeclaration]}
            |   |   |___(*) {id=464,slot_type=kleene_star__methodDeclaration_1}
            |   |   |   |___Token:protected {id=757,slot_type=[methodModifier,PROTECTED]}
            |   |   |___methodHeader {id=465,slot_type=methodHeader}
            |   |   |   |___Token:int {id=755,slot_type=[result,unannType,altnt_block__primitiveType_3,INT]}
            |   |   |   |___methodDeclarator {id=739,slot_type=methodDeclarator}
            |   |   |       |___Token:getXByFrequency {id=740,slot_type=Identifier}
            |   |   |       |___Token:( {id=741,slot_type=LPAREN}
            |   |   |       |___(?) {id=742,slot_type=optional__methodDeclarator_1}
            |   |   |       |   |___formalParameter {id=747,slot_type=[formalParameterList,aux_rule__formalParameterList_3,lastFormalParameter,formalParameter]}
            |   |   |       |       |___Token:double {id=752,slot_type=[unannType,altnt_block__primitiveType_3,DOUBLE]}
            |   |   |       |       |___Token:frequency {id=750,slot_type=[variableDeclaratorId,Identifier]}
            |   |   |       |___Token:) {id=743,slot_type=RPAREN}
            |   |   |___block {id=467,slot_type=[methodBody,block]}
            |   |       |___Token:{ {id=468,slot_type=LBRACE}
            |   |       |___(?) {id=469,slot_type=optional__block_1}
            |   |       |   |___(+) {id=471,slot_type=blockStatements}
            |   |       |       |___localVariableDeclarationStatement {id=701,slot_type=[blockStatement,localVariableDeclarationStatement]}
            |   |       |       |   |___localVariableDeclaration {id=702,slot_type=localVariableDeclaration}
            |   |       |       |   |   |___Token:Insets {id=737,slot_type=[unannType,unannClassOrInterfaceType,altnt_block__unannClassOrInterfaceType_3,unannClassType_lfno_unannClassOrInterfaceType,Identifier]}
            |   |       |       |   |   |___variableDeclarator {id=706,slot_type=[variableDeclaratorList,variableDeclarator]}
            |   |       |       |   |       |___Token:insets {id=733,slot_type=[variableDeclaratorId,Identifier]}
            |   |       |       |   |       |___(?) {id=708,slot_type=optional__variableDeclarator_2}
            |   |       |       |   |           |___aux_rule__variableDeclarator_1 {id=709,slot_type=aux_rule__variableDeclarator_1}
            |   |       |       |   |               |___Token:= {id=710,slot_type=ASSIGN}
            |   |       |       |   |               |___Token:null {id=732,slot_type=[variableInitializer,expression,conditionalExpression,conditionalOrExpression,conditionalAndExpression,inclusiveOrExpression,exclusiveOrExpression,andExpression,equalityExpression,relationalExpression,shiftExpression,additiveExpression,multiplicativeExpression,unaryExpression,aux_rule__unaryExpression_3,unaryExpressionNotPlusMinus,postfixExpression,altnt_block__postfixExpression_3,primary,altnt_block__primary_2,literal,NullLiteral]}
            |   |       |       |   |___Token:; {id=703,slot_type=SEMI}
            |   |       |       |___returnStatement {id=476,slot_type=[blockStatement,statement,statementWithoutTrailingSubstatement,returnStatement]}
            |   |       |           |___Token:return {id=477,slot_type=RETURN}
            |   |       |           |___(?) {id=478,slot_type=optional__returnStatement_1}
            |   |       |           |   |___castExpression {id=495,slot_type=[expression,conditionalExpression,conditionalOrExpression,conditionalAndExpression,inclusiveOrExpression,exclusiveOrExpression,andExpression,equalityExpression,relationalExpression,shiftExpression,additiveExpression,multiplicativeExpression,unaryExpression,aux_rule__unaryExpression_3,unaryExpressionNotPlusMinus,castExpression]}
            |   |       |           |       |___Token:( {id=496,slot_type=LPAREN}
            |   |       |           |       |___aux_rule__castExpression_6 {id=498,slot_type=[altnt_block__castExpression_3,aux_rule__castExpression_6]}
            |   |       |           |           |___Token:int {id=700,slot_type=[primitiveType,altnt_block__primitiveType_3,INT]}
            |   |       |           |           |___Token:) {id=500,slot_type=RPAREN}
            |   |       |           |           |___methodInvocation_lfno_primary {id=508,slot_type=[unaryExpression,aux_rule__unaryExpression_3,unaryExpressionNotPlusMinus,postfixExpression,altnt_block__postfixExpression_3,primary,altnt_block__primary_2,methodInvocation_lfno_primary]}
            |   |       |           |               |___aux_rule__methodInvocation_lfno_primary_14 {id=511,slot_type=[altnt_block__methodInvocation_lfno_primary_10,aux_rule__methodInvocation_lfno_primary_14]}
            |   |       |           |               |   |___Token:Math {id=698,slot_type=[typeName,Identifier]}
            |   |       |           |               |   |___Token:. {id=513,slot_type=DOT}
            |   |       |           |               |   |___altnt_block__methodInvocation_13 {id=514,slot_type=altnt_block__methodInvocation_13}
            |   |       |           |               |       |___Token:round {id=515,slot_type=Identifier}
            |   |       |           |               |       |___Token:( {id=516,slot_type=LPAREN}
            |   |       |           |               |       |___(?) {id=517,slot_type=optional__explicitConstructorInvocation_2}
            |   |       |           |               |           |___methodInvocation_lfno_primary {id=538,slot_type=[argumentList,expression,conditionalExpression,conditionalOrExpression,conditionalAndExpression,inclusiveOrExpression,exclusiveOrExpression,andExpression,equalityExpression,relationalExpression,shiftExpression,additiveExpression,multiplicativeExpression,unaryExpression,aux_rule__unaryExpression_3,unaryExpressionNotPlusMinus,postfixExpression,altnt_block__postfixExpression_3,primary,altnt_block__primary_2,methodInvocation_lfno_primary]}
            |   |       |           |               |               |___aux_rule__methodInvocation_lfno_primary_15 {id=541,slot_type=[altnt_block__methodInvocation_lfno_primary_10,aux_rule__methodInvocation_lfno_primary_15]}
            |   |       |           |               |               |   |___Token:interpolate {id=697,slot_type=[altnt_block__methodInvocation_lfno_primary_12,methodName,Identifier]}
            |   |       |           |               |               |   |___Token:( {id=543,slot_type=LPAREN}
            |   |       |           |               |               |   |___(?) {id=544,slot_type=optional__explicitConstructorInvocation_2}
            |   |       |           |               |               |       |___argumentList {id=545,slot_type=argumentList}
            |   |       |           |               |               |           |___Token:frequency {id=695,slot_type=[expression,conditionalExpression,conditionalOrExpression,conditionalAndExpression,inclusiveOrExpression,exclusiveOrExpression,andExpression,equalityExpression,relationalExpression,shiftExpression,additiveExpression,multiplicativeExpression,unaryExpression,aux_rule__unaryExpression_3,unaryExpressionNotPlusMinus,postfixExpression,altnt_block__postfixExpression_3,typeName,Identifier]}
            |   |       |           |               |               |           |___(*) {id=547,slot_type=kleene_star__argumentList_2}
            |   |       |           |               |               |               |___aux_rule__argumentList_1 {id=548,slot_type=aux_rule__argumentList_1}
            |   |       |           |               |               |               |   |___Token:, {id=650,slot_type=COMMA}
            |   |       |           |               |               |               |   |___methodInvocation_lfno_primary {id=670,slot_type=[expression,conditionalExpression,conditionalOrExpression,conditionalAndExpression,inclusiveOrExpression,exclusiveOrExpression,andExpression,equalityExpression,relationalExpression,shiftExpression,additiveExpression,multiplicativeExpression,unaryExpression,aux_rule__unaryExpression_3,unaryExpressionNotPlusMinus,postfixExpression,altnt_block__postfixExpression_3,primary,altnt_block__primary_2,methodInvocation_lfno_primary]}
            |   |       |           |               |               |               |       |___aux_rule__methodInvocation_lfno_primary_15 {id=673,slot_type=[altnt_block__methodInvocation_lfno_primary_10,aux_rule__methodInvocation_lfno_primary_15]}
            |   |       |           |               |               |               |       |   |___Token:getMinSpectrumCoord {id=677,slot_type=[altnt_block__methodInvocation_lfno_primary_12,methodName,Identifier]}
            |   |       |           |               |               |               |       |   |___Token:( {id=675,slot_type=LPAREN}
            |   |       |           |               |               |               |       |___Token:) {id=672,slot_type=RPAREN}
            |   |       |           |               |               |               |___aux_rule__argumentList_1 {id=549,slot_type=aux_rule__argumentList_1}
            |   |       |           |               |               |               |   |___Token:, {id=622,slot_type=COMMA}
            |   |       |           |               |               |               |   |___methodInvocation_lfno_primary {id=642,slot_type=[expression,conditionalExpression,conditionalOrExpression,conditionalAndExpression,inclusiveOrExpression,exclusiveOrExpression,andExpression,equalityExpression,relationalExpression,shiftExpression,additiveExpression,multiplicativeExpression,unaryExpression,aux_rule__unaryExpression_3,unaryExpressionNotPlusMinus,postfixExpression,altnt_block__postfixExpression_3,primary,altnt_block__primary_2,methodInvocation_lfno_primary]}
            |   |       |           |               |               |               |       |___aux_rule__methodInvocation_lfno_primary_15 {id=645,slot_type=[altnt_block__methodInvocation_lfno_primary_10,aux_rule__methodInvocation_lfno_primary_15]}
            |   |       |           |               |               |               |       |   |___Token:getMaxSpectrumCoord {id=649,slot_type=[altnt_block__methodInvocation_lfno_primary_12,methodName,Identifier]}
            |   |       |           |               |               |               |       |   |___Token:( {id=647,slot_type=LPAREN}
            |   |       |           |               |               |               |       |___Token:) {id=644,slot_type=RPAREN}
            |   |       |           |               |               |               |___aux_rule__argumentList_1 {id=550,slot_type=aux_rule__argumentList_1}
            |   |       |           |               |               |               |   |___Token:, {id=597,slot_type=COMMA}
            |   |       |           |               |               |               |   |___typeName {id=615,slot_type=[expression,conditionalExpression,conditionalOrExpression,conditionalAndExpression,inclusiveOrExpression,exclusiveOrExpression,andExpression,equalityExpression,relationalExpression,shiftExpression,additiveExpression,multiplicativeExpression,unaryExpression,aux_rule__unaryExpression_3,unaryExpressionNotPlusMinus,postfixExpression,altnt_block__postfixExpression_3,typeName]}
            |   |       |           |               |               |               |       |___(?) {id=616,slot_type=optional__typeName_2}
            |   |       |           |               |               |               |       |   |___aux_rule__typeName_1 {id=618,slot_type=aux_rule__typeName_1}
            |   |       |           |               |               |               |       |       |___Token:insets {id=621,slot_type=[packageName,Identifier]}
            |   |       |           |               |               |               |       |       |___Token:. {id=620,slot_type=DOT}
            |   |       |           |               |               |               |       |___Token:left {id=617,slot_type=Identifier}
            |   |       |           |               |               |               |___aux_rule__argumentList_1 {id=551,slot_type=aux_rule__argumentList_1}
            |   |       |           |               |               |                   |___Token:, {id=552,slot_type=COMMA}
            |   |       |           |               |               |                   |___additiveExpression {id=563,slot_type=[expression,conditionalExpression,conditionalOrExpression,conditionalAndExpression,inclusiveOrExpression,exclusiveOrExpression,andExpression,equalityExpression,relationalExpression,shiftExpression,additiveExpression]}
            |   |       |           |               |               |                       |___methodInvocation_lfno_primary {id=589,slot_type=[multiplicativeExpression,unaryExpression,aux_rule__unaryExpression_3,unaryExpressionNotPlusMinus,postfixExpression,altnt_block__postfixExpression_3,primary,altnt_block__primary_2,methodInvocation_lfno_primary]}
            |   |       |           |               |               |                       |   |___aux_rule__methodInvocation_lfno_primary_15 {id=592,slot_type=[altnt_block__methodInvocation_lfno_primary_10,aux_rule__methodInvocation_lfno_primary_15]}
            |   |       |           |               |               |                       |   |   |___Token:getWidth {id=596,slot_type=[altnt_block__methodInvocation_lfno_primary_12,methodName,Identifier]}
            |   |       |           |               |               |                       |   |   |___Token:( {id=594,slot_type=LPAREN}
            |   |       |           |               |               |                       |   |___Token:) {id=591,slot_type=RPAREN}
            |   |       |           |               |               |                       |___(*) {id=565,slot_type=kleene_star__additiveExpression_1}
            |   |       |           |               |               |                           |___aux_rule__additiveExpression_2 {id=566,slot_type=aux_rule__additiveExpression_2}
            |   |       |           |               |               |                               |___Token:- {id=581,slot_type=[altnt_block__additiveExpression_3,SUB]}
            |   |       |           |               |               |                               |___typeName {id=574,slot_type=[multiplicativeExpression,unaryExpression,aux_rule__unaryExpression_3,unaryExpressionNotPlusMinus,postfixExpression,altnt_block__postfixExpression_3,typeName]}
            |   |       |           |               |               |                                   |___(?) {id=575,slot_type=optional__typeName_2}
            |   |       |           |               |               |                                   |   |___aux_rule__typeName_1 {id=577,slot_type=aux_rule__typeName_1}
            |   |       |           |               |               |                                   |       |___Token:insets {id=580,slot_type=[packageName,Identifier]}
            |   |       |           |               |               |                                   |       |___Token:. {id=579,slot_type=DOT}
            |   |       |           |               |               |                                   |___Token:right {id=576,slot_type=Identifier}
            |   |       |           |               |               |___Token:) {id=540,slot_type=RPAREN}
            |   |       |           |               |___Token:) {id=510,slot_type=RPAREN}
            |   |       |           |___Token:; {id=479,slot_type=SEMI}
            |   |       |___Token:} {id=470,slot_type=RBRACE}
            |   |___methodDeclaration {id=416,slot_type=[classBodyDeclaration,methodDeclaration]}
            |   |   |___(*) {id=417,slot_type=kleene_star__methodDeclaration_1}
            |   |   |   |___Token:private {id=462,slot_type=[methodModifier,PRIVATE]}
            |   |   |___methodHeader {id=418,slot_type=methodHeader}
            |   |   |   |___Token:int {id=460,slot_type=[result,unannType,altnt_block__primitiveType_3,INT]}
            |   |   |   |___methodDeclarator {id=454,slot_type=methodDeclarator}
            |   |   |       |___Token:getWidth {id=455,slot_type=Identifier}
            |   |   |       |___Token:( {id=456,slot_type=LPAREN}
            |   |   |       |___Token:) {id=457,slot_type=RPAREN}
            |   |   |___block {id=420,slot_type=[methodBody,block]}
            |   |       |___Token:{ {id=421,slot_type=LBRACE}
            |   |       |___(?) {id=422,slot_type=optional__block_1}
            |   |       |   |___(+) {id=424,slot_type=blockStatements}
            |   |       |       |___returnStatement {id=428,slot_type=[blockStatement,statement,statementWithoutTrailingSubstatement,returnStatement]}
            |   |       |           |___Token:return {id=429,slot_type=RETURN}
            |   |       |           |___(?) {id=430,slot_type=optional__returnStatement_1}
            |   |       |           |   |___Token:0 {id=452,slot_type=[expression,conditionalExpression,conditionalOrExpression,conditionalAndExpression,inclusiveOrExpression,exclusiveOrExpression,andExpression,equalityExpression,relationalExpression,shiftExpression,additiveExpression,multiplicativeExpression,unaryExpression,aux_rule__unaryExpression_3,unaryExpressionNotPlusMinus,postfixExpression,altnt_block__postfixExpression_3,primary,altnt_block__primary_2,literal,IntegerLiteral]}
            |   |       |           |___Token:; {id=431,slot_type=SEMI}
            |   |       |___Token:} {id=423,slot_type=RBRACE}
            |   |___methodDeclaration {id=369,slot_type=[classBodyDeclaration,methodDeclaration]}
            |   |   |___(*) {id=370,slot_type=kleene_star__methodDeclaration_1}
            |   |   |   |___Token:private {id=415,slot_type=[methodModifier,PRIVATE]}
            |   |   |___methodHeader {id=371,slot_type=methodHeader}
            |   |   |   |___Token:double {id=413,slot_type=[result,unannType,altnt_block__primitiveType_3,DOUBLE]}
            |   |   |   |___methodDeclarator {id=407,slot_type=methodDeclarator}
            |   |   |       |___Token:getMaxSpectrumCoord {id=408,slot_type=Identifier}
            |   |   |       |___Token:( {id=409,slot_type=LPAREN}
            |   |   |       |___Token:) {id=410,slot_type=RPAREN}
            |   |   |___block {id=373,slot_type=[methodBody,block]}
            |   |       |___Token:{ {id=374,slot_type=LBRACE}
            |   |       |___(?) {id=375,slot_type=optional__block_1}
            |   |       |   |___(+) {id=377,slot_type=blockStatements}
            |   |       |       |___returnStatement {id=381,slot_type=[blockStatement,statement,statementWithoutTrailingSubstatement,returnStatement]}
            |   |       |           |___Token:return {id=382,slot_type=RETURN}
            |   |       |           |___(?) {id=383,slot_type=optional__returnStatement_1}
            |   |       |           |   |___Token:0 {id=405,slot_type=[expression,conditionalExpression,conditionalOrExpression,conditionalAndExpression,inclusiveOrExpression,exclusiveOrExpression,andExpression,equalityExpression,relationalExpression,shiftExpression,additiveExpression,multiplicativeExpression,unaryExpression,aux_rule__unaryExpression_3,unaryExpressionNotPlusMinus,postfixExpression,altnt_block__postfixExpression_3,primary,altnt_block__primary_2,literal,IntegerLiteral]}
            |   |       |           |___Token:; {id=384,slot_type=SEMI}
            |   |       |___Token:} {id=376,slot_type=RBRACE}
            |   |___methodDeclaration {id=322,slot_type=[classBodyDeclaration,methodDeclaration]}
            |   |   |___(*) {id=323,slot_type=kleene_star__methodDeclaration_1}
            |   |   |   |___Token:private {id=368,slot_type=[methodModifier,PRIVATE]}
            |   |   |___methodHeader {id=324,slot_type=methodHeader}
            |   |   |   |___Token:double {id=366,slot_type=[result,unannType,altnt_block__primitiveType_3,DOUBLE]}
            |   |   |   |___methodDeclarator {id=360,slot_type=methodDeclarator}
            |   |   |       |___Token:getMinSpectrumCoord {id=361,slot_type=Identifier}
            |   |   |       |___Token:( {id=362,slot_type=LPAREN}
            |   |   |       |___Token:) {id=363,slot_type=RPAREN}
            |   |   |___block {id=326,slot_type=[methodBody,block]}
            |   |       |___Token:{ {id=327,slot_type=LBRACE}
            |   |       |___(?) {id=328,slot_type=optional__block_1}
            |   |       |   |___(+) {id=330,slot_type=blockStatements}
            |   |       |       |___returnStatement {id=334,slot_type=[blockStatement,statement,statementWithoutTrailingSubstatement,returnStatement]}
            |   |       |           |___Token:return {id=335,slot_type=RETURN}
            |   |       |           |___(?) {id=336,slot_type=optional__returnStatement_1}
            |   |       |           |   |___Token:0 {id=358,slot_type=[expression,conditionalExpression,conditionalOrExpression,conditionalAndExpression,inclusiveOrExpression,exclusiveOrExpression,andExpression,equalityExpression,relationalExpression,shiftExpression,additiveExpression,multiplicativeExpression,unaryExpression,aux_rule__unaryExpression_3,unaryExpressionNotPlusMinus,postfixExpression,altnt_block__postfixExpression_3,primary,altnt_block__primary_2,literal,IntegerLiteral]}
            |   |       |           |___Token:; {id=337,slot_type=SEMI}
            |   |       |___Token:} {id=329,slot_type=RBRACE}
            |   |___methodDeclaration {id=25,slot_type=[classBodyDeclaration,methodDeclaration]}
            |       |___methodHeader {id=26,slot_type=methodHeader}
            |       |   |___Token:void {id=321,slot_type=[result,VOID]}
            |       |   |___methodDeclarator {id=317,slot_type=methodDeclarator}
            |       |       |___Token:foo {id=318,slot_type=Identifier}
            |       |       |___Token:( {id=319,slot_type=LPAREN}
            |       |       |___Token:) {id=320,slot_type=RPAREN}
            |       |___block {id=28,slot_type=[methodBody,block]}
            |           |___Token:{ {id=29,slot_type=LBRACE}
            |           |___(?) {id=30,slot_type=optional__block_1}
            |           |   |___(+) {id=32,slot_type=blockStatements}
            |           |       |___localVariableDeclarationStatement {id=281,slot_type=[blockStatement,localVariableDeclarationStatement]}
            |           |       |   |___localVariableDeclaration {id=282,slot_type=localVariableDeclaration}
            |           |       |   |   |___Token:int {id=315,slot_type=[unannType,altnt_block__primitiveType_3,INT]}
            |           |       |   |   |___variableDeclarator {id=286,slot_type=[variableDeclaratorList,variableDeclarator]}
            |           |       |   |       |___Token:maxBpmIndex {id=313,slot_type=[variableDeclaratorId,Identifier]}
            |           |       |   |       |___(?) {id=288,slot_type=optional__variableDeclarator_2}
            |           |       |   |           |___aux_rule__variableDeclarator_1 {id=289,slot_type=aux_rule__variableDeclarator_1}
            |           |       |   |               |___Token:= {id=290,slot_type=ASSIGN}
            |           |       |   |               |___Token:0 {id=312,slot_type=[variableInitializer,expression,conditionalExpression,conditionalOrExpression,conditionalAndExpression,inclusiveOrExpression,exclusiveOrExpression,andExpression,equalityExpression,relationalExpression,shiftExpression,additiveExpression,multiplicativeExpression,unaryExpression,aux_rule__unaryExpression_3,unaryExpressionNotPlusMinus,postfixExpression,altnt_block__postfixExpression_3,primary,altnt_block__primary_2,literal,IntegerLiteral]}
            |           |       |   |___Token:; {id=283,slot_type=SEMI}
            |           |       |___localVariableDeclarationStatement {id=35,slot_type=[blockStatement,localVariableDeclarationStatement]}
            |           |           |___localVariableDeclaration {id=36,slot_type=localVariableDeclaration}
            |           |           |   |___Token:int {id=280,slot_type=[unannType,altnt_block__primitiveType_3,INT]}
            |           |           |   |___variableDeclarator {id=40,slot_type=[variableDeclaratorList,variableDeclarator]}
            |           |           |       |___Token:xcur {id=278,slot_type=[variableDeclaratorId,Identifier]}
            |           |           |       |___(?) {id=42,slot_type=optional__variableDeclarator_2}
            |           |           |           |___aux_rule__variableDeclarator_1 {id=43,slot_type=aux_rule__variableDeclarator_1}
            |           |           |               |___Token:= {id=44,slot_type=ASSIGN}
            |           |           |               |___methodInvocation_lfno_primary {id=65,slot_type=[variableInitializer,expression,conditionalExpression,conditionalOrExpression,conditionalAndExpression,inclusiveOrExpression,exclusiveOrExpression,andExpression,equalityExpression,relationalExpression,shiftExpression,additiveExpression,multiplicativeExpression,unaryExpression,aux_rule__unaryExpression_3,unaryExpressionNotPlusMinus,postfixExpression,altnt_block__postfixExpression_3,primary,altnt_block__primary_2,methodInvocation_lfno_primary]}
            |           |           |                   |___aux_rule__methodInvocation_lfno_primary_15 {id=68,slot_type=[altnt_block__methodInvocation_lfno_primary_10,aux_rule__methodInvocation_lfno_primary_15]}
            |           |           |                   |   |___Token:getXByFrequency {id=277,slot_type=[altnt_block__methodInvocation_lfno_primary_12,methodName,Identifier]}
            |           |           |                   |   |___Token:( {id=70,slot_type=LPAREN}
            |           |           |                   |   |___(?) {id=71,slot_type=optional__explicitConstructorInvocation_2}
            |           |           |                   |       |___methodInvocation_lfno_primary {id=92,slot_type=[argumentList,expression,conditionalExpression,conditionalOrExpression,conditionalAndExpression,inclusiveOrExpression,exclusiveOrExpression,andExpression,equalityExpression,relationalExpression,shiftExpression,additiveExpression,multiplicativeExpression,unaryExpression,aux_rule__unaryExpression_3,unaryExpressionNotPlusMinus,postfixExpression,altnt_block__postfixExpression_3,primary,altnt_block__primary_2,methodInvocation_lfno_primary]}
            |           |           |                   |           |___aux_rule__methodInvocation_lfno_primary_15 {id=95,slot_type=[altnt_block__methodInvocation_lfno_primary_10,aux_rule__methodInvocation_lfno_primary_15]}
            |           |           |                   |           |   |___Token:maxKey {id=275,slot_type=[altnt_block__methodInvocation_lfno_primary_12,methodName,Identifier]}
            |           |           |                   |           |   |___Token:( {id=97,slot_type=LPAREN}
            |           |           |                   |           |   |___(?) {id=98,slot_type=optional__explicitConstructorInvocation_2}
            |           |           |                   |           |       |___argumentList {id=99,slot_type=argumentList}
            |           |           |                   |           |           |___primary {id=210,slot_type=[expression,conditionalExpression,conditionalOrExpression,conditionalAndExpression,inclusiveOrExpression,exclusiveOrExpression,andExpression,equalityExpression,relationalExpression,shiftExpression,additiveExpression,multiplicativeExpression,unaryExpression,aux_rule__unaryExpression_3,unaryExpressionNotPlusMinus,postfixExpression,altnt_block__postfixExpression_3,primary]}
            |           |           |                   |           |           |   |___methodInvocation_lfno_primary {id=219,slot_type=[altnt_block__primary_2,methodInvocation_lfno_primary]}
            |           |           |                   |           |           |   |   |___aux_rule__methodInvocation_lfno_primary_14 {id=222,slot_type=[altnt_block__methodInvocation_lfno_primary_10,aux_rule__methodInvocation_lfno_primary_14]}
            |           |           |                   |           |           |   |   |   |___Token:IntStream {id=273,slot_type=[typeName,Identifier]}
            |           |           |                   |           |           |   |   |   |___Token:. {id=224,slot_type=DOT}
            |           |           |                   |           |           |   |   |   |___altnt_block__methodInvocation_13 {id=225,slot_type=altnt_block__methodInvocation_13}
            |           |           |                   |           |           |   |   |       |___Token:range {id=226,slot_type=Identifier}
            |           |           |                   |           |           |   |   |       |___Token:( {id=227,slot_type=LPAREN}
            |           |           |                   |           |           |   |   |       |___(?) {id=228,slot_type=optional__explicitConstructorInvocation_2}
            |           |           |                   |           |           |   |   |           |___argumentList {id=229,slot_type=argumentList}
            |           |           |                   |           |           |   |   |               |___Token:0 {id=272,slot_type=[expression,conditionalExpression,conditionalOrExpression,conditionalAndExpression,inclusiveOrExpression,exclusiveOrExpression,andExpression,equalityExpression,relationalExpression,shiftExpression,additiveExpression,multiplicativeExpression,unaryExpression,aux_rule__unaryExpression_3,unaryExpressionNotPlusMinus,postfixExpression,altnt_block__postfixExpression_3,primary,altnt_block__primary_2,literal,IntegerLiteral]}
            |           |           |                   |           |           |   |   |               |___(*) {id=231,slot_type=kleene_star__argumentList_2}
            |           |           |                   |           |           |   |   |                   |___aux_rule__argumentList_1 {id=232,slot_type=aux_rule__argumentList_1}
            |           |           |                   |           |           |   |   |                       |___Token:, {id=233,slot_type=COMMA}
            |           |           |                   |           |           |   |   |                       |___Token:maxBpmIndex {id=252,slot_type=[expression,conditionalExpression,conditionalOrExpression,conditionalAndExpression,inclusiveOrExpression,exclusiveOrExpression,andExpression,equalityExpression,relationalExpression,shiftExpression,additiveExpression,multiplicativeExpression,unaryExpression,aux_rule__unaryExpression_3,unaryExpressionNotPlusMinus,postfixExpression,altnt_block__postfixExpression_3,typeName,Identifier]}
            |           |           |                   |           |           |   |   |___Token:) {id=221,slot_type=RPAREN}
            |           |           |                   |           |           |   |___(*) {id=212,slot_type=kleene_star__primary_1}
            |           |           |                   |           |           |       |___methodInvocation_lf_primary {id=214,slot_type=[primaryNoNewArray_lf_primary,methodInvocation_lf_primary]}
            |           |           |                   |           |           |           |___Token:. {id=215,slot_type=DOT}
            |           |           |                   |           |           |           |___Token:boxed {id=216,slot_type=Identifier}
            |           |           |                   |           |           |           |___Token:( {id=217,slot_type=LPAREN}
            |           |           |                   |           |           |           |___Token:) {id=218,slot_type=RPAREN}
            |           |           |                   |           |           |___(*) {id=101,slot_type=kleene_star__argumentList_2}
            |           |           |                   |           |               |___aux_rule__argumentList_1 {id=102,slot_type=aux_rule__argumentList_1}
            |           |           |                   |           |                   |___Token:, {id=103,slot_type=COMMA}
            |           |           |                   |           |                   |___lambdaExpression {id=105,slot_type=[expression,lambdaExpression]}
            |           |           |                   |           |                       |___Token:i {id=193,slot_type=[lambdaParameters,Identifier]}
            |           |           |                   |           |                       |___Token:-> {id=107,slot_type=ARROW}
            |           |           |                   |           |                       |___methodInvocation_lfno_primary {id=128,slot_type=[lambdaBody,expression,conditionalExpression,conditionalOrExpression,conditionalAndExpression,inclusiveOrExpression,exclusiveOrExpression,andExpression,equalityExpression,relationalExpression,shiftExpression,additiveExpression,multiplicativeExpression,unaryExpression,aux_rule__unaryExpression_3,unaryExpressionNotPlusMinus,postfixExpression,altnt_block__postfixExpression_3,primary,altnt_block__primary_2,methodInvocation_lfno_primary]}
            |           |           |                   |           |                           |___aux_rule__methodInvocation_lfno_primary_14 {id=131,slot_type=[altnt_block__methodInvocation_lfno_primary_10,aux_rule__methodInvocation_lfno_primary_14]}
            |           |           |                   |           |                           |   |___Token:Math {id=192,slot_type=[typeName,Identifier]}
            |           |           |                   |           |                           |   |___Token:. {id=133,slot_type=DOT}
            |           |           |                   |           |                           |   |___altnt_block__methodInvocation_13 {id=134,slot_type=altnt_block__methodInvocation_13}
            |           |           |                   |           |                           |       |___Token:abs {id=135,slot_type=Identifier}
            |           |           |                   |           |                           |       |___Token:( {id=136,slot_type=LPAREN}
            |           |           |                   |           |                           |       |___(?) {id=137,slot_type=optional__explicitConstructorInvocation_2}
            |           |           |                   |           |                           |           |___altnt_block__arrayAccess_lfno_primary_3 {id=159,slot_type=[argumentList,expression,conditionalExpression,conditionalOrExpression,conditionalAndExpression,inclusiveOrExpression,exclusiveOrExpression,andExpression,equalityExpression,relationalExpression,shiftExpression,additiveExpression,multiplicativeExpression,unaryExpression,aux_rule__unaryExpression_3,unaryExpressionNotPlusMinus,postfixExpression,altnt_block__postfixExpression_3,primary,altnt_block__primary_2,arrayAccess_lfno_primary,altnt_block__arrayAccess_lfno_primary_3]}
            |           |           |                   |           |                           |               |___methodInvocation_lfno_primary {id=182,slot_type=[altnt_block__arrayAccess_lfno_primary_4,methodInvocation_lfno_primary]}
            |           |           |                   |           |                           |               |   |___aux_rule__methodInvocation_lfno_primary_14 {id=185,slot_type=[altnt_block__methodInvocation_lfno_primary_10,aux_rule__methodInvocation_lfno_primary_14]}
            |           |           |                   |           |                           |               |   |   |___Token:spectrum {id=191,slot_type=[typeName,Identifier]}
            |           |           |                   |           |                           |               |   |   |___Token:. {id=187,slot_type=DOT}
            |           |           |                   |           |                           |               |   |   |___altnt_block__methodInvocation_13 {id=188,slot_type=altnt_block__methodInvocation_13}
            |           |           |                   |           |                           |               |   |       |___Token:getEnergy {id=189,slot_type=Identifier}
            |           |           |                   |           |                           |               |   |       |___Token:( {id=190,slot_type=LPAREN}
            |           |           |                   |           |                           |               |   |___Token:) {id=184,slot_type=RPAREN}
            |           |           |                   |           |                           |               |___Token:[ {id=161,slot_type=LBRACK}
            |           |           |                   |           |                           |               |___Token:i {id=181,slot_type=[expression,conditionalExpression,conditionalOrExpression,conditionalAndExpression,inclusiveOrExpression,exclusiveOrExpression,andExpression,equalityExpression,relationalExpression,shiftExpression,additiveExpression,multiplicativeExpression,unaryExpression,aux_rule__unaryExpression_3,unaryExpressionNotPlusMinus,postfixExpression,altnt_block__postfixExpression_3,typeName,Identifier]}
            |           |           |                   |           |                           |               |___Token:] {id=163,slot_type=RBRACK}
            |           |           |                   |           |                           |___Token:) {id=130,slot_type=RPAREN}
            |           |           |                   |           |___Token:) {id=94,slot_type=RPAREN}
            |           |           |                   |___Token:) {id=67,slot_type=RPAREN}
            |           |           |___Token:; {id=37,slot_type=SEMI}
            |           |___Token:} {id=31,slot_type=RBRACE}
            |___Token:} {id=13,slot_type=RBRACE}
------------------------------------------------------------


=================Reducing node 3, size=447==================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
        public static class Spectrum {
            public double[] getEnergy() {
                return new double[0];
            }
        }
        protected Spectrum spectrum;
        public static class Ref<T> {
            T value;
            public Ref() {
            }
            public Ref(T value) {
                this.value = value;
            }
            public boolean isNull() {
                return value == null;
            }
            public T get() {
                return value;
            }
            public void set(T value) {
                this.value = value;
            }
        }
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
            Ref<Double> max = new Ref<>();
            Ref<T> index = new Ref<>();
            stream.forEach(v -> {
                Double value = function.apply(v);
                if (max.isNull() || value > max.get()) {
                    max.set(value);
                    index.set(v);
                }
            });
            return index.get();
        }
        public static int interpolate(int x, int x0, int x1, int y0, int y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
            Insets insets = null;
            return (int) Math.round(interpolate(frequency, getMinSpectrumCoord(), getMaxSpectrumCoord(), insets.left, getWidth() - insets.right));
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
        void foo() {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i -> Math.abs(spectrum.getEnergy()[i])));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [kleene_star:kleene_star__compilationUnit_3]dd@1
    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 3
------------------------------------------------------------


=================Reducing node 6, size=447==================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
        public static class Spectrum {
            public double[] getEnergy() {
                return new double[0];
            }
        }
        protected Spectrum spectrum;
        public static class Ref<T> {
            T value;
            public Ref() {
            }
            public Ref(T value) {
                this.value = value;
            }
            public boolean isNull() {
                return value == null;
            }
            public T get() {
                return value;
            }
            public void set(T value) {
                this.value = value;
            }
        }
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
            Ref<Double> max = new Ref<>();
            Ref<T> index = new Ref<>();
            stream.forEach(v -> {
                Double value = function.apply(v);
                if (max.isNull() || value > max.get()) {
                    max.set(value);
                    index.set(v);
                }
            });
            return index.get();
        }
        public static int interpolate(int x, int x0, int x1, int y0, int y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
            Insets insets = null;
            return (int) Math.round(interpolate(frequency, getMinSpectrumCoord(), getMaxSpectrumCoord(), insets.left, getWidth() - insets.right));
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
        void foo() {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i -> Math.abs(spectrum.getEnergy()[i])));
        }
    }
------------------------------------------------------------


==============Node edit action set cache hit.===============

    NodeDeletionAction{target_node=6}
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [regular node]compatible replacement
    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
        public static class Spectrum {
            public double[] getEnergy() {
                return new double[0];
            }
        }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
        public static class Spectrum {
            public double[] getEnergy() {
                return new double[0];
            }
        }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [regular node]compatible replacement
    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
        public static class Ref<T> {
            T value;
            public Ref() {
            }
            public Ref(T value) {
                this.value = value;
            }
            public boolean isNull() {
                return value == null;
            }
            public T get() {
                return value;
            }
            public void set(T value) {
                this.value = value;
            }
        }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
        public static class Ref<T> {
            T value;
            public Ref() {
            }
            public Ref(T value) {
                this.value = value;
            }
            public boolean isNull() {
                return value == null;
            }
            public T get() {
                return value;
            }
            public void set(T value) {
                this.value = value;
            }
        }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 6
------------------------------------------------------------


=================Reducing node 10, size=447=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
        public static class Spectrum {
            public double[] getEnergy() {
                return new double[0];
            }
        }
        protected Spectrum spectrum;
        public static class Ref<T> {
            T value;
            public Ref() {
            }
            public Ref(T value) {
                this.value = value;
            }
            public boolean isNull() {
                return value == null;
            }
            public T get() {
                return value;
            }
            public void set(T value) {
                this.value = value;
            }
        }
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
            Ref<Double> max = new Ref<>();
            Ref<T> index = new Ref<>();
            stream.forEach(v -> {
                Double value = function.apply(v);
                if (max.isNull() || value > max.get()) {
                    max.set(value);
                    index.set(v);
                }
            });
            return index.get();
        }
        public static int interpolate(int x, int x0, int x1, int y0, int y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
            Insets insets = null;
            return (int) Math.round(interpolate(frequency, getMinSpectrumCoord(), getMaxSpectrumCoord(), insets.left, getWidth() - insets.right));
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
        void foo() {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i -> Math.abs(spectrum.getEnergy()[i])));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [regular node]compatible replacement
    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main
                                     {
            public double[] getEnergy() {
                return new double[0];
            }
        }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main
                                     {
            public double[] getEnergy() {
                return new double[0];
            }
        }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [regular node]compatible replacement
    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main
                                   {
            T value;
            public Ref() {
            }
            public Ref(T value) {
                this.value = value;
            }
            public boolean isNull() {
                return value == null;
            }
            public T get() {
                return value;
            }
            public void set(T value) {
                this.value = value;
            }
        }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main
                                   {
            T value;
            public Ref() {
            }
            public Ref(T value) {
                this.value = value;
            }
            public boolean isNull() {
                return value == null;
            }
            public T get() {
                return value;
            }
            public void set(T value) {
                this.value = value;
            }
        }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 10
------------------------------------------------------------


=================Reducing node 12, size=447=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
        public static class Spectrum {
            public double[] getEnergy() {
                return new double[0];
            }
        }
        protected Spectrum spectrum;
        public static class Ref<T> {
            T value;
            public Ref() {
            }
            public Ref(T value) {
                this.value = value;
            }
            public boolean isNull() {
                return value == null;
            }
            public T get() {
                return value;
            }
            public void set(T value) {
                this.value = value;
            }
        }
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
            Ref<Double> max = new Ref<>();
            Ref<T> index = new Ref<>();
            stream.forEach(v -> {
                Double value = function.apply(v);
                if (max.isNull() || value > max.get()) {
                    max.set(value);
                    index.set(v);
                }
            });
            return index.get();
        }
        public static int interpolate(int x, int x0, int x1, int y0, int y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
            Insets insets = null;
            return (int) Math.round(interpolate(frequency, getMinSpectrumCoord(), getMaxSpectrumCoord(), insets.left, getWidth() - insets.right));
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
        void foo() {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i -> Math.abs(spectrum.getEnergy()[i])));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [kleene_star:kleene_star__classBody_1]dd@11
    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [kleene_star:kleene_star__classBody_1]dd@5
    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
        public static class Spectrum {
            public double[] getEnergy() {
                return new double[0];
            }
        }
        protected Spectrum spectrum;
        public static class Ref<T> {
            T value;
            public Ref() {
            }
            public Ref(T value) {
                this.value = value;
            }
            public boolean isNull() {
                return value == null;
            }
            public T get() {
                return value;
            }
            public void set(T value) {
                this.value = value;
            }
        }
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
            Ref<Double> max = new Ref<>();
            Ref<T> index = new Ref<>();
            stream.forEach(v -> {
                Double value = function.apply(v);
                if (max.isNull() || value > max.get()) {
                    max.set(value);
                    index.set(v);
                }
            });
            return index.get();
        }
        public static int interpolate(int x, int x0, int x1, int y0, int y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
        public static class Spectrum {
            public double[] getEnergy() {
                return new double[0];
            }
        }
        protected Spectrum spectrum;
        public static class Ref<T> {
            T value;
            public Ref() {
            }
            public Ref(T value) {
                this.value = value;
            }
            public boolean isNull() {
                return value == null;
            }
            public T get() {
                return value;
            }
            public void set(T value) {
                this.value = value;
            }
        }
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
            Ref<Double> max = new Ref<>();
            Ref<T> index = new Ref<>();
            stream.forEach(v -> {
                Double value = function.apply(v);
                if (max.isNull() || value > max.get()) {
                    max.set(value);
                    index.set(v);
                }
            });
            return index.get();
        }
        public static int interpolate(int x, int x0, int x1, int y0, int y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [kleene_star:kleene_star__classBody_1]dd@2
    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
        public static class Spectrum {
            public double[] getEnergy() {
                return new double[0];
            }
        }
        protected Spectrum spectrum;
        public static class Ref<T> {
            T value;
            public Ref() {
            }
            public Ref(T value) {
                this.value = value;
            }
            public boolean isNull() {
                return value == null;
            }
            public T get() {
                return value;
            }
            public void set(T value) {
                this.value = value;
            }
        }
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
            Ref<Double> max = new Ref<>();
            Ref<T> index = new Ref<>();
            stream.forEach(v -> {
                Double value = function.apply(v);
                if (max.isNull() || value > max.get()) {
                    max.set(value);
                    index.set(v);
                }
            });
            return index.get();
        }
        public static int interpolate(int x, int x0, int x1, int y0, int y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
            Insets insets = null;
            return (int) Math.round(interpolate(frequency, getMinSpectrumCoord(), getMaxSpectrumCoord(), insets.left, getWidth() - insets.right));
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
        public static class Spectrum {
            public double[] getEnergy() {
                return new double[0];
            }
        }
        protected Spectrum spectrum;
        public static class Ref<T> {
            T value;
            public Ref() {
            }
            public Ref(T value) {
                this.value = value;
            }
            public boolean isNull() {
                return value == null;
            }
            public T get() {
                return value;
            }
            public void set(T value) {
                this.value = value;
            }
        }
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
            Ref<Double> max = new Ref<>();
            Ref<T> index = new Ref<>();
            stream.forEach(v -> {
                Double value = function.apply(v);
                if (max.isNull() || value > max.get()) {
                    max.set(value);
                    index.set(v);
                }
            });
            return index.get();
        }
        public static int interpolate(int x, int x0, int x1, int y0, int y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
            Insets insets = null;
            return (int) Math.round(interpolate(frequency, getMinSpectrumCoord(), getMaxSpectrumCoord(), insets.left, getWidth() - insets.right));
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [kleene_star:kleene_star__classBody_1]dd@1
    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
        public static class Spectrum {
            public double[] getEnergy() {
                return new double[0];
            }
        }
        protected Spectrum spectrum;
        public static class Ref<T> {
            T value;
            public Ref() {
            }
            public Ref(T value) {
                this.value = value;
            }
            public boolean isNull() {
                return value == null;
            }
            public T get() {
                return value;
            }
            public void set(T value) {
                this.value = value;
            }
        }
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
            Ref<Double> max = new Ref<>();
            Ref<T> index = new Ref<>();
            stream.forEach(v -> {
                Double value = function.apply(v);
                if (max.isNull() || value > max.get()) {
                    max.set(value);
                    index.set(v);
                }
            });
            return index.get();
        }
        public static int interpolate(int x, int x0, int x1, int y0, int y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
            Insets insets = null;
            return (int) Math.round(interpolate(frequency, getMinSpectrumCoord(), getMaxSpectrumCoord(), insets.left, getWidth() - insets.right));
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
        public static class Spectrum {
            public double[] getEnergy() {
                return new double[0];
            }
        }
        protected Spectrum spectrum;
        public static class Ref<T> {
            T value;
            public Ref() {
            }
            public Ref(T value) {
                this.value = value;
            }
            public boolean isNull() {
                return value == null;
            }
            public T get() {
                return value;
            }
            public void set(T value) {
                this.value = value;
            }
        }
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
            Ref<Double> max = new Ref<>();
            Ref<T> index = new Ref<>();
            stream.forEach(v -> {
                Double value = function.apply(v);
                if (max.isNull() || value > max.get()) {
                    max.set(value);
                    index.set(v);
                }
            });
            return index.get();
        }
        public static int interpolate(int x, int x0, int x1, int y0, int y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
            Insets insets = null;
            return (int) Math.round(interpolate(frequency, getMinSpectrumCoord(), getMaxSpectrumCoord(), insets.left, getWidth() - insets.right));
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [kleene_star:kleene_star__classBody_1]dd@1
    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
        public static class Spectrum {
            public double[] getEnergy() {
                return new double[0];
            }
        }
        protected Spectrum spectrum;
        public static class Ref<T> {
            T value;
            public Ref() {
            }
            public Ref(T value) {
                this.value = value;
            }
            public boolean isNull() {
                return value == null;
            }
            public T get() {
                return value;
            }
            public void set(T value) {
                this.value = value;
            }
        }
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
            Ref<Double> max = new Ref<>();
            Ref<T> index = new Ref<>();
            stream.forEach(v -> {
                Double value = function.apply(v);
                if (max.isNull() || value > max.get()) {
                    max.set(value);
                    index.set(v);
                }
            });
            return index.get();
        }
        public static int interpolate(int x, int x0, int x1, int y0, int y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
            Insets insets = null;
            return (int) Math.round(interpolate(frequency, getMinSpectrumCoord(), getMaxSpectrumCoord(), insets.left, getWidth() - insets.right));
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        void foo() {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i -> Math.abs(spectrum.getEnergy()[i])));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
        public static class Spectrum {
            public double[] getEnergy() {
                return new double[0];
            }
        }
        protected Spectrum spectrum;
        public static class Ref<T> {
            T value;
            public Ref() {
            }
            public Ref(T value) {
                this.value = value;
            }
            public boolean isNull() {
                return value == null;
            }
            public T get() {
                return value;
            }
            public void set(T value) {
                this.value = value;
            }
        }
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
            Ref<Double> max = new Ref<>();
            Ref<T> index = new Ref<>();
            stream.forEach(v -> {
                Double value = function.apply(v);
                if (max.isNull() || value > max.get()) {
                    max.set(value);
                    index.set(v);
                }
            });
            return index.get();
        }
        public static int interpolate(int x, int x0, int x1, int y0, int y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
            Insets insets = null;
            return (int) Math.round(interpolate(frequency, getMinSpectrumCoord(), getMaxSpectrumCoord(), insets.left, getWidth() - insets.right));
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        void foo() {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i -> Math.abs(spectrum.getEnergy()[i])));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [kleene_star:kleene_star__classBody_1]dd@3
    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
        public static class Spectrum {
            public double[] getEnergy() {
                return new double[0];
            }
        }
        protected Spectrum spectrum;
        public static class Ref<T> {
            T value;
            public Ref() {
            }
            public Ref(T value) {
                this.value = value;
            }
            public boolean isNull() {
                return value == null;
            }
            public T get() {
                return value;
            }
            public void set(T value) {
                this.value = value;
            }
        }
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
            Ref<Double> max = new Ref<>();
            Ref<T> index = new Ref<>();
            stream.forEach(v -> {
                Double value = function.apply(v);
                if (max.isNull() || value > max.get()) {
                    max.set(value);
                    index.set(v);
                }
            });
            return index.get();
        }
        public static int interpolate(int x, int x0, int x1, int y0, int y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
        void foo() {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i -> Math.abs(spectrum.getEnergy()[i])));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
        public static class Spectrum {
            public double[] getEnergy() {
                return new double[0];
            }
        }
        protected Spectrum spectrum;
        public static class Ref<T> {
            T value;
            public Ref() {
            }
            public Ref(T value) {
                this.value = value;
            }
            public boolean isNull() {
                return value == null;
            }
            public T get() {
                return value;
            }
            public void set(T value) {
                this.value = value;
            }
        }
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
            Ref<Double> max = new Ref<>();
            Ref<T> index = new Ref<>();
            stream.forEach(v -> {
                Double value = function.apply(v);
                if (max.isNull() || value > max.get()) {
                    max.set(value);
                    index.set(v);
                }
            });
            return index.get();
        }
        public static int interpolate(int x, int x0, int x1, int y0, int y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
        void foo() {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i -> Math.abs(spectrum.getEnergy()[i])));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [kleene_star:kleene_star__classBody_1]dd@1
    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
        public static class Spectrum {
            public double[] getEnergy() {
                return new double[0];
            }
        }
        protected Spectrum spectrum;
        public static class Ref<T> {
            T value;
            public Ref() {
            }
            public Ref(T value) {
                this.value = value;
            }
            public boolean isNull() {
                return value == null;
            }
            public T get() {
                return value;
            }
            public void set(T value) {
                this.value = value;
            }
        }
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
            Ref<Double> max = new Ref<>();
            Ref<T> index = new Ref<>();
            stream.forEach(v -> {
                Double value = function.apply(v);
                if (max.isNull() || value > max.get()) {
                    max.set(value);
                    index.set(v);
                }
            });
            return index.get();
        }
        public static int interpolate(int x, int x0, int x1, int y0, int y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
            Insets insets = null;
            return (int) Math.round(interpolate(frequency, getMinSpectrumCoord(), getMaxSpectrumCoord(), insets.left, getWidth() - insets.right));
        }
        private int getWidth() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
        void foo() {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i -> Math.abs(spectrum.getEnergy()[i])));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
        public static class Spectrum {
            public double[] getEnergy() {
                return new double[0];
            }
        }
        protected Spectrum spectrum;
        public static class Ref<T> {
            T value;
            public Ref() {
            }
            public Ref(T value) {
                this.value = value;
            }
            public boolean isNull() {
                return value == null;
            }
            public T get() {
                return value;
            }
            public void set(T value) {
                this.value = value;
            }
        }
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
            Ref<Double> max = new Ref<>();
            Ref<T> index = new Ref<>();
            stream.forEach(v -> {
                Double value = function.apply(v);
                if (max.isNull() || value > max.get()) {
                    max.set(value);
                    index.set(v);
                }
            });
            return index.get();
        }
        public static int interpolate(int x, int x0, int x1, int y0, int y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
            Insets insets = null;
            return (int) Math.round(interpolate(frequency, getMinSpectrumCoord(), getMaxSpectrumCoord(), insets.left, getWidth() - insets.right));
        }
        private int getWidth() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
        void foo() {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i -> Math.abs(spectrum.getEnergy()[i])));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [kleene_star:kleene_star__classBody_1]dd@2
    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
        public static class Spectrum {
            public double[] getEnergy() {
                return new double[0];
            }
        }
        protected Spectrum spectrum;
        public static class Ref<T> {
            T value;
            public Ref() {
            }
            public Ref(T value) {
                this.value = value;
            }
            public boolean isNull() {
                return value == null;
            }
            public T get() {
                return value;
            }
            public void set(T value) {
                this.value = value;
            }
        }
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
            Ref<Double> max = new Ref<>();
            Ref<T> index = new Ref<>();
            stream.forEach(v -> {
                Double value = function.apply(v);
                if (max.isNull() || value > max.get()) {
                    max.set(value);
                    index.set(v);
                }
            });
            return index.get();
        }
        public static int interpolate(int x, int x0, int x1, int y0, int y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
        void foo() {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i -> Math.abs(spectrum.getEnergy()[i])));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
        public static class Spectrum {
            public double[] getEnergy() {
                return new double[0];
            }
        }
        protected Spectrum spectrum;
        public static class Ref<T> {
            T value;
            public Ref() {
            }
            public Ref(T value) {
                this.value = value;
            }
            public boolean isNull() {
                return value == null;
            }
            public T get() {
                return value;
            }
            public void set(T value) {
                this.value = value;
            }
        }
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
            Ref<Double> max = new Ref<>();
            Ref<T> index = new Ref<>();
            stream.forEach(v -> {
                Double value = function.apply(v);
                if (max.isNull() || value > max.get()) {
                    max.set(value);
                    index.set(v);
                }
            });
            return index.get();
        }
        public static int interpolate(int x, int x0, int x1, int y0, int y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
        void foo() {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i -> Math.abs(spectrum.getEnergy()[i])));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [kleene_star:kleene_star__classBody_1]dd@1
    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
        public static class Spectrum {
            public double[] getEnergy() {
                return new double[0];
            }
        }
        protected Spectrum spectrum;
        public static class Ref<T> {
            T value;
            public Ref() {
            }
            public Ref(T value) {
                this.value = value;
            }
            public boolean isNull() {
                return value == null;
            }
            public T get() {
                return value;
            }
            public void set(T value) {
                this.value = value;
            }
        }
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
            Ref<Double> max = new Ref<>();
            Ref<T> index = new Ref<>();
            stream.forEach(v -> {
                Double value = function.apply(v);
                if (max.isNull() || value > max.get()) {
                    max.set(value);
                    index.set(v);
                }
            });
            return index.get();
        }
        public static int interpolate(int x, int x0, int x1, int y0, int y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
            Insets insets = null;
            return (int) Math.round(interpolate(frequency, getMinSpectrumCoord(), getMaxSpectrumCoord(), insets.left, getWidth() - insets.right));
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
        void foo() {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i -> Math.abs(spectrum.getEnergy()[i])));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
        public static class Spectrum {
            public double[] getEnergy() {
                return new double[0];
            }
        }
        protected Spectrum spectrum;
        public static class Ref<T> {
            T value;
            public Ref() {
            }
            public Ref(T value) {
                this.value = value;
            }
            public boolean isNull() {
                return value == null;
            }
            public T get() {
                return value;
            }
            public void set(T value) {
                this.value = value;
            }
        }
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
            Ref<Double> max = new Ref<>();
            Ref<T> index = new Ref<>();
            stream.forEach(v -> {
                Double value = function.apply(v);
                if (max.isNull() || value > max.get()) {
                    max.set(value);
                    index.set(v);
                }
            });
            return index.get();
        }
        public static int interpolate(int x, int x0, int x1, int y0, int y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
            Insets insets = null;
            return (int) Math.round(interpolate(frequency, getMinSpectrumCoord(), getMaxSpectrumCoord(), insets.left, getWidth() - insets.right));
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
        void foo() {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i -> Math.abs(spectrum.getEnergy()[i])));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [kleene_star:kleene_star__classBody_1]dd@1
    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
        public static class Spectrum {
            public double[] getEnergy() {
                return new double[0];
            }
        }
        protected Spectrum spectrum;
        public static class Ref<T> {
            T value;
            public Ref() {
            }
            public Ref(T value) {
                this.value = value;
            }
            public boolean isNull() {
                return value == null;
            }
            public T get() {
                return value;
            }
            public void set(T value) {
                this.value = value;
            }
        }
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
            Ref<Double> max = new Ref<>();
            Ref<T> index = new Ref<>();
            stream.forEach(v -> {
                Double value = function.apply(v);
                if (max.isNull() || value > max.get()) {
                    max.set(value);
                    index.set(v);
                }
            });
            return index.get();
        }
        public static int interpolate(int x, int x0, int x1, int y0, int y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
        void foo() {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i -> Math.abs(spectrum.getEnergy()[i])));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
        public static class Spectrum {
            public double[] getEnergy() {
                return new double[0];
            }
        }
        protected Spectrum spectrum;
        public static class Ref<T> {
            T value;
            public Ref() {
            }
            public Ref(T value) {
                this.value = value;
            }
            public boolean isNull() {
                return value == null;
            }
            public T get() {
                return value;
            }
            public void set(T value) {
                this.value = value;
            }
        }
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
            Ref<Double> max = new Ref<>();
            Ref<T> index = new Ref<>();
            stream.forEach(v -> {
                Double value = function.apply(v);
                if (max.isNull() || value > max.get()) {
                    max.set(value);
                    index.set(v);
                }
            });
            return index.get();
        }
        public static int interpolate(int x, int x0, int x1, int y0, int y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
        void foo() {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i -> Math.abs(spectrum.getEnergy()[i])));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [kleene_star:kleene_star__classBody_1]dd@6
    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
        protected int getXByFrequency(double frequency) {
            Insets insets = null;
            return (int) Math.round(interpolate(frequency, getMinSpectrumCoord(), getMaxSpectrumCoord(), insets.left, getWidth() - insets.right));
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
        void foo() {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i -> Math.abs(spectrum.getEnergy()[i])));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
        protected int getXByFrequency(double frequency) {
            Insets insets = null;
            return (int) Math.round(interpolate(frequency, getMinSpectrumCoord(), getMaxSpectrumCoord(), insets.left, getWidth() - insets.right));
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
        void foo() {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i -> Math.abs(spectrum.getEnergy()[i])));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [kleene_star:kleene_star__classBody_1]dd@3
    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
        public static class Spectrum {
            public double[] getEnergy() {
                return new double[0];
            }
        }
        protected Spectrum spectrum;
        public static class Ref<T> {
            T value;
            public Ref() {
            }
            public Ref(T value) {
                this.value = value;
            }
            public boolean isNull() {
                return value == null;
            }
            public T get() {
                return value;
            }
            public void set(T value) {
                this.value = value;
            }
        }
        protected int getXByFrequency(double frequency) {
            Insets insets = null;
            return (int) Math.round(interpolate(frequency, getMinSpectrumCoord(), getMaxSpectrumCoord(), insets.left, getWidth() - insets.right));
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
        void foo() {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i -> Math.abs(spectrum.getEnergy()[i])));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
        public static class Spectrum {
            public double[] getEnergy() {
                return new double[0];
            }
        }
        protected Spectrum spectrum;
        public static class Ref<T> {
            T value;
            public Ref() {
            }
            public Ref(T value) {
                this.value = value;
            }
            public boolean isNull() {
                return value == null;
            }
            public T get() {
                return value;
            }
            public void set(T value) {
                this.value = value;
            }
        }
        protected int getXByFrequency(double frequency) {
            Insets insets = null;
            return (int) Math.round(interpolate(frequency, getMinSpectrumCoord(), getMaxSpectrumCoord(), insets.left, getWidth() - insets.right));
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
        void foo() {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i -> Math.abs(spectrum.getEnergy()[i])));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [kleene_star:kleene_star__classBody_1]dd@1
    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
        public static class Spectrum {
            public double[] getEnergy() {
                return new double[0];
            }
        }
        protected Spectrum spectrum;
        public static class Ref<T> {
            T value;
            public Ref() {
            }
            public Ref(T value) {
                this.value = value;
            }
            public boolean isNull() {
                return value == null;
            }
            public T get() {
                return value;
            }
            public void set(T value) {
                this.value = value;
            }
        }
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
            Ref<Double> max = new Ref<>();
            Ref<T> index = new Ref<>();
            stream.forEach(v -> {
                Double value = function.apply(v);
                if (max.isNull() || value > max.get()) {
                    max.set(value);
                    index.set(v);
                }
            });
            return index.get();
        }
        public static int interpolate(int x, int x0, int x1, int y0, int y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
            Insets insets = null;
            return (int) Math.round(interpolate(frequency, getMinSpectrumCoord(), getMaxSpectrumCoord(), insets.left, getWidth() - insets.right));
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
        void foo() {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i -> Math.abs(spectrum.getEnergy()[i])));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
        public static class Spectrum {
            public double[] getEnergy() {
                return new double[0];
            }
        }
        protected Spectrum spectrum;
        public static class Ref<T> {
            T value;
            public Ref() {
            }
            public Ref(T value) {
                this.value = value;
            }
            public boolean isNull() {
                return value == null;
            }
            public T get() {
                return value;
            }
            public void set(T value) {
                this.value = value;
            }
        }
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
            Ref<Double> max = new Ref<>();
            Ref<T> index = new Ref<>();
            stream.forEach(v -> {
                Double value = function.apply(v);
                if (max.isNull() || value > max.get()) {
                    max.set(value);
                    index.set(v);
                }
            });
            return index.get();
        }
        public static int interpolate(int x, int x0, int x1, int y0, int y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
            Insets insets = null;
            return (int) Math.round(interpolate(frequency, getMinSpectrumCoord(), getMaxSpectrumCoord(), insets.left, getWidth() - insets.right));
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
        void foo() {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i -> Math.abs(spectrum.getEnergy()[i])));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [kleene_star:kleene_star__classBody_1]dd@2
    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
        public static class Spectrum {
            public double[] getEnergy() {
                return new double[0];
            }
        }
        protected Spectrum spectrum;
        public static class Ref<T> {
            T value;
            public Ref() {
            }
            public Ref(T value) {
                this.value = value;
            }
            public boolean isNull() {
                return value == null;
            }
            public T get() {
                return value;
            }
            public void set(T value) {
                this.value = value;
            }
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
            Insets insets = null;
            return (int) Math.round(interpolate(frequency, getMinSpectrumCoord(), getMaxSpectrumCoord(), insets.left, getWidth() - insets.right));
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
        void foo() {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i -> Math.abs(spectrum.getEnergy()[i])));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
        public static class Spectrum {
            public double[] getEnergy() {
                return new double[0];
            }
        }
        protected Spectrum spectrum;
        public static class Ref<T> {
            T value;
            public Ref() {
            }
            public Ref(T value) {
                this.value = value;
            }
            public boolean isNull() {
                return value == null;
            }
            public T get() {
                return value;
            }
            public void set(T value) {
                this.value = value;
            }
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
            Insets insets = null;
            return (int) Math.round(interpolate(frequency, getMinSpectrumCoord(), getMaxSpectrumCoord(), insets.left, getWidth() - insets.right));
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
        void foo() {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i -> Math.abs(spectrum.getEnergy()[i])));
        }
    }
------------------------------------------------------------


============Testing the following program: pass=============

// edit action set type: [kleene_star:kleene_star__classBody_1]dd@1
    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
        public static class Spectrum {
            public double[] getEnergy() {
                return new double[0];
            }
        }
        protected Spectrum spectrum;
        public static class Ref<T> {
            T value;
            public Ref() {
            }
            public Ref(T value) {
                this.value = value;
            }
            public boolean isNull() {
                return value == null;
            }
            public T get() {
                return value;
            }
            public void set(T value) {
                this.value = value;
            }
        }
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
            Ref<Double> max = new Ref<>();
            Ref<T> index = new Ref<>();
            stream.forEach(v -> {
                Double value = function.apply(v);
                if (max.isNull() || value > max.get()) {
                    max.set(value);
                    index.set(v);
                }
            });
            return index.get();
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
            Insets insets = null;
            return (int) Math.round(interpolate(frequency, getMinSpectrumCoord(), getMaxSpectrumCoord(), insets.left, getWidth() - insets.right));
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
        void foo() {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i -> Math.abs(spectrum.getEnergy()[i])));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
        public static class Spectrum {
            public double[] getEnergy() {
                return new double[0];
            }
        }
        protected Spectrum spectrum;
        public static class Ref<T> {
            T value;
            public Ref() {
            }
            public Ref(T value) {
                this.value = value;
            }
            public boolean isNull() {
                return value == null;
            }
            public T get() {
                return value;
            }
            public void set(T value) {
                this.value = value;
            }
        }
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
            Ref<Double> max = new Ref<>();
            Ref<T> index = new Ref<>();
            stream.forEach(v -> {
                Double value = function.apply(v);
                if (max.isNull() || value > max.get()) {
                    max.set(value);
                    index.set(v);
                }
            });
            return index.get();
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
            Insets insets = null;
            return (int) Math.round(interpolate(frequency, getMinSpectrumCoord(), getMaxSpectrumCoord(), insets.left, getWidth() - insets.right));
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
        void foo() {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i -> Math.abs(spectrum.getEnergy()[i])));
        }
    }
------------------------------------------------------------


===========TestScriptExecutionCacheEntryEviction============

0 entries are removed: 0 --> 0.
------------------------------------------------------------


===========Node edit action set cache is cleared.===========

    size before clearance = 20
------------------------------------------------------------


================The best program is updated.================

token count change 447 -> 404
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [kleene_star:kleene_star__classBody_1]dd@1
    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
        public static class Spectrum {
            public double[] getEnergy() {
                return new double[0];
            }
        }
        protected Spectrum spectrum;
        public static class Ref<T> {
            T value;
            public Ref() {
            }
            public Ref(T value) {
                this.value = value;
            }
            public boolean isNull() {
                return value == null;
            }
            public T get() {
                return value;
            }
            public void set(T value) {
                this.value = value;
            }
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
            Insets insets = null;
            return (int) Math.round(interpolate(frequency, getMinSpectrumCoord(), getMaxSpectrumCoord(), insets.left, getWidth() - insets.right));
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
        void foo() {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i -> Math.abs(spectrum.getEnergy()[i])));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
        public static class Spectrum {
            public double[] getEnergy() {
                return new double[0];
            }
        }
        protected Spectrum spectrum;
        public static class Ref<T> {
            T value;
            public Ref() {
            }
            public Ref(T value) {
                this.value = value;
            }
            public boolean isNull() {
                return value == null;
            }
            public T get() {
                return value;
            }
            public void set(T value) {
                this.value = value;
            }
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
            Insets insets = null;
            return (int) Math.round(interpolate(frequency, getMinSpectrumCoord(), getMaxSpectrumCoord(), insets.left, getWidth() - insets.right));
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
        void foo() {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i -> Math.abs(spectrum.getEnergy()[i])));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [kleene_star:kleene_star__classBody_1]dd@3
    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
            Ref<Double> max = new Ref<>();
            Ref<T> index = new Ref<>();
            stream.forEach(v -> {
                Double value = function.apply(v);
                if (max.isNull() || value > max.get()) {
                    max.set(value);
                    index.set(v);
                }
            });
            return index.get();
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
            Insets insets = null;
            return (int) Math.round(interpolate(frequency, getMinSpectrumCoord(), getMaxSpectrumCoord(), insets.left, getWidth() - insets.right));
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
        void foo() {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i -> Math.abs(spectrum.getEnergy()[i])));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
            Ref<Double> max = new Ref<>();
            Ref<T> index = new Ref<>();
            stream.forEach(v -> {
                Double value = function.apply(v);
                if (max.isNull() || value > max.get()) {
                    max.set(value);
                    index.set(v);
                }
            });
            return index.get();
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
            Insets insets = null;
            return (int) Math.round(interpolate(frequency, getMinSpectrumCoord(), getMaxSpectrumCoord(), insets.left, getWidth() - insets.right));
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
        void foo() {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i -> Math.abs(spectrum.getEnergy()[i])));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [kleene_star:kleene_star__classBody_1]dd@1
    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
        public static class Spectrum {
            public double[] getEnergy() {
                return new double[0];
            }
        }
        protected Spectrum spectrum;
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
            Ref<Double> max = new Ref<>();
            Ref<T> index = new Ref<>();
            stream.forEach(v -> {
                Double value = function.apply(v);
                if (max.isNull() || value > max.get()) {
                    max.set(value);
                    index.set(v);
                }
            });
            return index.get();
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
            Insets insets = null;
            return (int) Math.round(interpolate(frequency, getMinSpectrumCoord(), getMaxSpectrumCoord(), insets.left, getWidth() - insets.right));
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
        void foo() {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i -> Math.abs(spectrum.getEnergy()[i])));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
        public static class Spectrum {
            public double[] getEnergy() {
                return new double[0];
            }
        }
        protected Spectrum spectrum;
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
            Ref<Double> max = new Ref<>();
            Ref<T> index = new Ref<>();
            stream.forEach(v -> {
                Double value = function.apply(v);
                if (max.isNull() || value > max.get()) {
                    max.set(value);
                    index.set(v);
                }
            });
            return index.get();
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
            Insets insets = null;
            return (int) Math.round(interpolate(frequency, getMinSpectrumCoord(), getMaxSpectrumCoord(), insets.left, getWidth() - insets.right));
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
        void foo() {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i -> Math.abs(spectrum.getEnergy()[i])));
        }
    }
------------------------------------------------------------


============Testing the following program: pass=============

// edit action set type: [kleene_star:kleene_star__classBody_1]dd@2
    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
        public static class Ref<T> {
            T value;
            public Ref() {
            }
            public Ref(T value) {
                this.value = value;
            }
            public boolean isNull() {
                return value == null;
            }
            public T get() {
                return value;
            }
            public void set(T value) {
                this.value = value;
            }
        }
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
            Ref<Double> max = new Ref<>();
            Ref<T> index = new Ref<>();
            stream.forEach(v -> {
                Double value = function.apply(v);
                if (max.isNull() || value > max.get()) {
                    max.set(value);
                    index.set(v);
                }
            });
            return index.get();
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
            Insets insets = null;
            return (int) Math.round(interpolate(frequency, getMinSpectrumCoord(), getMaxSpectrumCoord(), insets.left, getWidth() - insets.right));
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
        void foo() {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i -> Math.abs(spectrum.getEnergy()[i])));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
        public static class Ref<T> {
            T value;
            public Ref() {
            }
            public Ref(T value) {
                this.value = value;
            }
            public boolean isNull() {
                return value == null;
            }
            public T get() {
                return value;
            }
            public void set(T value) {
                this.value = value;
            }
        }
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
            Ref<Double> max = new Ref<>();
            Ref<T> index = new Ref<>();
            stream.forEach(v -> {
                Double value = function.apply(v);
                if (max.isNull() || value > max.get()) {
                    max.set(value);
                    index.set(v);
                }
            });
            return index.get();
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
            Insets insets = null;
            return (int) Math.round(interpolate(frequency, getMinSpectrumCoord(), getMaxSpectrumCoord(), insets.left, getWidth() - insets.right));
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
        void foo() {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i -> Math.abs(spectrum.getEnergy()[i])));
        }
    }
------------------------------------------------------------


===========TestScriptExecutionCacheEntryEviction============

0 entries are removed: 0 --> 0.
------------------------------------------------------------


===========Node edit action set cache is cleared.===========

    size before clearance = 4
------------------------------------------------------------


================The best program is updated.================

token count change 404 -> 378
------------------------------------------------------------


===================Node reduction is done===================

Succeeded to reduce node 12 from 447 to 378
------------------------------------------------------------


================Reducing node 1214, size=378================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
        public static class Ref<T> {
            T value;
            public Ref() {
            }
            public Ref(T value) {
                this.value = value;
            }
            public boolean isNull() {
                return value == null;
            }
            public T get() {
                return value;
            }
            public void set(T value) {
                this.value = value;
            }
        }
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
            Ref<Double> max = new Ref<>();
            Ref<T> index = new Ref<>();
            stream.forEach(v -> {
                Double value = function.apply(v);
                if (max.isNull() || value > max.get()) {
                    max.set(value);
                    index.set(v);
                }
            });
            return index.get();
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
            Insets insets = null;
            return (int) Math.round(interpolate(frequency, getMinSpectrumCoord(), getMaxSpectrumCoord(), insets.left, getWidth() - insets.right));
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
        void foo() {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i -> Math.abs(spectrum.getEnergy()[i])));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [regular_node]can be epsilon
    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
        public static class Ref<T> {
            T value;
            public Ref() {
            }
            public Ref(T value) {
                this.value = value;
            }
            public boolean isNull() {
                return value == null;
            }
            public T get() {
                return value;
            }
            public void set(T value) {
                this.value = value;
            }
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
            Insets insets = null;
            return (int) Math.round(interpolate(frequency, getMinSpectrumCoord(), getMaxSpectrumCoord(), insets.left, getWidth() - insets.right));
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
        void foo() {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i -> Math.abs(spectrum.getEnergy()[i])));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
        public static class Ref<T> {
            T value;
            public Ref() {
            }
            public Ref(T value) {
                this.value = value;
            }
            public boolean isNull() {
                return value == null;
            }
            public T get() {
                return value;
            }
            public void set(T value) {
                this.value = value;
            }
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
            Insets insets = null;
            return (int) Math.round(interpolate(frequency, getMinSpectrumCoord(), getMaxSpectrumCoord(), insets.left, getWidth() - insets.right));
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
        void foo() {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i -> Math.abs(spectrum.getEnergy()[i])));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [regular node]compatible replacement
    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
        public static class Ref<T> {
            T value;
            public Ref() {
            }
            public Ref(T value) {
                this.value = value;
            }
            public boolean isNull() {
                return value == null;
            }
            public T get() {
                return value;
            }
            public void set(T value) {
                this.value = value;
            }
        }
                                                                                  {
            Ref<Double> max = new Ref<>();
            Ref<T> index = new Ref<>();
            stream.forEach(v -> {
                Double value = function.apply(v);
                if (max.isNull() || value > max.get()) {
                    max.set(value);
                    index.set(v);
                }
            });
            return index.get();
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
            Insets insets = null;
            return (int) Math.round(interpolate(frequency, getMinSpectrumCoord(), getMaxSpectrumCoord(), insets.left, getWidth() - insets.right));
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
        void foo() {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i -> Math.abs(spectrum.getEnergy()[i])));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
        public static class Ref<T> {
            T value;
            public Ref() {
            }
            public Ref(T value) {
                this.value = value;
            }
            public boolean isNull() {
                return value == null;
            }
            public T get() {
                return value;
            }
            public void set(T value) {
                this.value = value;
            }
        }
                                                                                  {
            Ref<Double> max = new Ref<>();
            Ref<T> index = new Ref<>();
            stream.forEach(v -> {
                Double value = function.apply(v);
                if (max.isNull() || value > max.get()) {
                    max.set(value);
                    index.set(v);
                }
            });
            return index.get();
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
            Insets insets = null;
            return (int) Math.round(interpolate(frequency, getMinSpectrumCoord(), getMaxSpectrumCoord(), insets.left, getWidth() - insets.right));
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
        void foo() {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i -> Math.abs(spectrum.getEnergy()[i])));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 1214
------------------------------------------------------------


================Reducing node 1218, size=378================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
        public static class Ref<T> {
            T value;
            public Ref() {
            }
            public Ref(T value) {
                this.value = value;
            }
            public boolean isNull() {
                return value == null;
            }
            public T get() {
                return value;
            }
            public void set(T value) {
                this.value = value;
            }
        }
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
            Ref<Double> max = new Ref<>();
            Ref<T> index = new Ref<>();
            stream.forEach(v -> {
                Double value = function.apply(v);
                if (max.isNull() || value > max.get()) {
                    max.set(value);
                    index.set(v);
                }
            });
            return index.get();
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
            Insets insets = null;
            return (int) Math.round(interpolate(frequency, getMinSpectrumCoord(), getMaxSpectrumCoord(), insets.left, getWidth() - insets.right));
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
        void foo() {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i -> Math.abs(spectrum.getEnergy()[i])));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 1218
------------------------------------------------------------


================Reducing node 1220, size=378================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
        public static class Ref<T> {
            T value;
            public Ref() {
            }
            public Ref(T value) {
                this.value = value;
            }
            public boolean isNull() {
                return value == null;
            }
            public T get() {
                return value;
            }
            public void set(T value) {
                this.value = value;
            }
        }
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
            Ref<Double> max = new Ref<>();
            Ref<T> index = new Ref<>();
            stream.forEach(v -> {
                Double value = function.apply(v);
                if (max.isNull() || value > max.get()) {
                    max.set(value);
                    index.set(v);
                }
            });
            return index.get();
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
            Insets insets = null;
            return (int) Math.round(interpolate(frequency, getMinSpectrumCoord(), getMaxSpectrumCoord(), insets.left, getWidth() - insets.right));
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
        void foo() {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i -> Math.abs(spectrum.getEnergy()[i])));
        }
    }
------------------------------------------------------------


============Testing the following program: pass=============

// edit action set type: [kleene_star:optional__block_1]dd@1
    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
        public static class Ref<T> {
            T value;
            public Ref() {
            }
            public Ref(T value) {
                this.value = value;
            }
            public boolean isNull() {
                return value == null;
            }
            public T get() {
                return value;
            }
            public void set(T value) {
                this.value = value;
            }
        }
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
            Insets insets = null;
            return (int) Math.round(interpolate(frequency, getMinSpectrumCoord(), getMaxSpectrumCoord(), insets.left, getWidth() - insets.right));
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
        void foo() {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i -> Math.abs(spectrum.getEnergy()[i])));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
        public static class Ref<T> {
            T value;
            public Ref() {
            }
            public Ref(T value) {
                this.value = value;
            }
            public boolean isNull() {
                return value == null;
            }
            public T get() {
                return value;
            }
            public void set(T value) {
                this.value = value;
            }
        }
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
            Insets insets = null;
            return (int) Math.round(interpolate(frequency, getMinSpectrumCoord(), getMaxSpectrumCoord(), insets.left, getWidth() - insets.right));
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
        void foo() {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i -> Math.abs(spectrum.getEnergy()[i])));
        }
    }
------------------------------------------------------------


===========TestScriptExecutionCacheEntryEviction============

0 entries are removed: 0 --> 0.
------------------------------------------------------------


===========Node edit action set cache is cleared.===========

    size before clearance = 3
------------------------------------------------------------


================The best program is updated.================

token count change 378 -> 293
------------------------------------------------------------


===================Node reduction is done===================

Succeeded to reduce node 1220 from 378 to 293
------------------------------------------------------------


================Reducing node 1712, size=293================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
        public static class Ref<T> {
            T value;
            public Ref() {
            }
            public Ref(T value) {
                this.value = value;
            }
            public boolean isNull() {
                return value == null;
            }
            public T get() {
                return value;
            }
            public void set(T value) {
                this.value = value;
            }
        }
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
            Insets insets = null;
            return (int) Math.round(interpolate(frequency, getMinSpectrumCoord(), getMaxSpectrumCoord(), insets.left, getWidth() - insets.right));
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
        void foo() {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i -> Math.abs(spectrum.getEnergy()[i])));
        }
    }
------------------------------------------------------------


============Testing the following program: pass=============

// edit action set type: [regular_node]can be epsilon
    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
            Insets insets = null;
            return (int) Math.round(interpolate(frequency, getMinSpectrumCoord(), getMaxSpectrumCoord(), insets.left, getWidth() - insets.right));
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
        void foo() {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i -> Math.abs(spectrum.getEnergy()[i])));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
            Insets insets = null;
            return (int) Math.round(interpolate(frequency, getMinSpectrumCoord(), getMaxSpectrumCoord(), insets.left, getWidth() - insets.right));
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
        void foo() {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i -> Math.abs(spectrum.getEnergy()[i])));
        }
    }
------------------------------------------------------------


============Test script execution is cancelled.=============

It took less than 1 second to cancel the task.

    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
            T value;
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
            Insets insets = null;
            return (int) Math.round(interpolate(frequency, getMinSpectrumCoord(), getMaxSpectrumCoord(), insets.left, getWidth() - insets.right));
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
        void foo() {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i -> Math.abs(spectrum.getEnergy()[i])));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
            T value;
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
            Insets insets = null;
            return (int) Math.round(interpolate(frequency, getMinSpectrumCoord(), getMaxSpectrumCoord(), insets.left, getWidth() - insets.right));
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
        void foo() {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i -> Math.abs(spectrum.getEnergy()[i])));
        }
    }
------------------------------------------------------------


============Test script execution is cancelled.=============

It took less than 1 second to cancel the task.

    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
                           {
                return value;
            }
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
            Insets insets = null;
            return (int) Math.round(interpolate(frequency, getMinSpectrumCoord(), getMaxSpectrumCoord(), insets.left, getWidth() - insets.right));
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
        void foo() {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i -> Math.abs(spectrum.getEnergy()[i])));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
                           {
                return value;
            }
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
            Insets insets = null;
            return (int) Math.round(interpolate(frequency, getMinSpectrumCoord(), getMaxSpectrumCoord(), insets.left, getWidth() - insets.right));
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
        void foo() {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i -> Math.abs(spectrum.getEnergy()[i])));
        }
    }
------------------------------------------------------------


============Test script execution is cancelled.=============

It took less than 1 second to cancel the task.

    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
            public Ref() {
            }
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
            Insets insets = null;
            return (int) Math.round(interpolate(frequency, getMinSpectrumCoord(), getMaxSpectrumCoord(), insets.left, getWidth() - insets.right));
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
        void foo() {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i -> Math.abs(spectrum.getEnergy()[i])));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
            public Ref() {
            }
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
            Insets insets = null;
            return (int) Math.round(interpolate(frequency, getMinSpectrumCoord(), getMaxSpectrumCoord(), insets.left, getWidth() - insets.right));
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
        void foo() {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i -> Math.abs(spectrum.getEnergy()[i])));
        }
    }
------------------------------------------------------------


============Test script execution is cancelled.=============

It took less than 1 second to cancel the task.

    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
                                    {
                return value == null;
            }
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
            Insets insets = null;
            return (int) Math.round(interpolate(frequency, getMinSpectrumCoord(), getMaxSpectrumCoord(), insets.left, getWidth() - insets.right));
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
        void foo() {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i -> Math.abs(spectrum.getEnergy()[i])));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
                                    {
                return value == null;
            }
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
            Insets insets = null;
            return (int) Math.round(interpolate(frequency, getMinSpectrumCoord(), getMaxSpectrumCoord(), insets.left, getWidth() - insets.right));
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
        void foo() {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i -> Math.abs(spectrum.getEnergy()[i])));
        }
    }
------------------------------------------------------------


============Test script execution is cancelled.=============

It took less than 1 second to cancel the task.

    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
                                     {
                this.value = value;
            }
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
            Insets insets = null;
            return (int) Math.round(interpolate(frequency, getMinSpectrumCoord(), getMaxSpectrumCoord(), insets.left, getWidth() - insets.right));
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
        void foo() {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i -> Math.abs(spectrum.getEnergy()[i])));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
                                     {
                this.value = value;
            }
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
            Insets insets = null;
            return (int) Math.round(interpolate(frequency, getMinSpectrumCoord(), getMaxSpectrumCoord(), insets.left, getWidth() - insets.right));
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
        void foo() {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i -> Math.abs(spectrum.getEnergy()[i])));
        }
    }
------------------------------------------------------------


============Test script execution is cancelled.=============

It took less than 1 second to cancel the task.

    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
            public T get() {
                return value;
            }
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
            Insets insets = null;
            return (int) Math.round(interpolate(frequency, getMinSpectrumCoord(), getMaxSpectrumCoord(), insets.left, getWidth() - insets.right));
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
        void foo() {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i -> Math.abs(spectrum.getEnergy()[i])));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
            public T get() {
                return value;
            }
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
            Insets insets = null;
            return (int) Math.round(interpolate(frequency, getMinSpectrumCoord(), getMaxSpectrumCoord(), insets.left, getWidth() - insets.right));
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
        void foo() {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i -> Math.abs(spectrum.getEnergy()[i])));
        }
    }
------------------------------------------------------------


============Test script execution is cancelled.=============

It took less than 1 second to cancel the task.

    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
            public boolean isNull() {
                return value == null;
            }
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
            Insets insets = null;
            return (int) Math.round(interpolate(frequency, getMinSpectrumCoord(), getMaxSpectrumCoord(), insets.left, getWidth() - insets.right));
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
        void foo() {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i -> Math.abs(spectrum.getEnergy()[i])));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
            public boolean isNull() {
                return value == null;
            }
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
            Insets insets = null;
            return (int) Math.round(interpolate(frequency, getMinSpectrumCoord(), getMaxSpectrumCoord(), insets.left, getWidth() - insets.right));
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
        void foo() {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i -> Math.abs(spectrum.getEnergy()[i])));
        }
    }
------------------------------------------------------------


============Test script execution is cancelled.=============

It took less than 1 second to cancel the task.

    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
            public Ref(T value) {
                this.value = value;
            }
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
            Insets insets = null;
            return (int) Math.round(interpolate(frequency, getMinSpectrumCoord(), getMaxSpectrumCoord(), insets.left, getWidth() - insets.right));
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
        void foo() {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i -> Math.abs(spectrum.getEnergy()[i])));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
            public Ref(T value) {
                this.value = value;
            }
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
            Insets insets = null;
            return (int) Math.round(interpolate(frequency, getMinSpectrumCoord(), getMaxSpectrumCoord(), insets.left, getWidth() - insets.right));
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
        void foo() {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i -> Math.abs(spectrum.getEnergy()[i])));
        }
    }
------------------------------------------------------------


============Test script execution is cancelled.=============

It took less than 1 second to cancel the task.

    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
            public void set(T value) {
                this.value = value;
            }
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
            Insets insets = null;
            return (int) Math.round(interpolate(frequency, getMinSpectrumCoord(), getMaxSpectrumCoord(), insets.left, getWidth() - insets.right));
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
        void foo() {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i -> Math.abs(spectrum.getEnergy()[i])));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
            public void set(T value) {
                this.value = value;
            }
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
            Insets insets = null;
            return (int) Math.round(interpolate(frequency, getMinSpectrumCoord(), getMaxSpectrumCoord(), insets.left, getWidth() - insets.right));
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
        void foo() {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i -> Math.abs(spectrum.getEnergy()[i])));
        }
    }
------------------------------------------------------------


============Test script execution is cancelled.=============

It took less than 1 second to cancel the task.

    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
            T value;
            public Ref() {
            }
            public Ref(T value) {
                this.value = value;
            }
            public boolean isNull() {
                return value == null;
            }
            public T get() {
                return value;
            }
            public void set(T value) {
                this.value = value;
            }
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
            Insets insets = null;
            return (int) Math.round(interpolate(frequency, getMinSpectrumCoord(), getMaxSpectrumCoord(), insets.left, getWidth() - insets.right));
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
        void foo() {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i -> Math.abs(spectrum.getEnergy()[i])));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
            T value;
            public Ref() {
            }
            public Ref(T value) {
                this.value = value;
            }
            public boolean isNull() {
                return value == null;
            }
            public T get() {
                return value;
            }
            public void set(T value) {
                this.value = value;
            }
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
            Insets insets = null;
            return (int) Math.round(interpolate(frequency, getMinSpectrumCoord(), getMaxSpectrumCoord(), insets.left, getWidth() - insets.right));
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
        void foo() {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i -> Math.abs(spectrum.getEnergy()[i])));
        }
    }
------------------------------------------------------------


===========TestScriptExecutionCacheEntryEviction============

0 entries are removed: 0 --> 0.
------------------------------------------------------------


===========Node edit action set cache is cleared.===========

    size before clearance = 11
------------------------------------------------------------


================The best program is updated.================

token count change 293 -> 224
------------------------------------------------------------


===================Node reduction is done===================

Succeeded to reduce node 1712 from 293 to 224
------------------------------------------------------------


=================Reducing node 25, size=224=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
            Insets insets = null;
            return (int) Math.round(interpolate(frequency, getMinSpectrumCoord(), getMaxSpectrumCoord(), insets.left, getWidth() - insets.right));
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
        void foo() {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i -> Math.abs(spectrum.getEnergy()[i])));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [regular_node]can be epsilon
    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
            Insets insets = null;
            return (int) Math.round(interpolate(frequency, getMinSpectrumCoord(), getMaxSpectrumCoord(), insets.left, getWidth() - insets.right));
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
            Insets insets = null;
            return (int) Math.round(interpolate(frequency, getMinSpectrumCoord(), getMaxSpectrumCoord(), insets.left, getWidth() - insets.right));
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
    }
------------------------------------------------------------


============Testing the following program: pass=============

// edit action set type: [regular node]compatible replacement
    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
            Insets insets = null;
            return (int) Math.round(interpolate(frequency, getMinSpectrumCoord(), getMaxSpectrumCoord(), insets.left, getWidth() - insets.right));
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i -> Math.abs(spectrum.getEnergy()[i])));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
            Insets insets = null;
            return (int) Math.round(interpolate(frequency, getMinSpectrumCoord(), getMaxSpectrumCoord(), insets.left, getWidth() - insets.right));
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i -> Math.abs(spectrum.getEnergy()[i])));
        }
    }
------------------------------------------------------------


===========TestScriptExecutionCacheEntryEviction============

0 entries are removed: 0 --> 0.
------------------------------------------------------------


===========Node edit action set cache is cleared.===========

    size before clearance = 2
------------------------------------------------------------


================The best program is updated.================

token count change 224 -> 220
------------------------------------------------------------


===================Node reduction is done===================

Succeeded to reduce node 25 from 224 to 220
------------------------------------------------------------


================Reducing node 463, size=220=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
            Insets insets = null;
            return (int) Math.round(interpolate(frequency, getMinSpectrumCoord(), getMaxSpectrumCoord(), insets.left, getWidth() - insets.right));
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i -> Math.abs(spectrum.getEnergy()[i])));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [regular_node]can be epsilon
    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i -> Math.abs(spectrum.getEnergy()[i])));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i -> Math.abs(spectrum.getEnergy()[i])));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [regular node]compatible replacement
    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
                                                        {
            Insets insets = null;
            return (int) Math.round(interpolate(frequency, getMinSpectrumCoord(), getMaxSpectrumCoord(), insets.left, getWidth() - insets.right));
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i -> Math.abs(spectrum.getEnergy()[i])));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
                                                        {
            Insets insets = null;
            return (int) Math.round(interpolate(frequency, getMinSpectrumCoord(), getMaxSpectrumCoord(), insets.left, getWidth() - insets.right));
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i -> Math.abs(spectrum.getEnergy()[i])));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 463
------------------------------------------------------------


=================Reducing node 28, size=220=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
            Insets insets = null;
            return (int) Math.round(interpolate(frequency, getMinSpectrumCoord(), getMaxSpectrumCoord(), insets.left, getWidth() - insets.right));
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i -> Math.abs(spectrum.getEnergy()[i])));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [regular_node]can be epsilon
    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
            Insets insets = null;
            return (int) Math.round(interpolate(frequency, getMinSpectrumCoord(), getMaxSpectrumCoord(), insets.left, getWidth() - insets.right));
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
            Insets insets = null;
            return (int) Math.round(interpolate(frequency, getMinSpectrumCoord(), getMaxSpectrumCoord(), insets.left, getWidth() - insets.right));
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 28
------------------------------------------------------------


================Reducing node 758, size=220=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
            Insets insets = null;
            return (int) Math.round(interpolate(frequency, getMinSpectrumCoord(), getMaxSpectrumCoord(), insets.left, getWidth() - insets.right));
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i -> Math.abs(spectrum.getEnergy()[i])));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [regular_node]can be epsilon
    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        protected int getXByFrequency(double frequency) {
            Insets insets = null;
            return (int) Math.round(interpolate(frequency, getMinSpectrumCoord(), getMaxSpectrumCoord(), insets.left, getWidth() - insets.right));
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i -> Math.abs(spectrum.getEnergy()[i])));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        protected int getXByFrequency(double frequency) {
            Insets insets = null;
            return (int) Math.round(interpolate(frequency, getMinSpectrumCoord(), getMaxSpectrumCoord(), insets.left, getWidth() - insets.right));
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i -> Math.abs(spectrum.getEnergy()[i])));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [regular node]compatible replacement
    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
                                                                                               {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
            Insets insets = null;
            return (int) Math.round(interpolate(frequency, getMinSpectrumCoord(), getMaxSpectrumCoord(), insets.left, getWidth() - insets.right));
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i -> Math.abs(spectrum.getEnergy()[i])));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
                                                                                               {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
            Insets insets = null;
            return (int) Math.round(interpolate(frequency, getMinSpectrumCoord(), getMaxSpectrumCoord(), insets.left, getWidth() - insets.right));
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i -> Math.abs(spectrum.getEnergy()[i])));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 758
------------------------------------------------------------


=================Reducing node 30, size=220=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
            Insets insets = null;
            return (int) Math.round(interpolate(frequency, getMinSpectrumCoord(), getMaxSpectrumCoord(), insets.left, getWidth() - insets.right));
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i -> Math.abs(spectrum.getEnergy()[i])));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [kleene_star:optional__block_1]dd@1
    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
            Insets insets = null;
            return (int) Math.round(interpolate(frequency, getMinSpectrumCoord(), getMaxSpectrumCoord(), insets.left, getWidth() - insets.right));
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
            Insets insets = null;
            return (int) Math.round(interpolate(frequency, getMinSpectrumCoord(), getMaxSpectrumCoord(), insets.left, getWidth() - insets.right));
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 30
------------------------------------------------------------


=================Reducing node 32, size=220=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
            Insets insets = null;
            return (int) Math.round(interpolate(frequency, getMinSpectrumCoord(), getMaxSpectrumCoord(), insets.left, getWidth() - insets.right));
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i -> Math.abs(spectrum.getEnergy()[i])));
        }
    }
------------------------------------------------------------


==============Node edit action set cache hit.===============

    NodeDeletionAction{target_node=32}
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [kleene_plus:blockStatements]remove whole except first
    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
            Insets insets = null;
            return (int) Math.round(interpolate(frequency, getMinSpectrumCoord(), getMaxSpectrumCoord(), insets.left, getWidth() - insets.right));
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
            Insets insets = null;
            return (int) Math.round(interpolate(frequency, getMinSpectrumCoord(), getMaxSpectrumCoord(), insets.left, getWidth() - insets.right));
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
        }
    }
------------------------------------------------------------


==============Node edit action set cache hit.===============

    NodeDeletionAction{target_node=35}
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [kleene_plus:blockStatements]dd@1
    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
            Insets insets = null;
            return (int) Math.round(interpolate(frequency, getMinSpectrumCoord(), getMaxSpectrumCoord(), insets.left, getWidth() - insets.right));
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i -> Math.abs(spectrum.getEnergy()[i])));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
            Insets insets = null;
            return (int) Math.round(interpolate(frequency, getMinSpectrumCoord(), getMaxSpectrumCoord(), insets.left, getWidth() - insets.right));
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i -> Math.abs(spectrum.getEnergy()[i])));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 32
------------------------------------------------------------


================Reducing node 467, size=220=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
            Insets insets = null;
            return (int) Math.round(interpolate(frequency, getMinSpectrumCoord(), getMaxSpectrumCoord(), insets.left, getWidth() - insets.right));
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i -> Math.abs(spectrum.getEnergy()[i])));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 467
------------------------------------------------------------


================Reducing node 469, size=220=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
            Insets insets = null;
            return (int) Math.round(interpolate(frequency, getMinSpectrumCoord(), getMaxSpectrumCoord(), insets.left, getWidth() - insets.right));
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i -> Math.abs(spectrum.getEnergy()[i])));
        }
    }
------------------------------------------------------------


============Testing the following program: pass=============

// edit action set type: [kleene_star:optional__block_1]dd@1
    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i -> Math.abs(spectrum.getEnergy()[i])));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i -> Math.abs(spectrum.getEnergy()[i])));
        }
    }
------------------------------------------------------------


===========TestScriptExecutionCacheEntryEviction============

0 entries are removed: 0 --> 0.
------------------------------------------------------------


===========Node edit action set cache is cleared.===========

    size before clearance = 9
------------------------------------------------------------


================The best program is updated.================

token count change 220 -> 181
------------------------------------------------------------


===================Node reduction is done===================

Succeeded to reduce node 469 from 220 to 181
------------------------------------------------------------


=================Reducing node 35, size=181=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i -> Math.abs(spectrum.getEnergy()[i])));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [regular_node]can be epsilon
    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 35
------------------------------------------------------------


=================Reducing node 36, size=181=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i -> Math.abs(spectrum.getEnergy()[i])));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 36
------------------------------------------------------------


=================Reducing node 40, size=181=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i -> Math.abs(spectrum.getEnergy()[i])));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [regular node]compatible replacement
    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
            int xcur                                                                                                           ;
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
            int xcur                                                                                                           ;
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [regular node]compatible replacement
    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
            int        getXByFrequency                                                                                         ;
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
            int        getXByFrequency                                                                                         ;
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 40
------------------------------------------------------------


=================Reducing node 42, size=181=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i -> Math.abs(spectrum.getEnergy()[i])));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [kleene_star:optional__variableDeclarator_2]dd@1
    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
            int xcur                                                                                                           ;
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
            int xcur                                                                                                           ;
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 42
------------------------------------------------------------


=================Reducing node 43, size=181=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i -> Math.abs(spectrum.getEnergy()[i])));
        }
    }
------------------------------------------------------------


==============Node edit action set cache hit.===============

    NodeDeletionAction{target_node=43}
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 43
------------------------------------------------------------


=================Reducing node 65, size=181=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i -> Math.abs(spectrum.getEnergy()[i])));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [regular node]compatible replacement
    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency                                                                                         ;
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency                                                                                         ;
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [regular node]compatible replacement
    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
            int xcur =                 maxKey                                                                                  ;
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
            int xcur =                 maxKey                                                                                  ;
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [regular node]compatible replacement
    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
            int xcur =                 maxKey(IntStream.range(0, maxBpmIndex).boxed(), i -> Math.abs(spectrum.getEnergy()[i])) ;
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
            int xcur =                 maxKey(IntStream.range(0, maxBpmIndex).boxed(), i -> Math.abs(spectrum.getEnergy()[i])) ;
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 65
------------------------------------------------------------


=================Reducing node 68, size=181=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i -> Math.abs(spectrum.getEnergy()[i])));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [regular node]compatible replacement
    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
            int xcur =                 maxKey(IntStream.range(0, maxBpmIndex).boxed(), i -> Math.abs(spectrum.getEnergy()[i]) );
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
            int xcur =                 maxKey(IntStream.range(0, maxBpmIndex).boxed(), i -> Math.abs(spectrum.getEnergy()[i]) );
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 68
------------------------------------------------------------


=================Reducing node 71, size=181=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i -> Math.abs(spectrum.getEnergy()[i])));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [kleene_star:optional__explicitConstructorInvocation_2]dd@1
    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(                                                                                       );
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(                                                                                       );
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 71
------------------------------------------------------------


=================Reducing node 92, size=181=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i -> Math.abs(spectrum.getEnergy()[i])));
        }
    }
------------------------------------------------------------


==============Node edit action set cache hit.===============

    NodeDeletionAction{target_node=92}
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [regular node]compatible replacement
    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey                                                                                 );
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey                                                                                 );
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [regular node]compatible replacement
    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(       IntStream.range(0, maxBpmIndex)                                                 );
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(       IntStream.range(0, maxBpmIndex)                                                 );
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [regular node]compatible replacement
    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(       IntStream.range(0, maxBpmIndex).boxed()                                         );
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(       IntStream.range(0, maxBpmIndex).boxed()                                         );
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [regular node]compatible replacement
    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(       IntStream.range(0, maxBpmIndex).boxed(), i -> Math.abs(spectrum.getEnergy()[i]) );
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(       IntStream.range(0, maxBpmIndex).boxed(), i -> Math.abs(spectrum.getEnergy()[i]) );
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 92
------------------------------------------------------------


=================Reducing node 95, size=181=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i -> Math.abs(spectrum.getEnergy()[i])));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [regular node]compatible replacement
    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(       IntStream.range(0, maxBpmIndex                                                 ));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(       IntStream.range(0, maxBpmIndex                                                 ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 95
------------------------------------------------------------


=================Reducing node 98, size=181=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i -> Math.abs(spectrum.getEnergy()[i])));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [kleene_star:optional__explicitConstructorInvocation_2]dd@1
    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(                                                                               ));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(                                                                               ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 98
------------------------------------------------------------


=================Reducing node 99, size=181=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i -> Math.abs(spectrum.getEnergy()[i])));
        }
    }
------------------------------------------------------------


==============Node edit action set cache hit.===============

    NodeDeletionAction{target_node=99}
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [regular node]compatible replacement
    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream                                                                      ));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream                                                                      ));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [regular node]compatible replacement
    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(                                boxed                                          ));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(                                boxed                                          ));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [regular node]compatible replacement
    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(          range                                                                ));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(          range                                                                ));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [regular node]compatible replacement
    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(                                         i                                     ));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(                                         i                                     ));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [regular node]compatible replacement
    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex)                                                ));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex)                                                ));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [regular node]compatible replacement
    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed()                                        ));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed()                                        ));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [regular node]compatible replacement
    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(                                              Math.abs(spectrum.getEnergy()[i])));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(                                              Math.abs(spectrum.getEnergy()[i])));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [regular node]compatible replacement
    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(                                         i -> Math.abs(spectrum.getEnergy()[i])));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(                                         i -> Math.abs(spectrum.getEnergy()[i])));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 99
------------------------------------------------------------


=================Reducing node 2, size=181==================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    import java.awt.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i -> Math.abs(spectrum.getEnergy()[i])));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [kleene_star:kleene_star__compilationUnit_2]dd@3
    --file: Main.java--
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i -> Math.abs(spectrum.getEnergy()[i])));
        }
    }
    --file: <formatted tokenized program in its original format>--
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i -> Math.abs(spectrum.getEnergy()[i])));
        }
    }
------------------------------------------------------------


============Testing the following program: pass=============

// edit action set type: [kleene_star:kleene_star__compilationUnit_2]dd@1
    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i -> Math.abs(spectrum.getEnergy()[i])));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java.util.function.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i -> Math.abs(spectrum.getEnergy()[i])));
        }
    }
------------------------------------------------------------


===========TestScriptExecutionCacheEntryEviction============

0 entries are removed: 0 --> 0.
------------------------------------------------------------


===========Node edit action set cache is cleared.===========

    size before clearance = 25
------------------------------------------------------------


================The best program is updated.================

token count change 181 -> 174
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [kleene_star:kleene_star__compilationUnit_2]dd@2
    --file: Main.java--
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i -> Math.abs(spectrum.getEnergy()[i])));
        }
    }
    --file: <formatted tokenized program in its original format>--
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i -> Math.abs(spectrum.getEnergy()[i])));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [kleene_star:kleene_star__compilationUnit_2]dd@1
    --file: Main.java--
    import java.util.stream.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i -> Math.abs(spectrum.getEnergy()[i])));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i -> Math.abs(spectrum.getEnergy()[i])));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [kleene_star:kleene_star__compilationUnit_2]dd@1
    --file: Main.java--
    import java.util.function.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i -> Math.abs(spectrum.getEnergy()[i])));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.function.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i -> Math.abs(spectrum.getEnergy()[i])));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Succeeded to reduce node 2 from 181 to 174
------------------------------------------------------------


================Reducing node 762, size=174=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i -> Math.abs(spectrum.getEnergy()[i])));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 762
------------------------------------------------------------


================Reducing node 764, size=174=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
            return y0 + (x - x0) * (y1 - y0) / (x1 - x0);
        }
        protected int getXByFrequency(double frequency) {
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i -> Math.abs(spectrum.getEnergy()[i])));
        }
    }
------------------------------------------------------------


============Testing the following program: pass=============

// edit action set type: [kleene_star:optional__block_1]dd@1
    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
        }
        protected int getXByFrequency(double frequency) {
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i -> Math.abs(spectrum.getEnergy()[i])));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java.util.function.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
        }
        protected int getXByFrequency(double frequency) {
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i -> Math.abs(spectrum.getEnergy()[i])));
        }
    }
------------------------------------------------------------


===========TestScriptExecutionCacheEntryEviction============

0 entries are removed: 0 --> 0.
------------------------------------------------------------


===========Node edit action set cache is cleared.===========

    size before clearance = 4
------------------------------------------------------------


================The best program is updated.================

token count change 174 -> 153
------------------------------------------------------------


===================Node reduction is done===================

Succeeded to reduce node 764 from 174 to 153
------------------------------------------------------------


================Reducing node 1216, size=153================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
        }
        protected int getXByFrequency(double frequency) {
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i -> Math.abs(spectrum.getEnergy()[i])));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 1216
------------------------------------------------------------


================Reducing node 760, size=153=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
        }
        protected int getXByFrequency(double frequency) {
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i -> Math.abs(spectrum.getEnergy()[i])));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 760
------------------------------------------------------------


================Reducing node 929, size=153=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
        }
        protected int getXByFrequency(double frequency) {
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i -> Math.abs(spectrum.getEnergy()[i])));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 929
------------------------------------------------------------


================Reducing node 1635, size=153================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
        }
        protected int getXByFrequency(double frequency) {
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i -> Math.abs(spectrum.getEnergy()[i])));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 1635
------------------------------------------------------------


================Reducing node 101, size=153=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
        }
        protected int getXByFrequency(double frequency) {
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i -> Math.abs(spectrum.getEnergy()[i])));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [kleene_star:kleene_star__argumentList_2]dd@1
    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
        }
        protected int getXByFrequency(double frequency) {
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed()                                        ));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java.util.function.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
        }
        protected int getXByFrequency(double frequency) {
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed()                                        ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 101
------------------------------------------------------------


================Reducing node 102, size=153=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
        }
        protected int getXByFrequency(double frequency) {
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i -> Math.abs(spectrum.getEnergy()[i])));
        }
    }
------------------------------------------------------------


==============Node edit action set cache hit.===============

    NodeDeletionAction{target_node=102}
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 102
------------------------------------------------------------


================Reducing node 105, size=153=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
        }
        protected int getXByFrequency(double frequency) {
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i -> Math.abs(spectrum.getEnergy()[i])));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [regular node]compatible replacement
    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
        }
        protected int getXByFrequency(double frequency) {
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i                                     ));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java.util.function.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
        }
        protected int getXByFrequency(double frequency) {
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i                                     ));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [regular node]compatible replacement
    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
        }
        protected int getXByFrequency(double frequency) {
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(),      Math                             ));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java.util.function.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
        }
        protected int getXByFrequency(double frequency) {
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(),      Math                             ));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [regular node]compatible replacement
    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
        }
        protected int getXByFrequency(double frequency) {
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(),           abs                         ));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java.util.function.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
        }
        protected int getXByFrequency(double frequency) {
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(),           abs                         ));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [regular node]compatible replacement
    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
        }
        protected int getXByFrequency(double frequency) {
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(),               spectrum.getEnergy()[i] ));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java.util.function.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
        }
        protected int getXByFrequency(double frequency) {
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(),               spectrum.getEnergy()[i] ));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [regular node]compatible replacement
    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
        }
        protected int getXByFrequency(double frequency) {
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(),      Math.abs(spectrum.getEnergy()[i])));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java.util.function.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
        }
        protected int getXByFrequency(double frequency) {
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(),      Math.abs(spectrum.getEnergy()[i])));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 105
------------------------------------------------------------


================Reducing node 932, size=153=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(double x, double x0, double x1, double y0, double y1) {
        }
        protected int getXByFrequency(double frequency) {
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i -> Math.abs(spectrum.getEnergy()[i])));
        }
    }
------------------------------------------------------------


============Testing the following program: pass=============

// edit action set type: [kleene_star:optional__methodDeclarator_1]dd@1
    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i -> Math.abs(spectrum.getEnergy()[i])));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java.util.function.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i -> Math.abs(spectrum.getEnergy()[i])));
        }
    }
------------------------------------------------------------


===========TestScriptExecutionCacheEntryEviction============

0 entries are removed: 0 --> 0.
------------------------------------------------------------


===========Node edit action set cache is cleared.===========

    size before clearance = 7
------------------------------------------------------------


================The best program is updated.================

token count change 153 -> 139
------------------------------------------------------------


===================Node reduction is done===================

Succeeded to reduce node 932 from 153 to 139
------------------------------------------------------------


================Reducing node 1638, size=139================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i -> Math.abs(spectrum.getEnergy()[i])));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [kleene_star:optional__methodDeclarator_1]dd@1
    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    public class Main {
        public static <T>T maxKey(                                              ) {
        }
        public static double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i -> Math.abs(spectrum.getEnergy()[i])));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java.util.function.*;
    public class Main {
        public static <T>T maxKey(                                              ) {
        }
        public static double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i -> Math.abs(spectrum.getEnergy()[i])));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 1638
------------------------------------------------------------


================Reducing node 1641, size=139================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i -> Math.abs(spectrum.getEnergy()[i])));
        }
    }
------------------------------------------------------------


==============Node edit action set cache hit.===============

    NodeDeletionAction{target_node=1641}
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [regular node]compatible replacement
    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream                              ) {
        }
        public static double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i -> Math.abs(spectrum.getEnergy()[i])));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java.util.function.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream                              ) {
        }
        public static double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i -> Math.abs(spectrum.getEnergy()[i])));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [regular node]compatible replacement
    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    public class Main {
        public static <T>T maxKey(                  Function<T, Double> function) {
        }
        public static double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i -> Math.abs(spectrum.getEnergy()[i])));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java.util.function.*;
    public class Main {
        public static <T>T maxKey(                  Function<T, Double> function) {
        }
        public static double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i -> Math.abs(spectrum.getEnergy()[i])));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 1641
------------------------------------------------------------


================Reducing node 128, size=139=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i -> Math.abs(spectrum.getEnergy()[i])));
        }
    }
------------------------------------------------------------


============Testing the following program: pass=============

// edit action set type: [regular node]compatible replacement
    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java.util.function.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


============Test script execution is cancelled.=============

It took less than 1 second to cancel the task.

    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i -> Math                             ));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java.util.function.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i -> Math                             ));
        }
    }
------------------------------------------------------------


============Test script execution is cancelled.=============

It took less than 1 second to cancel the task.

    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->                               i  ));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java.util.function.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->                               i  ));
        }
    }
------------------------------------------------------------


============Test script execution is cancelled.=============

It took less than 1 second to cancel the task.

    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->          spectrum.getEnergy()    ));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java.util.function.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->          spectrum.getEnergy()    ));
        }
    }
------------------------------------------------------------


============Test script execution is cancelled.=============

It took less than 1 second to cancel the task.

    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->          spectrum.getEnergy()[i] ));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java.util.function.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->          spectrum.getEnergy()[i] ));
        }
    }
------------------------------------------------------------


===========TestScriptExecutionCacheEntryEviction============

0 entries are removed: 0 --> 0.
------------------------------------------------------------


===========Node edit action set cache is cleared.===========

    size before clearance = 8
------------------------------------------------------------


================The best program is updated.================

token count change 139 -> 127
------------------------------------------------------------


===================Node reduction is done===================

Succeeded to reduce node 128 from 139 to 127
------------------------------------------------------------


================Reducing node 210, size=127=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [regular node]compatible replacement
    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream                              , i ->      abs                         ));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java.util.function.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream                              , i ->      abs                         ));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [regular node]compatible replacement
    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(                                boxed  , i ->      abs                         ));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java.util.function.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(                                boxed  , i ->      abs                         ));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [regular node]compatible replacement
    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(          range                        , i ->      abs                         ));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java.util.function.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(          range                        , i ->      abs                         ));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [regular node]compatible replacement
    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex)        , i ->      abs                         ));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java.util.function.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex)        , i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 210
------------------------------------------------------------


================Reducing node 416, size=127=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
        private int getWidth() {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


============Testing the following program: pass=============

// edit action set type: [regular_node]can be epsilon
    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java.util.function.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


============Test script execution is cancelled.=============

It took less than 1 second to cancel the task.

    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
                               {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java.util.function.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
                               {
            return 0;
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===========TestScriptExecutionCacheEntryEviction============

0 entries are removed: 0 --> 0.
------------------------------------------------------------


===========Node edit action set cache is cleared.===========

    size before clearance = 6
------------------------------------------------------------


================The best program is updated.================

token count change 127 -> 117
------------------------------------------------------------


===================Node reduction is done===================

Succeeded to reduce node 416 from 127 to 117
------------------------------------------------------------


================Reducing node 369, size=117=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
        private double getMaxSpectrumCoord() {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


============Testing the following program: pass=============

// edit action set type: [regular_node]can be epsilon
    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java.util.function.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


============Test script execution is cancelled.=============

It took less than 1 second to cancel the task.

    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
                                             {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java.util.function.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
                                             {
            return 0;
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===========TestScriptExecutionCacheEntryEviction============

0 entries are removed: 0 --> 0.
------------------------------------------------------------


===========Node edit action set cache is cleared.===========

    size before clearance = 2
------------------------------------------------------------


================The best program is updated.================

token count change 117 -> 107
------------------------------------------------------------


===================Node reduction is done===================

Succeeded to reduce node 369 from 117 to 107
------------------------------------------------------------


================Reducing node 322, size=107=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
        private double getMinSpectrumCoord() {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


============Testing the following program: pass=============

// edit action set type: [regular_node]can be epsilon
    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java.util.function.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


============Test script execution is cancelled.=============

It took less than 1 second to cancel the task.

    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
                                             {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java.util.function.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
                                             {
            return 0;
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===========TestScriptExecutionCacheEntryEviction============

0 entries are removed: 0 --> 0.
------------------------------------------------------------


===========Node edit action set cache is cleared.===========

    size before clearance = 2
------------------------------------------------------------


================The best program is updated.================

token count change 107 -> 97
------------------------------------------------------------


===================Node reduction is done===================

Succeeded to reduce node 322 from 107 to 97
------------------------------------------------------------


================Reducing node 2143, size=97=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [regular_node]can be epsilon
    --file: Main.java--
    import java.util.function.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.function.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2143
------------------------------------------------------------


================Reducing node 2129, size=97=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [regular_node]can be epsilon
    --file: Main.java--
    import java.util.stream.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2129
------------------------------------------------------------


=================Reducing node 219, size=97=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [regular node]compatible replacement
    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(                0              .boxed(), i ->      abs                         ));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java.util.function.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(                0              .boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 219
------------------------------------------------------------


================Reducing node 1644, size=97=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 1644
------------------------------------------------------------


=================Reducing node 222, size=97=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 222
------------------------------------------------------------


================Reducing node 1650, size=97=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {
        }
        public static double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [regular node]compatible replacement
    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function            function) {
        }
        public static double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java.util.function.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream, Function            function) {
        }
        public static double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


============Testing the following program: pass=============

// edit action set type: [regular node]compatible replacement
    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream,          T          function) {
        }
        public static double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java.util.function.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream,          T          function) {
        }
        public static double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===========TestScriptExecutionCacheEntryEviction============

0 entries are removed: 0 --> 0.
------------------------------------------------------------


===========Node edit action set cache is cleared.===========

    size before clearance = 5
------------------------------------------------------------


================The best program is updated.================

token count change 97 -> 92
------------------------------------------------------------


===================Node reduction is done===================

Succeeded to reduce node 1650 from 97 to 92
------------------------------------------------------------


================Reducing node 1642, size=92=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream,          T          function) {
        }
        public static double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [kleene_star:optional__formalParameterList_2]dd@1
    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    public class Main {
        public static <T>T maxKey(                           T          function) {
        }
        public static double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java.util.function.*;
    public class Main {
        public static <T>T maxKey(                           T          function) {
        }
        public static double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 1642
------------------------------------------------------------


================Reducing node 1672, size=92=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream,          T          function) {
        }
        public static double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


==============Node edit action set cache hit.===============

    NodeDeletionAction{target_node=1672}
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 1672
------------------------------------------------------------


=================Reducing node 465, size=92=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream,          T          function) {
        }
        public static double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 465
------------------------------------------------------------


================Reducing node 2145, size=92=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream,          T          function) {
        }
        public static double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [regular node]compatible replacement
    --file: Main.java--
    import java            .*;
    import java.util.function.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream,          T          function) {
        }
        public static double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java            .*;
    import java.util.function.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream,          T          function) {
        }
        public static double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [regular node]compatible replacement
    --file: Main.java--
    import      util       .*;
    import java.util.function.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream,          T          function) {
        }
        public static double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import      util       .*;
    import java.util.function.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream,          T          function) {
        }
        public static double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [regular node]compatible replacement
    --file: Main.java--
    import           stream.*;
    import java.util.function.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream,          T          function) {
        }
        public static double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import           stream.*;
    import java.util.function.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream,          T          function) {
        }
        public static double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2145
------------------------------------------------------------


================Reducing node 2131, size=92=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java.util.function.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream,          T          function) {
        }
        public static double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [regular node]compatible replacement
    --file: Main.java--
    import java.util.stream.*;
    import      util         .*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream,          T          function) {
        }
        public static double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import      util         .*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream,          T          function) {
        }
        public static double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [regular node]compatible replacement
    --file: Main.java--
    import java.util.stream.*;
    import           function.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream,          T          function) {
        }
        public static double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import           function.*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream,          T          function) {
        }
        public static double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


============Testing the following program: pass=============

// edit action set type: [regular node]compatible replacement
    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream,          T          function) {
        }
        public static double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java              .*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream,          T          function) {
        }
        public static double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===========TestScriptExecutionCacheEntryEviction============

0 entries are removed: 0 --> 0.
------------------------------------------------------------


===========Node edit action set cache is cleared.===========

    size before clearance = 7
------------------------------------------------------------


================The best program is updated.================

token count change 92 -> 88
------------------------------------------------------------


===================Node reduction is done===================

Succeeded to reduce node 2131 from 92 to 88
------------------------------------------------------------


================Reducing node 1676, size=88=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream,          T          function) {
        }
        public static double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 1676
------------------------------------------------------------


=================Reducing node 739, size=88=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream,          T          function) {
        }
        public static double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 739
------------------------------------------------------------


=================Reducing node 281, size=88=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream,          T          function) {
        }
        public static double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [regular_node]can be epsilon
    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream,          T          function) {
        }
        public static double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
                   {
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java              .*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream,          T          function) {
        }
        public static double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
                   {
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 281
------------------------------------------------------------


=================Reducing node 225, size=88=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream,          T          function) {
        }
        public static double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 225
------------------------------------------------------------


================Reducing node 2150, size=88=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream,          T          function) {
        }
        public static double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [kleene_star:kleene_star__packageName_1]dd@2
    --file: Main.java--
    import java            .*;
    import java              .*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream,          T          function) {
        }
        public static double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java            .*;
    import java              .*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream,          T          function) {
        }
        public static double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [kleene_star:kleene_star__packageName_1]dd@1
    --file: Main.java--
    import java.util       .*;
    import java              .*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream,          T          function) {
        }
        public static double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util       .*;
    import java              .*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream,          T          function) {
        }
        public static double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [kleene_star:kleene_star__packageName_1]dd@1
    --file: Main.java--
    import java     .stream.*;
    import java              .*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream,          T          function) {
        }
        public static double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java     .stream.*;
    import java              .*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream,          T          function) {
        }
        public static double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2150
------------------------------------------------------------


================Reducing node 1682, size=88=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream,          T          function) {
        }
        public static double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [regular node]compatible replacement
    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
    public class Main {
        public static <T>T maxKey(       T  stream,          T          function) {
        }
        public static double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java              .*;
    public class Main {
        public static <T>T maxKey(       T  stream,          T          function) {
        }
        public static double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [regular node]compatible replacement
    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
    public class Main {
        public static <T>T maxKey(Stream    stream,          T          function) {
        }
        public static double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java              .*;
    public class Main {
        public static <T>T maxKey(Stream    stream,          T          function) {
        }
        public static double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 1682
------------------------------------------------------------


=================Reducing node 282, size=88=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream,          T          function) {
        }
        public static double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 282
------------------------------------------------------------


=================Reducing node 212, size=88=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream,          T          function) {
        }
        public static double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [kleene_star:kleene_star__primary_1]dd@1
    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream,          T          function) {
        }
        public static double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex)        , i ->      abs                         ));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java              .*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream,          T          function) {
        }
        public static double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex)        , i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 212
------------------------------------------------------------


=================Reducing node 214, size=88=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream,          T          function) {
        }
        public static double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


==============Node edit action set cache hit.===============

    NodeDeletionAction{target_node=214}
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 214
------------------------------------------------------------


================Reducing node 1684, size=88=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream,          T          function) {
        }
        public static double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [kleene_star:optional__classType_lfno_classOrInterfaceType_2]dd@1
    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
    public class Main {
        public static <T>T maxKey(Stream    stream,          T          function) {
        }
        public static double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java              .*;
    public class Main {
        public static <T>T maxKey(Stream    stream,          T          function) {
        }
        public static double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 1684
------------------------------------------------------------


================Reducing node 1685, size=88=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream,          T          function) {
        }
        public static double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


==============Node edit action set cache hit.===============

    NodeDeletionAction{target_node=1685}
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 1685
------------------------------------------------------------


================Reducing node 1633, size=88=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream,          T          function) {
        }
        public static double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [kleene_star:optional__methodHeader_5]dd@1
    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
    public class Main {
        public static    T maxKey(Stream<T> stream,          T          function) {
        }
        public static double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java              .*;
    public class Main {
        public static    T maxKey(Stream<T> stream,          T          function) {
        }
        public static double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 1633
------------------------------------------------------------


================Reducing node 1701, size=88=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream,          T          function) {
        }
        public static double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


==============Node edit action set cache hit.===============

    NodeDeletionAction{target_node=1701}
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 1701
------------------------------------------------------------


=================Reducing node 286, size=88=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream,          T          function) {
        }
        public static double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex = 0;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


============Testing the following program: pass=============

// edit action set type: [regular node]compatible replacement
    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream,          T          function) {
        }
        public static double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java              .*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream,          T          function) {
        }
        public static double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===========TestScriptExecutionCacheEntryEviction============

0 entries are removed: 0 --> 0.
------------------------------------------------------------


===========Node edit action set cache is cleared.===========

    size before clearance = 10
------------------------------------------------------------


================The best program is updated.================

token count change 88 -> 86
------------------------------------------------------------


===================Node reduction is done===================

Succeeded to reduce node 286 from 88 to 86
------------------------------------------------------------


=================Reducing node 228, size=86=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream,          T          function) {
        }
        public static double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [kleene_star:optional__explicitConstructorInvocation_2]dd@1
    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream,          T          function) {
        }
        public static double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(              ).boxed(), i ->      abs                         ));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java              .*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream,          T          function) {
        }
        public static double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(              ).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 228
------------------------------------------------------------


=================Reducing node 229, size=86=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream,          T          function) {
        }
        public static double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


==============Node edit action set cache hit.===============

    NodeDeletionAction{target_node=229}
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [regular node]compatible replacement
    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream,          T          function) {
        }
        public static double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(   maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java              .*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream,          T          function) {
        }
        public static double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(   maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [regular node]compatible replacement
    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream,          T          function) {
        }
        public static double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0             ).boxed(), i ->      abs                         ));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java              .*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream,          T          function) {
        }
        public static double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0             ).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 229
------------------------------------------------------------


================Reducing node 2152, size=86=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream,          T          function) {
        }
        public static double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [regular_node]can be epsilon
    --file: Main.java--
    import java.util       .*;
    import java              .*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream,          T          function) {
        }
        public static double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util       .*;
    import java              .*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream,          T          function) {
        }
        public static double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2152
------------------------------------------------------------


================Reducing node 2151, size=86=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream,          T          function) {
        }
        public static double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [regular_node]can be epsilon
    --file: Main.java--
    import java     .stream.*;
    import java              .*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream,          T          function) {
        }
        public static double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java     .stream.*;
    import java              .*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream,          T          function) {
        }
        public static double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2151
------------------------------------------------------------


================Reducing node 1215, size=86=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
    public class Main {
        public static <T>T maxKey(Stream<T> stream,          T          function) {
        }
        public static double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


============Testing the following program: pass=============

// edit action set type: [kleene_star:kleene_star__methodDeclaration_1]dd@2
    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
    public class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
        public static double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java              .*;
    public class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
        public static double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===========TestScriptExecutionCacheEntryEviction============

0 entries are removed: 0 --> 0.
------------------------------------------------------------


===========Node edit action set cache is cleared.===========

    size before clearance = 6
------------------------------------------------------------


================The best program is updated.================

token count change 86 -> 84
------------------------------------------------------------


===================Node reduction is done===================

Succeeded to reduce node 1215 from 86 to 84
------------------------------------------------------------


=================Reducing node 759, size=84=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
    public class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
        public static double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


============Testing the following program: pass=============

// edit action set type: [kleene_star:kleene_star__methodDeclaration_1]dd@2
    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
    public class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                      double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java              .*;
    public class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                      double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===========TestScriptExecutionCacheEntryEviction============

0 entries are removed: 0 --> 0.
------------------------------------------------------------


===========Node edit action set cache is cleared.===========

    size before clearance = 1
------------------------------------------------------------


================The best program is updated.================

token count change 84 -> 82
------------------------------------------------------------


===================Node reduction is done===================

Succeeded to reduce node 759 from 84 to 82
------------------------------------------------------------


=================Reducing node 742, size=82=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
    public class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                      double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [kleene_star:optional__methodDeclarator_1]dd@1
    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
    public class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                      double interpolate(                                                    ) {
        }
        protected int getXByFrequency(                ) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java              .*;
    public class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                      double interpolate(                                                    ) {
        }
        protected int getXByFrequency(                ) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 742
------------------------------------------------------------


=================Reducing node 747, size=82=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
    public class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                      double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


==============Node edit action set cache hit.===============

    NodeDeletionAction{target_node=747}
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 747
------------------------------------------------------------


=================Reducing node 231, size=82=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
    public class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                      double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [kleene_star:kleene_star__argumentList_2]dd@1
    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
    public class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                      double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0             ).boxed(), i ->      abs                         ));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java              .*;
    public class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                      double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0             ).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 231
------------------------------------------------------------


=================Reducing node 232, size=82=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
    public class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                      double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


==============Node edit action set cache hit.===============

    NodeDeletionAction{target_node=232}
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 232
------------------------------------------------------------


================Reducing node 2156, size=82=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
    public class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                      double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2156
------------------------------------------------------------


================Reducing node 2155, size=82=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
    public class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                      double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2155
------------------------------------------------------------


================Reducing node 2154, size=82=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
    public class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                      double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2154
------------------------------------------------------------


================Reducing node 2153, size=82=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
    public class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                      double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2153
------------------------------------------------------------


================Reducing node 2149, size=82=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
    public class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                      double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2149
------------------------------------------------------------


================Reducing node 2148, size=82=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
    public class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                      double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2148
------------------------------------------------------------


================Reducing node 2147, size=82=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
    public class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                      double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2147
------------------------------------------------------------


================Reducing node 2146, size=82=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
    public class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                      double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2146
------------------------------------------------------------


================Reducing node 2144, size=82=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
    public class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                      double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2144
------------------------------------------------------------


================Reducing node 2135, size=82=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
    public class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                      double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2135
------------------------------------------------------------


================Reducing node 2134, size=82=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
    public class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                      double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2134
------------------------------------------------------------


================Reducing node 2133, size=82=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
    public class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                      double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2133
------------------------------------------------------------


================Reducing node 2132, size=82=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
    public class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                      double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2132
------------------------------------------------------------


================Reducing node 2130, size=82=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
    public class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                      double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2130
------------------------------------------------------------


================Reducing node 1706, size=82=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
    public class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                      double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 1706
------------------------------------------------------------


================Reducing node 1704, size=82=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
    public class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                      double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 1704
------------------------------------------------------------


================Reducing node 1702, size=82=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
    public class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                      double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 1702
------------------------------------------------------------


================Reducing node 1699, size=82=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
    public class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                      double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 1699
------------------------------------------------------------


================Reducing node 1694, size=82=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
    public class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                      double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 1694
------------------------------------------------------------


================Reducing node 1688, size=82=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
    public class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                      double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 1688
------------------------------------------------------------


================Reducing node 1686, size=82=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
    public class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                      double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 1686
------------------------------------------------------------


================Reducing node 1683, size=82=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
    public class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                      double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 1683
------------------------------------------------------------


================Reducing node 1679, size=82=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
    public class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                      double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 1679
------------------------------------------------------------


================Reducing node 1674, size=82=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
    public class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                      double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 1674
------------------------------------------------------------


================Reducing node 1671, size=82=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
    public class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                      double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 1671
------------------------------------------------------------


================Reducing node 1647, size=82=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
    public class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                      double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 1647
------------------------------------------------------------


================Reducing node 1639, size=82=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
    public class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                      double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 1639
------------------------------------------------------------


================Reducing node 1637, size=82=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
    public class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                      double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 1637
------------------------------------------------------------


================Reducing node 1636, size=82=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
    public class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                      double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 1636
------------------------------------------------------------


================Reducing node 1221, size=82=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
    public class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                      double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 1221
------------------------------------------------------------


================Reducing node 1219, size=82=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
    public class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                      double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 1219
------------------------------------------------------------


=================Reducing node 981, size=82=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
    public class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                      double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 981
------------------------------------------------------------


=================Reducing node 933, size=82=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
    public class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                      double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 933
------------------------------------------------------------


=================Reducing node 931, size=82=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
    public class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                      double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 931
------------------------------------------------------------


=================Reducing node 930, size=82=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
    public class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                      double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 930
------------------------------------------------------------


=================Reducing node 765, size=82=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
    public class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                      double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 765
------------------------------------------------------------


=================Reducing node 763, size=82=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
    public class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                      double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 763
------------------------------------------------------------


=================Reducing node 755, size=82=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
    public class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                      double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 755
------------------------------------------------------------


=================Reducing node 752, size=82=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
    public class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                      double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 752
------------------------------------------------------------


=================Reducing node 750, size=82=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
    public class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                      double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 750
------------------------------------------------------------


=================Reducing node 743, size=82=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
    public class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                      double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 743
------------------------------------------------------------


=================Reducing node 741, size=82=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
    public class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                      double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 741
------------------------------------------------------------


=================Reducing node 740, size=82=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
    public class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                      double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 740
------------------------------------------------------------


=================Reducing node 470, size=82=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
    public class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                      double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 470
------------------------------------------------------------


=================Reducing node 468, size=82=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
    public class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                      double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 468
------------------------------------------------------------


=================Reducing node 464, size=82=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
    public class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                      double interpolate(                                                    ) {
        }
        protected int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


============Testing the following program: pass=============

// edit action set type: [kleene_star:kleene_star__methodDeclaration_1]dd@1
    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
    public class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                      double interpolate(                                                    ) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java              .*;
    public class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                      double interpolate(                                                    ) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===========TestScriptExecutionCacheEntryEviction============

0 entries are removed: 0 --> 0.
------------------------------------------------------------


===========Node edit action set cache is cleared.===========

    size before clearance = 3
------------------------------------------------------------


================The best program is updated.================

token count change 82 -> 81
------------------------------------------------------------


===================Node reduction is done===================

Succeeded to reduce node 464 from 82 to 81
------------------------------------------------------------


=================Reducing node 315, size=81=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
    public class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                      double interpolate(                                                    ) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 315
------------------------------------------------------------


=================Reducing node 313, size=81=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
    public class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                      double interpolate(                                                    ) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 313
------------------------------------------------------------


=================Reducing node 283, size=81=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
    public class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                      double interpolate(                                                    ) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 283
------------------------------------------------------------


=================Reducing node 280, size=81=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
    public class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                      double interpolate(                                                    ) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 280
------------------------------------------------------------


=================Reducing node 278, size=81=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
    public class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                      double interpolate(                                                    ) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 278
------------------------------------------------------------


=================Reducing node 277, size=81=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
    public class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                      double interpolate(                                                    ) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 277
------------------------------------------------------------


=================Reducing node 275, size=81=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
    public class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                      double interpolate(                                                    ) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 275
------------------------------------------------------------


=================Reducing node 273, size=81=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
    public class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                      double interpolate(                                                    ) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 273
------------------------------------------------------------


=================Reducing node 272, size=81=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
    public class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                      double interpolate(                                                    ) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 272
------------------------------------------------------------


=================Reducing node 252, size=81=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
    public class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                      double interpolate(                                                    ) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 252
------------------------------------------------------------


=================Reducing node 233, size=81=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
    public class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                      double interpolate(                                                    ) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 233
------------------------------------------------------------


=================Reducing node 227, size=81=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
    public class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                      double interpolate(                                                    ) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 227
------------------------------------------------------------


=================Reducing node 226, size=81=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
    public class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                      double interpolate(                                                    ) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 226
------------------------------------------------------------


=================Reducing node 224, size=81=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
    public class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                      double interpolate(                                                    ) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 224
------------------------------------------------------------


=================Reducing node 221, size=81=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
    public class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                      double interpolate(                                                    ) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 221
------------------------------------------------------------


=================Reducing node 218, size=81=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
    public class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                      double interpolate(                                                    ) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 218
------------------------------------------------------------


=================Reducing node 217, size=81=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
    public class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                      double interpolate(                                                    ) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 217
------------------------------------------------------------


=================Reducing node 216, size=81=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
    public class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                      double interpolate(                                                    ) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 216
------------------------------------------------------------


=================Reducing node 215, size=81=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
    public class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                      double interpolate(                                                    ) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 215
------------------------------------------------------------


=================Reducing node 193, size=81=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
    public class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                      double interpolate(                                                    ) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 193
------------------------------------------------------------


=================Reducing node 135, size=81=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
    public class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                      double interpolate(                                                    ) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 135
------------------------------------------------------------


=================Reducing node 107, size=81=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
    public class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                      double interpolate(                                                    ) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 107
------------------------------------------------------------


=================Reducing node 103, size=81=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
    public class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                      double interpolate(                                                    ) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 103
------------------------------------------------------------


=================Reducing node 97, size=81==================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
    public class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                      double interpolate(                                                    ) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 97
------------------------------------------------------------


=================Reducing node 94, size=81==================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
    public class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                      double interpolate(                                                    ) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 94
------------------------------------------------------------


=================Reducing node 70, size=81==================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
    public class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                      double interpolate(                                                    ) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 70
------------------------------------------------------------


=================Reducing node 67, size=81==================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
    public class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                      double interpolate(                                                    ) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 67
------------------------------------------------------------


=================Reducing node 44, size=81==================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
    public class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                      double interpolate(                                                    ) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 44
------------------------------------------------------------


=================Reducing node 37, size=81==================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
    public class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                      double interpolate(                                                    ) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 37
------------------------------------------------------------


=================Reducing node 31, size=81==================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
    public class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                      double interpolate(                                                    ) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 31
------------------------------------------------------------


=================Reducing node 29, size=81==================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
    public class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                      double interpolate(                                                    ) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 29
------------------------------------------------------------


=================Reducing node 13, size=81==================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
    public class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                      double interpolate(                                                    ) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 13
------------------------------------------------------------


=================Reducing node 11, size=81==================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
    public class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                      double interpolate(                                                    ) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 11
------------------------------------------------------------


==================Reducing node 9, size=81==================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
    public class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                      double interpolate(                                                    ) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 9
------------------------------------------------------------


==================Reducing node 8, size=81==================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
    public class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                      double interpolate(                                                    ) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 8
------------------------------------------------------------


==================Reducing node 7, size=81==================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
    public class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                      double interpolate(                                                    ) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


============Testing the following program: pass=============

// edit action set type: [kleene_star:kleene_star__normalClassDeclaration_1]dd@1
    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                      double interpolate(                                                    ) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java              .*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                      double interpolate(                                                    ) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===========TestScriptExecutionCacheEntryEviction============

0 entries are removed: 0 --> 0.
------------------------------------------------------------


===========Node edit action set cache is cleared.===========

    size before clearance = 1
------------------------------------------------------------


================The best program is updated.================

token count change 81 -> 80
------------------------------------------------------------


===================Node reduction is done===================

Succeeded to reduce node 7 from 81 to 80
------------------------------------------------------------


Rebuilding spar-tree: The spartree is rebuilt.

=Fixpoint iteration 2. Reducer: perses_node_priority_with_dfs_delta

The spar-tree is the following.
compilationUnit {id=2158,slot_type=compilationUnit}
|___(*) {id=2159,slot_type=kleene_star__compilationUnit_2}
|   |___typeImportOnDemandDeclaration {id=2480,slot_type=[importDeclaration,typeImportOnDemandDeclaration]}
|   |   |___Token:import {id=2481,slot_type=IMPORT}
|   |   |___packageName {id=2482,slot_type=packageName}
|   |   |   |___Token:java {id=2486,slot_type=Identifier}
|   |   |   |___(*) {id=2487,slot_type=kleene_star__packageName_1}
|   |   |       |___aux_rule__packageName_2 {id=2488,slot_type=aux_rule__packageName_2}
|   |   |       |   |___Token:. {id=2492,slot_type=DOT}
|   |   |       |   |___Token:util {id=2493,slot_type=Identifier}
|   |   |       |___aux_rule__packageName_2 {id=2489,slot_type=aux_rule__packageName_2}
|   |   |           |___Token:. {id=2490,slot_type=DOT}
|   |   |           |___Token:stream {id=2491,slot_type=Identifier}
|   |   |___Token:. {id=2483,slot_type=DOT}
|   |   |___Token:* {id=2484,slot_type=MUL}
|   |   |___Token:; {id=2485,slot_type=SEMI}
|   |___typeImportOnDemandDeclaration {id=2473,slot_type=[importDeclaration,typeImportOnDemandDeclaration]}
|       |___Token:import {id=2474,slot_type=IMPORT}
|       |___Token:java {id=2479,slot_type=[packageName,Identifier]}
|       |___Token:. {id=2476,slot_type=DOT}
|       |___Token:* {id=2477,slot_type=MUL}
|       |___Token:; {id=2478,slot_type=SEMI}
|___(*) {id=2160,slot_type=kleene_star__compilationUnit_3}
    |___normalClassDeclaration {id=2163,slot_type=[typeDeclaration,classDeclaration,normalClassDeclaration]}
        |___Token:class {id=2164,slot_type=CLASS}
        |___Token:Main {id=2165,slot_type=Identifier}
        |___classBody {id=2166,slot_type=classBody}
            |___Token:{ {id=2167,slot_type=LBRACE}
            |___(*) {id=2168,slot_type=kleene_star__classBody_1}
            |   |___methodDeclaration {id=2411,slot_type=[classBodyDeclaration,methodDeclaration]}
            |   |   |___methodHeader {id=2412,slot_type=methodHeader}
            |   |   |   |___(?) {id=2417,slot_type=optional__methodHeader_5}
            |   |   |   |   |___typeParameters {id=2465,slot_type=[aux_rule__methodHeader_4,typeParameters]}
            |   |   |   |       |___Token:< {id=2466,slot_type=LT}
            |   |   |   |       |___Token:T {id=2470,slot_type=[typeParameterList,typeParameter,Identifier]}
            |   |   |   |       |___Token:> {id=2468,slot_type=GT}
            |   |   |   |___Token:T {id=2463,slot_type=[result,unannType,unannClassOrInterfaceType,altnt_block__unannClassOrInterfaceType_3,unannClassType_lfno_unannClassOrInterfaceType,Identifier]}
            |   |   |   |___methodDeclarator {id=2419,slot_type=methodDeclarator}
            |   |   |       |___Token:maxKey {id=2420,slot_type=Identifier}
            |   |   |       |___Token:( {id=2421,slot_type=LPAREN}
            |   |   |       |___(?) {id=2422,slot_type=optional__methodDeclarator_1}
            |   |   |       |   |___aux_rule__formalParameterList_3 {id=2425,slot_type=[formalParameterList,aux_rule__formalParameterList_3]}
            |   |   |       |       |___(?) {id=2426,slot_type=optional__formalParameterList_2}
            |   |   |       |       |   |___aux_rule__formalParameterList_1 {id=2436,slot_type=aux_rule__formalParameterList_1}
            |   |   |       |       |       |___formalParameter {id=2440,slot_type=[formalParameters,altnt_block__formalParameters_5,formalParameter]}
            |   |   |       |       |       |   |___unannClassType_lfno_unannClassOrInterfaceType {id=2446,slot_type=[unannType,unannClassOrInterfaceType,altnt_block__unannClassOrInterfaceType_3,unannClassType_lfno_unannClassOrInterfaceType]}
            |   |   |       |       |       |   |   |___Token:Stream {id=2447,slot_type=Identifier}
            |   |   |       |       |       |   |   |___(?) {id=2448,slot_type=optional__classType_lfno_classOrInterfaceType_2}
            |   |   |       |       |       |   |       |___typeArguments {id=2449,slot_type=typeArguments}
            |   |   |       |       |       |   |           |___Token:< {id=2450,slot_type=LT}
            |   |   |       |       |       |   |           |___Token:T {id=2458,slot_type=[typeArgumentList,typeArgument,referenceType,classOrInterfaceType,altnt_block__classOrInterfaceType_3,classType_lfno_classOrInterfaceType,Identifier]}
            |   |   |       |       |       |   |           |___Token:> {id=2452,slot_type=GT}
            |   |   |       |       |       |   |___Token:stream {id=2443,slot_type=[variableDeclaratorId,Identifier]}
            |   |   |       |       |       |___Token:, {id=2438,slot_type=COMMA}
            |   |   |       |       |___formalParameter {id=2428,slot_type=[lastFormalParameter,formalParameter]}
            |   |   |       |           |___Token:T {id=2435,slot_type=[unannType,unannClassOrInterfaceType,altnt_block__unannClassOrInterfaceType_3,unannClassType_lfno_unannClassOrInterfaceType,Identifier]}
            |   |   |       |           |___Token:function {id=2431,slot_type=[variableDeclaratorId,Identifier]}
            |   |   |       |___Token:) {id=2423,slot_type=RPAREN}
            |   |   |___block {id=2414,slot_type=[methodBody,block]}
            |   |       |___Token:{ {id=2415,slot_type=LBRACE}
            |   |       |___Token:} {id=2416,slot_type=RBRACE}
            |   |___methodDeclaration {id=2397,slot_type=[classBodyDeclaration,methodDeclaration]}
            |   |   |___methodHeader {id=2398,slot_type=methodHeader}
            |   |   |   |___Token:double {id=2410,slot_type=[result,unannType,altnt_block__primitiveType_3,DOUBLE]}
            |   |   |   |___methodDeclarator {id=2404,slot_type=methodDeclarator}
            |   |   |       |___Token:interpolate {id=2405,slot_type=Identifier}
            |   |   |       |___Token:( {id=2406,slot_type=LPAREN}
            |   |   |       |___Token:) {id=2407,slot_type=RPAREN}
            |   |   |___block {id=2400,slot_type=[methodBody,block]}
            |   |       |___Token:{ {id=2401,slot_type=LBRACE}
            |   |       |___Token:} {id=2402,slot_type=RBRACE}
            |   |___methodDeclaration {id=2373,slot_type=[classBodyDeclaration,methodDeclaration]}
            |   |   |___methodHeader {id=2374,slot_type=methodHeader}
            |   |   |   |___Token:int {id=2396,slot_type=[result,unannType,altnt_block__primitiveType_3,INT]}
            |   |   |   |___methodDeclarator {id=2380,slot_type=methodDeclarator}
            |   |   |       |___Token:getXByFrequency {id=2381,slot_type=Identifier}
            |   |   |       |___Token:( {id=2382,slot_type=LPAREN}
            |   |   |       |___(?) {id=2383,slot_type=optional__methodDeclarator_1}
            |   |   |       |   |___formalParameter {id=2388,slot_type=[formalParameterList,aux_rule__formalParameterList_3,lastFormalParameter,formalParameter]}
            |   |   |       |       |___Token:double {id=2393,slot_type=[unannType,altnt_block__primitiveType_3,DOUBLE]}
            |   |   |       |       |___Token:frequency {id=2391,slot_type=[variableDeclaratorId,Identifier]}
            |   |   |       |___Token:) {id=2384,slot_type=RPAREN}
            |   |   |___block {id=2376,slot_type=[methodBody,block]}
            |   |       |___Token:{ {id=2377,slot_type=LBRACE}
            |   |       |___Token:} {id=2378,slot_type=RBRACE}
            |   |___block {id=2175,slot_type=[classBodyDeclaration,instanceInitializer,block]}
            |       |___Token:{ {id=2176,slot_type=LBRACE}
            |       |___(?) {id=2177,slot_type=optional__block_1}
            |       |   |___(+) {id=2179,slot_type=blockStatements}
            |       |       |___localVariableDeclarationStatement {id=2363,slot_type=[blockStatement,localVariableDeclarationStatement]}
            |       |       |   |___localVariableDeclaration {id=2364,slot_type=localVariableDeclaration}
            |       |       |   |   |___Token:int {id=2372,slot_type=[unannType,altnt_block__primitiveType_3,INT]}
            |       |       |   |   |___Token:maxBpmIndex {id=2370,slot_type=[variableDeclaratorList,variableDeclarator,variableDeclaratorId,Identifier]}
            |       |       |   |___Token:; {id=2365,slot_type=SEMI}
            |       |       |___localVariableDeclarationStatement {id=2182,slot_type=[blockStatement,localVariableDeclarationStatement]}
            |       |           |___localVariableDeclaration {id=2183,slot_type=localVariableDeclaration}
            |       |           |   |___Token:int {id=2362,slot_type=[unannType,altnt_block__primitiveType_3,INT]}
            |       |           |   |___variableDeclarator {id=2187,slot_type=[variableDeclaratorList,variableDeclarator]}
            |       |           |       |___Token:xcur {id=2360,slot_type=[variableDeclaratorId,Identifier]}
            |       |           |       |___(?) {id=2189,slot_type=optional__variableDeclarator_2}
            |       |           |           |___aux_rule__variableDeclarator_1 {id=2190,slot_type=aux_rule__variableDeclarator_1}
            |       |           |               |___Token:= {id=2191,slot_type=ASSIGN}
            |       |           |               |___methodInvocation_lfno_primary {id=2212,slot_type=[variableInitializer,expression,conditionalExpression,conditionalOrExpression,conditionalAndExpression,inclusiveOrExpression,exclusiveOrExpression,andExpression,equalityExpression,relationalExpression,shiftExpression,additiveExpression,multiplicativeExpression,unaryExpression,aux_rule__unaryExpression_3,unaryExpressionNotPlusMinus,postfixExpression,altnt_block__postfixExpression_3,primary,altnt_block__primary_2,methodInvocation_lfno_primary]}
            |       |           |                   |___aux_rule__methodInvocation_lfno_primary_15 {id=2215,slot_type=[altnt_block__methodInvocation_lfno_primary_10,aux_rule__methodInvocation_lfno_primary_15]}
            |       |           |                   |   |___Token:getXByFrequency {id=2359,slot_type=[altnt_block__methodInvocation_lfno_primary_12,methodName,Identifier]}
            |       |           |                   |   |___Token:( {id=2217,slot_type=LPAREN}
            |       |           |                   |   |___(?) {id=2218,slot_type=optional__explicitConstructorInvocation_2}
            |       |           |                   |       |___methodInvocation_lfno_primary {id=2239,slot_type=[argumentList,expression,conditionalExpression,conditionalOrExpression,conditionalAndExpression,inclusiveOrExpression,exclusiveOrExpression,andExpression,equalityExpression,relationalExpression,shiftExpression,additiveExpression,multiplicativeExpression,unaryExpression,aux_rule__unaryExpression_3,unaryExpressionNotPlusMinus,postfixExpression,altnt_block__postfixExpression_3,primary,altnt_block__primary_2,methodInvocation_lfno_primary]}
            |       |           |                   |           |___aux_rule__methodInvocation_lfno_primary_15 {id=2242,slot_type=[altnt_block__methodInvocation_lfno_primary_10,aux_rule__methodInvocation_lfno_primary_15]}
            |       |           |                   |           |   |___Token:maxKey {id=2357,slot_type=[altnt_block__methodInvocation_lfno_primary_12,methodName,Identifier]}
            |       |           |                   |           |   |___Token:( {id=2244,slot_type=LPAREN}
            |       |           |                   |           |   |___(?) {id=2245,slot_type=optional__explicitConstructorInvocation_2}
            |       |           |                   |           |       |___argumentList {id=2246,slot_type=argumentList}
            |       |           |                   |           |           |___primary {id=2292,slot_type=[expression,conditionalExpression,conditionalOrExpression,conditionalAndExpression,inclusiveOrExpression,exclusiveOrExpression,andExpression,equalityExpression,relationalExpression,shiftExpression,additiveExpression,multiplicativeExpression,unaryExpression,aux_rule__unaryExpression_3,unaryExpressionNotPlusMinus,postfixExpression,altnt_block__postfixExpression_3,primary]}
            |       |           |                   |           |           |   |___methodInvocation_lfno_primary {id=2301,slot_type=[altnt_block__primary_2,methodInvocation_lfno_primary]}
            |       |           |                   |           |           |   |   |___aux_rule__methodInvocation_lfno_primary_14 {id=2304,slot_type=[altnt_block__methodInvocation_lfno_primary_10,aux_rule__methodInvocation_lfno_primary_14]}
            |       |           |                   |           |           |   |   |   |___Token:IntStream {id=2355,slot_type=[typeName,Identifier]}
            |       |           |                   |           |           |   |   |   |___Token:. {id=2306,slot_type=DOT}
            |       |           |                   |           |           |   |   |   |___altnt_block__methodInvocation_13 {id=2307,slot_type=altnt_block__methodInvocation_13}
            |       |           |                   |           |           |   |   |       |___Token:range {id=2308,slot_type=Identifier}
            |       |           |                   |           |           |   |   |       |___Token:( {id=2309,slot_type=LPAREN}
            |       |           |                   |           |           |   |   |       |___(?) {id=2310,slot_type=optional__explicitConstructorInvocation_2}
            |       |           |                   |           |           |   |   |           |___argumentList {id=2311,slot_type=argumentList}
            |       |           |                   |           |           |   |   |               |___Token:0 {id=2354,slot_type=[expression,conditionalExpression,conditionalOrExpression,conditionalAndExpression,inclusiveOrExpression,exclusiveOrExpression,andExpression,equalityExpression,relationalExpression,shiftExpression,additiveExpression,multiplicativeExpression,unaryExpression,aux_rule__unaryExpression_3,unaryExpressionNotPlusMinus,postfixExpression,altnt_block__postfixExpression_3,primary,altnt_block__primary_2,literal,IntegerLiteral]}
            |       |           |                   |           |           |   |   |               |___(*) {id=2313,slot_type=kleene_star__argumentList_2}
            |       |           |                   |           |           |   |   |                   |___aux_rule__argumentList_1 {id=2314,slot_type=aux_rule__argumentList_1}
            |       |           |                   |           |           |   |   |                       |___Token:, {id=2315,slot_type=COMMA}
            |       |           |                   |           |           |   |   |                       |___Token:maxBpmIndex {id=2334,slot_type=[expression,conditionalExpression,conditionalOrExpression,conditionalAndExpression,inclusiveOrExpression,exclusiveOrExpression,andExpression,equalityExpression,relationalExpression,shiftExpression,additiveExpression,multiplicativeExpression,unaryExpression,aux_rule__unaryExpression_3,unaryExpressionNotPlusMinus,postfixExpression,altnt_block__postfixExpression_3,typeName,Identifier]}
            |       |           |                   |           |           |   |   |___Token:) {id=2303,slot_type=RPAREN}
            |       |           |                   |           |           |   |___(*) {id=2294,slot_type=kleene_star__primary_1}
            |       |           |                   |           |           |       |___methodInvocation_lf_primary {id=2296,slot_type=[primaryNoNewArray_lf_primary,methodInvocation_lf_primary]}
            |       |           |                   |           |           |           |___Token:. {id=2297,slot_type=DOT}
            |       |           |                   |           |           |           |___Token:boxed {id=2298,slot_type=Identifier}
            |       |           |                   |           |           |           |___Token:( {id=2299,slot_type=LPAREN}
            |       |           |                   |           |           |           |___Token:) {id=2300,slot_type=RPAREN}
            |       |           |                   |           |           |___(*) {id=2248,slot_type=kleene_star__argumentList_2}
            |       |           |                   |           |               |___aux_rule__argumentList_1 {id=2249,slot_type=aux_rule__argumentList_1}
            |       |           |                   |           |                   |___Token:, {id=2250,slot_type=COMMA}
            |       |           |                   |           |                   |___lambdaExpression {id=2252,slot_type=[expression,lambdaExpression]}
            |       |           |                   |           |                       |___Token:i {id=2275,slot_type=[lambdaParameters,Identifier]}
            |       |           |                   |           |                       |___Token:-> {id=2254,slot_type=ARROW}
            |       |           |                   |           |                       |___Token:abs {id=2274,slot_type=[lambdaBody,expression,conditionalExpression,conditionalOrExpression,conditionalAndExpression,inclusiveOrExpression,exclusiveOrExpression,andExpression,equalityExpression,relationalExpression,shiftExpression,additiveExpression,multiplicativeExpression,unaryExpression,aux_rule__unaryExpression_3,unaryExpressionNotPlusMinus,postfixExpression,altnt_block__postfixExpression_3,typeName,Identifier]}
            |       |           |                   |           |___Token:) {id=2241,slot_type=RPAREN}
            |       |           |                   |___Token:) {id=2214,slot_type=RPAREN}
            |       |           |___Token:; {id=2184,slot_type=SEMI}
            |       |___Token:} {id=2178,slot_type=RBRACE}
            |___Token:} {id=2169,slot_type=RBRACE}
------------------------------------------------------------


================Reducing node 2160, size=80=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                      double interpolate(                                                    ) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [kleene_star:kleene_star__compilationUnit_3]dd@1
    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java              .*;
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2160
------------------------------------------------------------


================Reducing node 2163, size=80=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                      double interpolate(                                                    ) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


==============Node edit action set cache hit.===============

    NodeDeletionAction{target_node=2163}
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2163
------------------------------------------------------------


================Reducing node 2166, size=80=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                      double interpolate(                                                    ) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2166
------------------------------------------------------------


================Reducing node 2168, size=80=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                      double interpolate(                                                    ) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [kleene_star:kleene_star__classBody_1]dd@4
    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
           class Main {
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java              .*;
           class Main {
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [kleene_star:kleene_star__classBody_1]dd@2
    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                      double interpolate(                                                    ) {
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java              .*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                      double interpolate(                                                    ) {
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [kleene_star:kleene_star__classBody_1]dd@1
    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                      double interpolate(                                                    ) {
        }
                  int getXByFrequency(double frequency) {
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java              .*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                      double interpolate(                                                    ) {
        }
                  int getXByFrequency(double frequency) {
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [kleene_star:kleene_star__classBody_1]dd@1
    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                      double interpolate(                                                    ) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java              .*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                      double interpolate(                                                    ) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [kleene_star:kleene_star__classBody_1]dd@2
    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
           class Main {
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java              .*;
           class Main {
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


============Testing the following program: pass=============

// edit action set type: [kleene_star:kleene_star__classBody_1]dd@1
    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java              .*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===========TestScriptExecutionCacheEntryEviction============

0 entries are removed: 0 --> 0.
------------------------------------------------------------


===========Node edit action set cache is cleared.===========

    size before clearance = 7
------------------------------------------------------------


================The best program is updated.================

token count change 80 -> 74
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [kleene_star:kleene_star__classBody_1]dd@1
    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
           class Main {
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java              .*;
           class Main {
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Succeeded to reduce node 2168 from 80 to 74
------------------------------------------------------------


================Reducing node 2175, size=74=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [regular_node]can be epsilon
    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java              .*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2175
------------------------------------------------------------


================Reducing node 2177, size=74=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [kleene_star:optional__block_1]dd@1
    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java              .*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2177
------------------------------------------------------------


================Reducing node 2179, size=74=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


==============Node edit action set cache hit.===============

    NodeDeletionAction{target_node=2179}
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [kleene_plus:blockStatements]remove whole except first
    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java              .*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
        }
    }
------------------------------------------------------------


==============Node edit action set cache hit.===============

    NodeDeletionAction{target_node=2182}
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [kleene_plus:blockStatements]dd@1
    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java              .*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2179
------------------------------------------------------------


================Reducing node 2182, size=74=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


==============Node edit action set cache hit.===============

    NodeDeletionAction{target_node=2182}
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2182
------------------------------------------------------------


================Reducing node 2183, size=74=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2183
------------------------------------------------------------


================Reducing node 2187, size=74=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [regular node]compatible replacement
    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur                                                                                                           ;
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java              .*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur                                                                                                           ;
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [regular node]compatible replacement
    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int        getXByFrequency                                                                                         ;
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java              .*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int        getXByFrequency                                                                                         ;
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2187
------------------------------------------------------------


================Reducing node 2189, size=74=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [kleene_star:optional__variableDeclarator_2]dd@1
    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur                                                                                                           ;
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java              .*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur                                                                                                           ;
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2189
------------------------------------------------------------


================Reducing node 2190, size=74=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


==============Node edit action set cache hit.===============

    NodeDeletionAction{target_node=2190}
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2190
------------------------------------------------------------


================Reducing node 2212, size=74=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [regular node]compatible replacement
    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency                                                                                         ;
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java              .*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency                                                                                         ;
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [regular node]compatible replacement
    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur =                 maxKey                                                                                  ;
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java              .*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur =                 maxKey                                                                                  ;
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [regular node]compatible replacement
    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur =                 maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ) ;
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java              .*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur =                 maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ) ;
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2212
------------------------------------------------------------


================Reducing node 2215, size=74=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [regular node]compatible replacement
    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur =                 maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                          );
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java              .*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur =                 maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                          );
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2215
------------------------------------------------------------


================Reducing node 2218, size=74=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [kleene_star:optional__explicitConstructorInvocation_2]dd@1
    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(                                                                                       );
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java              .*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(                                                                                       );
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2218
------------------------------------------------------------


================Reducing node 2239, size=74=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


==============Node edit action set cache hit.===============

    NodeDeletionAction{target_node=2239}
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [regular node]compatible replacement
    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey                                                                                 );
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java              .*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey                                                                                 );
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [regular node]compatible replacement
    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(       IntStream.range(0, maxBpmIndex)                                                 );
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java              .*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(       IntStream.range(0, maxBpmIndex)                                                 );
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [regular node]compatible replacement
    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(       IntStream.range(0, maxBpmIndex).boxed()                                         );
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java              .*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(       IntStream.range(0, maxBpmIndex).boxed()                                         );
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [regular node]compatible replacement
    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(       IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                          );
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java              .*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(       IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                          );
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2239
------------------------------------------------------------


================Reducing node 2242, size=74=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [regular node]compatible replacement
    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(       IntStream.range(0, maxBpmIndex                                                 ));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java              .*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(       IntStream.range(0, maxBpmIndex                                                 ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2242
------------------------------------------------------------


================Reducing node 2411, size=74=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


==============Node edit action set cache hit.===============

    NodeDeletionAction{target_node=2411}
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [regular node]compatible replacement
    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
           class Main {
                                                                                  {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java              .*;
           class Main {
                                                                                  {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2411
------------------------------------------------------------


================Reducing node 2245, size=74=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [kleene_star:optional__explicitConstructorInvocation_2]dd@1
    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(                                                                               ));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java              .*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(                                                                               ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2245
------------------------------------------------------------


================Reducing node 2246, size=74=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


==============Node edit action set cache hit.===============

    NodeDeletionAction{target_node=2246}
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [regular node]compatible replacement
    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream                                                                      ));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java              .*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream                                                                      ));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [regular node]compatible replacement
    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(                                boxed                                          ));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java              .*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(                                boxed                                          ));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [regular node]compatible replacement
    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(                                         i                                     ));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java              .*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(                                         i                                     ));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [regular node]compatible replacement
    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(                                                   abs                         ));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java              .*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(                                                   abs                         ));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [regular node]compatible replacement
    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(          range                                                                ));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java              .*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(          range                                                                ));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [regular node]compatible replacement
    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(                                         i ->      abs                         ));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java              .*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(                                         i ->      abs                         ));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [regular node]compatible replacement
    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex)                                                ));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java              .*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex)                                                ));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [regular node]compatible replacement
    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed()                                        ));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
    import java              .*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed()                                        ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2246
------------------------------------------------------------


================Reducing node 2412, size=74=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2412
------------------------------------------------------------


================Reducing node 2159, size=74=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
    import java              .*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [kleene_star:kleene_star__compilationUnit_2]dd@2
    --file: Main.java--
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
    --file: <formatted tokenized program in its original format>--
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


============Testing the following program: pass=============

// edit action set type: [kleene_star:kleene_star__compilationUnit_2]dd@1
    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===========TestScriptExecutionCacheEntryEviction============

0 entries are removed: 0 --> 0.
------------------------------------------------------------


===========Node edit action set cache is cleared.===========

    size before clearance = 30
------------------------------------------------------------


================The best program is updated.================

token count change 74 -> 69
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [kleene_star:kleene_star__compilationUnit_2]dd@1
    --file: Main.java--
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
    --file: <formatted tokenized program in its original format>--
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Succeeded to reduce node 2159 from 74 to 69
------------------------------------------------------------


================Reducing node 2292, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [regular node]compatible replacement
    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(                                boxed  , i ->      abs                         ));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(                                boxed  , i ->      abs                         ));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [regular node]compatible replacement
    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(          range                        , i ->      abs                         ));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(          range                        , i ->      abs                         ));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [regular node]compatible replacement
    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream                              , i ->      abs                         ));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream                              , i ->      abs                         ));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [regular node]compatible replacement
    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex)        , i ->      abs                         ));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex)        , i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2292
------------------------------------------------------------


================Reducing node 2419, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2419
------------------------------------------------------------


================Reducing node 2480, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


==============Node edit action set cache hit.===============

    NodeDeletionAction{target_node=2480}
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2480
------------------------------------------------------------


================Reducing node 2422, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [kleene_star:optional__methodDeclarator_1]dd@1
    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(                                              ) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(                                              ) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2422
------------------------------------------------------------


================Reducing node 2425, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


==============Node edit action set cache hit.===============

    NodeDeletionAction{target_node=2425}
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [regular node]compatible replacement
    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(                           T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(                           T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [regular node]compatible replacement
    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream                              ) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream                              ) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2425
------------------------------------------------------------


================Reducing node 2373, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [regular_node]can be epsilon
    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [regular node]compatible replacement
    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                                                        {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                                                        {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2373
------------------------------------------------------------


================Reducing node 2301, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [regular node]compatible replacement
    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(                0              .boxed(), i ->      abs                         ));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(                0              .boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2301
------------------------------------------------------------


================Reducing node 2304, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2304
------------------------------------------------------------


================Reducing node 2426, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [kleene_star:optional__formalParameterList_2]dd@1
    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(                           T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(                           T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2426
------------------------------------------------------------


================Reducing node 2436, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


==============Node edit action set cache hit.===============

    NodeDeletionAction{target_node=2436}
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2436
------------------------------------------------------------


================Reducing node 2374, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2374
------------------------------------------------------------


================Reducing node 2482, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [regular node]compatible replacement
    --file: Main.java--
    import java            .*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java            .*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [regular node]compatible replacement
    --file: Main.java--
    import      util       .*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import      util       .*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [regular node]compatible replacement
    --file: Main.java--
    import           stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import           stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2482
------------------------------------------------------------


================Reducing node 2440, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2440
------------------------------------------------------------


================Reducing node 2380, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2380
------------------------------------------------------------


================Reducing node 2307, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2307
------------------------------------------------------------


================Reducing node 2487, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [kleene_star:kleene_star__packageName_1]dd@2
    --file: Main.java--
    import java            .*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java            .*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [kleene_star:kleene_star__packageName_1]dd@1
    --file: Main.java--
    import java.util       .*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util       .*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [kleene_star:kleene_star__packageName_1]dd@1
    --file: Main.java--
    import java     .stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java     .stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2487
------------------------------------------------------------


================Reducing node 2446, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [regular node]compatible replacement
    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream    stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream    stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [regular node]compatible replacement
    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(       T  stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(       T  stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2446
------------------------------------------------------------


================Reducing node 2294, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [kleene_star:kleene_star__primary_1]dd@1
    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex)        , i ->      abs                         ));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex)        , i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2294
------------------------------------------------------------


================Reducing node 2296, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


==============Node edit action set cache hit.===============

    NodeDeletionAction{target_node=2296}
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2296
------------------------------------------------------------


================Reducing node 2248, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [kleene_star:kleene_star__argumentList_2]dd@1
    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed()                                        ));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed()                                        ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2248
------------------------------------------------------------


================Reducing node 2249, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


==============Node edit action set cache hit.===============

    NodeDeletionAction{target_node=2249}
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2249
------------------------------------------------------------


================Reducing node 2448, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [kleene_star:optional__classType_lfno_classOrInterfaceType_2]dd@1
    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream    stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream    stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2448
------------------------------------------------------------


================Reducing node 2449, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


==============Node edit action set cache hit.===============

    NodeDeletionAction{target_node=2449}
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2449
------------------------------------------------------------


================Reducing node 2417, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [kleene_star:optional__methodHeader_5]dd@1
    --file: Main.java--
    import java.util.stream.*;
           class Main {
                         T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
           class Main {
                         T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2417
------------------------------------------------------------


================Reducing node 2465, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


==============Node edit action set cache hit.===============

    NodeDeletionAction{target_node=2465}
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2465
------------------------------------------------------------


================Reducing node 2363, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [regular_node]can be epsilon
    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2363
------------------------------------------------------------


================Reducing node 2310, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [kleene_star:optional__explicitConstructorInvocation_2]dd@1
    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(              ).boxed(), i ->      abs                         ));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(              ).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2310
------------------------------------------------------------


================Reducing node 2311, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


==============Node edit action set cache hit.===============

    NodeDeletionAction{target_node=2311}
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [regular node]compatible replacement
    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0             ).boxed(), i ->      abs                         ));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0             ).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [regular node]compatible replacement
    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(   maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(   maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2311
------------------------------------------------------------


================Reducing node 2252, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [regular node]compatible replacement
    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i                                     ));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i                                     ));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [regular node]compatible replacement
    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(),           abs                         ));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(),           abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2252
------------------------------------------------------------


================Reducing node 2489, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


==============Node edit action set cache hit.===============

    NodeDeletionAction{target_node=2489}
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2489
------------------------------------------------------------


================Reducing node 2488, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


==============Node edit action set cache hit.===============

    NodeDeletionAction{target_node=2488}
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2488
------------------------------------------------------------


================Reducing node 2428, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2428
------------------------------------------------------------


================Reducing node 2414, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2414
------------------------------------------------------------


================Reducing node 2383, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [kleene_star:optional__methodDeclarator_1]dd@1
    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(                ) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(                ) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2383
------------------------------------------------------------


================Reducing node 2388, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


==============Node edit action set cache hit.===============

    NodeDeletionAction{target_node=2388}
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2388
------------------------------------------------------------


================Reducing node 2376, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2376
------------------------------------------------------------


================Reducing node 2364, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2364
------------------------------------------------------------


================Reducing node 2313, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [kleene_star:kleene_star__argumentList_2]dd@1
    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0             ).boxed(), i ->      abs                         ));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0             ).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2313
------------------------------------------------------------


================Reducing node 2314, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


==============Node edit action set cache hit.===============

    NodeDeletionAction{target_node=2314}
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2314
------------------------------------------------------------


================Reducing node 2493, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2493
------------------------------------------------------------


================Reducing node 2492, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2492
------------------------------------------------------------


================Reducing node 2491, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2491
------------------------------------------------------------


================Reducing node 2490, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2490
------------------------------------------------------------


================Reducing node 2486, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2486
------------------------------------------------------------


================Reducing node 2485, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2485
------------------------------------------------------------


================Reducing node 2484, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2484
------------------------------------------------------------


================Reducing node 2483, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2483
------------------------------------------------------------


================Reducing node 2481, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2481
------------------------------------------------------------


================Reducing node 2470, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2470
------------------------------------------------------------


================Reducing node 2468, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2468
------------------------------------------------------------


================Reducing node 2466, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2466
------------------------------------------------------------


================Reducing node 2463, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2463
------------------------------------------------------------


================Reducing node 2458, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2458
------------------------------------------------------------


================Reducing node 2452, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2452
------------------------------------------------------------


================Reducing node 2450, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2450
------------------------------------------------------------


================Reducing node 2447, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2447
------------------------------------------------------------


================Reducing node 2443, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2443
------------------------------------------------------------


================Reducing node 2438, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2438
------------------------------------------------------------


================Reducing node 2435, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2435
------------------------------------------------------------


================Reducing node 2431, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2431
------------------------------------------------------------


================Reducing node 2423, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2423
------------------------------------------------------------


================Reducing node 2421, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2421
------------------------------------------------------------


================Reducing node 2420, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2420
------------------------------------------------------------


================Reducing node 2416, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2416
------------------------------------------------------------


================Reducing node 2415, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2415
------------------------------------------------------------


================Reducing node 2396, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2396
------------------------------------------------------------


================Reducing node 2393, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2393
------------------------------------------------------------


================Reducing node 2391, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2391
------------------------------------------------------------


================Reducing node 2384, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2384
------------------------------------------------------------


================Reducing node 2382, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2382
------------------------------------------------------------


================Reducing node 2381, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2381
------------------------------------------------------------


================Reducing node 2378, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2378
------------------------------------------------------------


================Reducing node 2377, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2377
------------------------------------------------------------


================Reducing node 2372, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2372
------------------------------------------------------------


================Reducing node 2370, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2370
------------------------------------------------------------


================Reducing node 2365, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2365
------------------------------------------------------------


================Reducing node 2362, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2362
------------------------------------------------------------


================Reducing node 2360, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2360
------------------------------------------------------------


================Reducing node 2359, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2359
------------------------------------------------------------


================Reducing node 2357, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2357
------------------------------------------------------------


================Reducing node 2355, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2355
------------------------------------------------------------


================Reducing node 2354, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2354
------------------------------------------------------------


================Reducing node 2334, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2334
------------------------------------------------------------


================Reducing node 2315, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2315
------------------------------------------------------------


================Reducing node 2309, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2309
------------------------------------------------------------


================Reducing node 2308, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2308
------------------------------------------------------------


================Reducing node 2306, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2306
------------------------------------------------------------


================Reducing node 2303, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2303
------------------------------------------------------------


================Reducing node 2300, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2300
------------------------------------------------------------


================Reducing node 2299, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2299
------------------------------------------------------------


================Reducing node 2298, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2298
------------------------------------------------------------


================Reducing node 2297, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2297
------------------------------------------------------------


================Reducing node 2275, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2275
------------------------------------------------------------


================Reducing node 2274, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2274
------------------------------------------------------------


================Reducing node 2254, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2254
------------------------------------------------------------


================Reducing node 2250, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2250
------------------------------------------------------------


================Reducing node 2244, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2244
------------------------------------------------------------


================Reducing node 2241, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2241
------------------------------------------------------------


================Reducing node 2217, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2217
------------------------------------------------------------


================Reducing node 2214, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2214
------------------------------------------------------------


================Reducing node 2191, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2191
------------------------------------------------------------


================Reducing node 2184, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2184
------------------------------------------------------------


================Reducing node 2178, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2178
------------------------------------------------------------


================Reducing node 2176, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2176
------------------------------------------------------------


================Reducing node 2169, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2169
------------------------------------------------------------


================Reducing node 2167, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2167
------------------------------------------------------------


================Reducing node 2165, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2165
------------------------------------------------------------


================Reducing node 2164, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2164
------------------------------------------------------------


Rebuilding spar-tree: The spartree is rebuilt.

=Fixpoint iteration 3. Reducer: perses_node_priority_with_dfs_delta

The spar-tree is the following.
compilationUnit {id=2495,slot_type=compilationUnit}
|___(*) {id=2496,slot_type=kleene_star__compilationUnit_2}
|   |___typeImportOnDemandDeclaration {id=2794,slot_type=[importDeclaration,typeImportOnDemandDeclaration]}
|       |___Token:import {id=2795,slot_type=IMPORT}
|       |___packageName {id=2796,slot_type=packageName}
|       |   |___Token:java {id=2800,slot_type=Identifier}
|       |   |___(*) {id=2801,slot_type=kleene_star__packageName_1}
|       |       |___aux_rule__packageName_2 {id=2802,slot_type=aux_rule__packageName_2}
|       |       |   |___Token:. {id=2806,slot_type=DOT}
|       |       |   |___Token:util {id=2807,slot_type=Identifier}
|       |       |___aux_rule__packageName_2 {id=2803,slot_type=aux_rule__packageName_2}
|       |           |___Token:. {id=2804,slot_type=DOT}
|       |           |___Token:stream {id=2805,slot_type=Identifier}
|       |___Token:. {id=2797,slot_type=DOT}
|       |___Token:* {id=2798,slot_type=MUL}
|       |___Token:; {id=2799,slot_type=SEMI}
|___(*) {id=2497,slot_type=kleene_star__compilationUnit_3}
    |___normalClassDeclaration {id=2500,slot_type=[typeDeclaration,classDeclaration,normalClassDeclaration]}
        |___Token:class {id=2501,slot_type=CLASS}
        |___Token:Main {id=2502,slot_type=Identifier}
        |___classBody {id=2503,slot_type=classBody}
            |___Token:{ {id=2504,slot_type=LBRACE}
            |___(*) {id=2505,slot_type=kleene_star__classBody_1}
            |   |___methodDeclaration {id=2733,slot_type=[classBodyDeclaration,methodDeclaration]}
            |   |   |___methodHeader {id=2734,slot_type=methodHeader}
            |   |   |   |___(?) {id=2739,slot_type=optional__methodHeader_5}
            |   |   |   |   |___typeParameters {id=2787,slot_type=[aux_rule__methodHeader_4,typeParameters]}
            |   |   |   |       |___Token:< {id=2788,slot_type=LT}
            |   |   |   |       |___Token:T {id=2792,slot_type=[typeParameterList,typeParameter,Identifier]}
            |   |   |   |       |___Token:> {id=2790,slot_type=GT}
            |   |   |   |___Token:T {id=2785,slot_type=[result,unannType,unannClassOrInterfaceType,altnt_block__unannClassOrInterfaceType_3,unannClassType_lfno_unannClassOrInterfaceType,Identifier]}
            |   |   |   |___methodDeclarator {id=2741,slot_type=methodDeclarator}
            |   |   |       |___Token:maxKey {id=2742,slot_type=Identifier}
            |   |   |       |___Token:( {id=2743,slot_type=LPAREN}
            |   |   |       |___(?) {id=2744,slot_type=optional__methodDeclarator_1}
            |   |   |       |   |___aux_rule__formalParameterList_3 {id=2747,slot_type=[formalParameterList,aux_rule__formalParameterList_3]}
            |   |   |       |       |___(?) {id=2748,slot_type=optional__formalParameterList_2}
            |   |   |       |       |   |___aux_rule__formalParameterList_1 {id=2758,slot_type=aux_rule__formalParameterList_1}
            |   |   |       |       |       |___formalParameter {id=2762,slot_type=[formalParameters,altnt_block__formalParameters_5,formalParameter]}
            |   |   |       |       |       |   |___unannClassType_lfno_unannClassOrInterfaceType {id=2768,slot_type=[unannType,unannClassOrInterfaceType,altnt_block__unannClassOrInterfaceType_3,unannClassType_lfno_unannClassOrInterfaceType]}
            |   |   |       |       |       |   |   |___Token:Stream {id=2769,slot_type=Identifier}
            |   |   |       |       |       |   |   |___(?) {id=2770,slot_type=optional__classType_lfno_classOrInterfaceType_2}
            |   |   |       |       |       |   |       |___typeArguments {id=2771,slot_type=typeArguments}
            |   |   |       |       |       |   |           |___Token:< {id=2772,slot_type=LT}
            |   |   |       |       |       |   |           |___Token:T {id=2780,slot_type=[typeArgumentList,typeArgument,referenceType,classOrInterfaceType,altnt_block__classOrInterfaceType_3,classType_lfno_classOrInterfaceType,Identifier]}
            |   |   |       |       |       |   |           |___Token:> {id=2774,slot_type=GT}
            |   |   |       |       |       |   |___Token:stream {id=2765,slot_type=[variableDeclaratorId,Identifier]}
            |   |   |       |       |       |___Token:, {id=2760,slot_type=COMMA}
            |   |   |       |       |___formalParameter {id=2750,slot_type=[lastFormalParameter,formalParameter]}
            |   |   |       |           |___Token:T {id=2757,slot_type=[unannType,unannClassOrInterfaceType,altnt_block__unannClassOrInterfaceType_3,unannClassType_lfno_unannClassOrInterfaceType,Identifier]}
            |   |   |       |           |___Token:function {id=2753,slot_type=[variableDeclaratorId,Identifier]}
            |   |   |       |___Token:) {id=2745,slot_type=RPAREN}
            |   |   |___block {id=2736,slot_type=[methodBody,block]}
            |   |       |___Token:{ {id=2737,slot_type=LBRACE}
            |   |       |___Token:} {id=2738,slot_type=RBRACE}
            |   |___methodDeclaration {id=2709,slot_type=[classBodyDeclaration,methodDeclaration]}
            |   |   |___methodHeader {id=2710,slot_type=methodHeader}
            |   |   |   |___Token:int {id=2732,slot_type=[result,unannType,altnt_block__primitiveType_3,INT]}
            |   |   |   |___methodDeclarator {id=2716,slot_type=methodDeclarator}
            |   |   |       |___Token:getXByFrequency {id=2717,slot_type=Identifier}
            |   |   |       |___Token:( {id=2718,slot_type=LPAREN}
            |   |   |       |___(?) {id=2719,slot_type=optional__methodDeclarator_1}
            |   |   |       |   |___formalParameter {id=2724,slot_type=[formalParameterList,aux_rule__formalParameterList_3,lastFormalParameter,formalParameter]}
            |   |   |       |       |___Token:double {id=2729,slot_type=[unannType,altnt_block__primitiveType_3,DOUBLE]}
            |   |   |       |       |___Token:frequency {id=2727,slot_type=[variableDeclaratorId,Identifier]}
            |   |   |       |___Token:) {id=2720,slot_type=RPAREN}
            |   |   |___block {id=2712,slot_type=[methodBody,block]}
            |   |       |___Token:{ {id=2713,slot_type=LBRACE}
            |   |       |___Token:} {id=2714,slot_type=RBRACE}
            |   |___block {id=2511,slot_type=[classBodyDeclaration,instanceInitializer,block]}
            |       |___Token:{ {id=2512,slot_type=LBRACE}
            |       |___(?) {id=2513,slot_type=optional__block_1}
            |       |   |___(+) {id=2515,slot_type=blockStatements}
            |       |       |___localVariableDeclarationStatement {id=2699,slot_type=[blockStatement,localVariableDeclarationStatement]}
            |       |       |   |___localVariableDeclaration {id=2700,slot_type=localVariableDeclaration}
            |       |       |   |   |___Token:int {id=2708,slot_type=[unannType,altnt_block__primitiveType_3,INT]}
            |       |       |   |   |___Token:maxBpmIndex {id=2706,slot_type=[variableDeclaratorList,variableDeclarator,variableDeclaratorId,Identifier]}
            |       |       |   |___Token:; {id=2701,slot_type=SEMI}
            |       |       |___localVariableDeclarationStatement {id=2518,slot_type=[blockStatement,localVariableDeclarationStatement]}
            |       |           |___localVariableDeclaration {id=2519,slot_type=localVariableDeclaration}
            |       |           |   |___Token:int {id=2698,slot_type=[unannType,altnt_block__primitiveType_3,INT]}
            |       |           |   |___variableDeclarator {id=2523,slot_type=[variableDeclaratorList,variableDeclarator]}
            |       |           |       |___Token:xcur {id=2696,slot_type=[variableDeclaratorId,Identifier]}
            |       |           |       |___(?) {id=2525,slot_type=optional__variableDeclarator_2}
            |       |           |           |___aux_rule__variableDeclarator_1 {id=2526,slot_type=aux_rule__variableDeclarator_1}
            |       |           |               |___Token:= {id=2527,slot_type=ASSIGN}
            |       |           |               |___methodInvocation_lfno_primary {id=2548,slot_type=[variableInitializer,expression,conditionalExpression,conditionalOrExpression,conditionalAndExpression,inclusiveOrExpression,exclusiveOrExpression,andExpression,equalityExpression,relationalExpression,shiftExpression,additiveExpression,multiplicativeExpression,unaryExpression,aux_rule__unaryExpression_3,unaryExpressionNotPlusMinus,postfixExpression,altnt_block__postfixExpression_3,primary,altnt_block__primary_2,methodInvocation_lfno_primary]}
            |       |           |                   |___aux_rule__methodInvocation_lfno_primary_15 {id=2551,slot_type=[altnt_block__methodInvocation_lfno_primary_10,aux_rule__methodInvocation_lfno_primary_15]}
            |       |           |                   |   |___Token:getXByFrequency {id=2695,slot_type=[altnt_block__methodInvocation_lfno_primary_12,methodName,Identifier]}
            |       |           |                   |   |___Token:( {id=2553,slot_type=LPAREN}
            |       |           |                   |   |___(?) {id=2554,slot_type=optional__explicitConstructorInvocation_2}
            |       |           |                   |       |___methodInvocation_lfno_primary {id=2575,slot_type=[argumentList,expression,conditionalExpression,conditionalOrExpression,conditionalAndExpression,inclusiveOrExpression,exclusiveOrExpression,andExpression,equalityExpression,relationalExpression,shiftExpression,additiveExpression,multiplicativeExpression,unaryExpression,aux_rule__unaryExpression_3,unaryExpressionNotPlusMinus,postfixExpression,altnt_block__postfixExpression_3,primary,altnt_block__primary_2,methodInvocation_lfno_primary]}
            |       |           |                   |           |___aux_rule__methodInvocation_lfno_primary_15 {id=2578,slot_type=[altnt_block__methodInvocation_lfno_primary_10,aux_rule__methodInvocation_lfno_primary_15]}
            |       |           |                   |           |   |___Token:maxKey {id=2693,slot_type=[altnt_block__methodInvocation_lfno_primary_12,methodName,Identifier]}
            |       |           |                   |           |   |___Token:( {id=2580,slot_type=LPAREN}
            |       |           |                   |           |   |___(?) {id=2581,slot_type=optional__explicitConstructorInvocation_2}
            |       |           |                   |           |       |___argumentList {id=2582,slot_type=argumentList}
            |       |           |                   |           |           |___primary {id=2628,slot_type=[expression,conditionalExpression,conditionalOrExpression,conditionalAndExpression,inclusiveOrExpression,exclusiveOrExpression,andExpression,equalityExpression,relationalExpression,shiftExpression,additiveExpression,multiplicativeExpression,unaryExpression,aux_rule__unaryExpression_3,unaryExpressionNotPlusMinus,postfixExpression,altnt_block__postfixExpression_3,primary]}
            |       |           |                   |           |           |   |___methodInvocation_lfno_primary {id=2637,slot_type=[altnt_block__primary_2,methodInvocation_lfno_primary]}
            |       |           |                   |           |           |   |   |___aux_rule__methodInvocation_lfno_primary_14 {id=2640,slot_type=[altnt_block__methodInvocation_lfno_primary_10,aux_rule__methodInvocation_lfno_primary_14]}
            |       |           |                   |           |           |   |   |   |___Token:IntStream {id=2691,slot_type=[typeName,Identifier]}
            |       |           |                   |           |           |   |   |   |___Token:. {id=2642,slot_type=DOT}
            |       |           |                   |           |           |   |   |   |___altnt_block__methodInvocation_13 {id=2643,slot_type=altnt_block__methodInvocation_13}
            |       |           |                   |           |           |   |   |       |___Token:range {id=2644,slot_type=Identifier}
            |       |           |                   |           |           |   |   |       |___Token:( {id=2645,slot_type=LPAREN}
            |       |           |                   |           |           |   |   |       |___(?) {id=2646,slot_type=optional__explicitConstructorInvocation_2}
            |       |           |                   |           |           |   |   |           |___argumentList {id=2647,slot_type=argumentList}
            |       |           |                   |           |           |   |   |               |___Token:0 {id=2690,slot_type=[expression,conditionalExpression,conditionalOrExpression,conditionalAndExpression,inclusiveOrExpression,exclusiveOrExpression,andExpression,equalityExpression,relationalExpression,shiftExpression,additiveExpression,multiplicativeExpression,unaryExpression,aux_rule__unaryExpression_3,unaryExpressionNotPlusMinus,postfixExpression,altnt_block__postfixExpression_3,primary,altnt_block__primary_2,literal,IntegerLiteral]}
            |       |           |                   |           |           |   |   |               |___(*) {id=2649,slot_type=kleene_star__argumentList_2}
            |       |           |                   |           |           |   |   |                   |___aux_rule__argumentList_1 {id=2650,slot_type=aux_rule__argumentList_1}
            |       |           |                   |           |           |   |   |                       |___Token:, {id=2651,slot_type=COMMA}
            |       |           |                   |           |           |   |   |                       |___Token:maxBpmIndex {id=2670,slot_type=[expression,conditionalExpression,conditionalOrExpression,conditionalAndExpression,inclusiveOrExpression,exclusiveOrExpression,andExpression,equalityExpression,relationalExpression,shiftExpression,additiveExpression,multiplicativeExpression,unaryExpression,aux_rule__unaryExpression_3,unaryExpressionNotPlusMinus,postfixExpression,altnt_block__postfixExpression_3,typeName,Identifier]}
            |       |           |                   |           |           |   |   |___Token:) {id=2639,slot_type=RPAREN}
            |       |           |                   |           |           |   |___(*) {id=2630,slot_type=kleene_star__primary_1}
            |       |           |                   |           |           |       |___methodInvocation_lf_primary {id=2632,slot_type=[primaryNoNewArray_lf_primary,methodInvocation_lf_primary]}
            |       |           |                   |           |           |           |___Token:. {id=2633,slot_type=DOT}
            |       |           |                   |           |           |           |___Token:boxed {id=2634,slot_type=Identifier}
            |       |           |                   |           |           |           |___Token:( {id=2635,slot_type=LPAREN}
            |       |           |                   |           |           |           |___Token:) {id=2636,slot_type=RPAREN}
            |       |           |                   |           |           |___(*) {id=2584,slot_type=kleene_star__argumentList_2}
            |       |           |                   |           |               |___aux_rule__argumentList_1 {id=2585,slot_type=aux_rule__argumentList_1}
            |       |           |                   |           |                   |___Token:, {id=2586,slot_type=COMMA}
            |       |           |                   |           |                   |___lambdaExpression {id=2588,slot_type=[expression,lambdaExpression]}
            |       |           |                   |           |                       |___Token:i {id=2611,slot_type=[lambdaParameters,Identifier]}
            |       |           |                   |           |                       |___Token:-> {id=2590,slot_type=ARROW}
            |       |           |                   |           |                       |___Token:abs {id=2610,slot_type=[lambdaBody,expression,conditionalExpression,conditionalOrExpression,conditionalAndExpression,inclusiveOrExpression,exclusiveOrExpression,andExpression,equalityExpression,relationalExpression,shiftExpression,additiveExpression,multiplicativeExpression,unaryExpression,aux_rule__unaryExpression_3,unaryExpressionNotPlusMinus,postfixExpression,altnt_block__postfixExpression_3,typeName,Identifier]}
            |       |           |                   |           |___Token:) {id=2577,slot_type=RPAREN}
            |       |           |                   |___Token:) {id=2550,slot_type=RPAREN}
            |       |           |___Token:; {id=2520,slot_type=SEMI}
            |       |___Token:} {id=2514,slot_type=RBRACE}
            |___Token:} {id=2506,slot_type=RBRACE}
------------------------------------------------------------


================Reducing node 2497, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [kleene_star:kleene_star__compilationUnit_3]dd@1
    --file: Main.java--
    import java.util.stream.*;
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2497
------------------------------------------------------------


================Reducing node 2500, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


==============Node edit action set cache hit.===============

    NodeDeletionAction{target_node=2500}
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2500
------------------------------------------------------------


================Reducing node 2503, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2503
------------------------------------------------------------


================Reducing node 2505, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [kleene_star:kleene_star__classBody_1]dd@3
    --file: Main.java--
    import java.util.stream.*;
           class Main {
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
           class Main {
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [kleene_star:kleene_star__classBody_1]dd@1
    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [kleene_star:kleene_star__classBody_1]dd@2
    --file: Main.java--
    import java.util.stream.*;
           class Main {
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
           class Main {
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [kleene_star:kleene_star__classBody_1]dd@1
    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [kleene_star:kleene_star__classBody_1]dd@1
    --file: Main.java--
    import java.util.stream.*;
           class Main {
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
           class Main {
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2505
------------------------------------------------------------


================Reducing node 2511, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


==============Node edit action set cache hit.===============

    NodeDeletionAction{target_node=2511}
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2511
------------------------------------------------------------


================Reducing node 2513, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [kleene_star:optional__block_1]dd@1
    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2513
------------------------------------------------------------


================Reducing node 2515, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


==============Node edit action set cache hit.===============

    NodeDeletionAction{target_node=2515}
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [kleene_plus:blockStatements]remove whole except first
    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
        }
    }
------------------------------------------------------------


==============Node edit action set cache hit.===============

    NodeDeletionAction{target_node=2518}
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [kleene_plus:blockStatements]dd@1
    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2515
------------------------------------------------------------


================Reducing node 2518, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


==============Node edit action set cache hit.===============

    NodeDeletionAction{target_node=2518}
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2518
------------------------------------------------------------


================Reducing node 2519, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2519
------------------------------------------------------------


================Reducing node 2523, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [regular node]compatible replacement
    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur                                                                                                           ;
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur                                                                                                           ;
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [regular node]compatible replacement
    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int        getXByFrequency                                                                                         ;
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int        getXByFrequency                                                                                         ;
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2523
------------------------------------------------------------


================Reducing node 2525, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [kleene_star:optional__variableDeclarator_2]dd@1
    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur                                                                                                           ;
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur                                                                                                           ;
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2525
------------------------------------------------------------


================Reducing node 2526, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


==============Node edit action set cache hit.===============

    NodeDeletionAction{target_node=2526}
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2526
------------------------------------------------------------


================Reducing node 2548, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [regular node]compatible replacement
    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency                                                                                         ;
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency                                                                                         ;
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [regular node]compatible replacement
    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur =                 maxKey                                                                                  ;
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur =                 maxKey                                                                                  ;
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [regular node]compatible replacement
    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur =                 maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ) ;
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur =                 maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ) ;
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2548
------------------------------------------------------------


================Reducing node 2551, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [regular node]compatible replacement
    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur =                 maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                          );
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur =                 maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                          );
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2551
------------------------------------------------------------


================Reducing node 2554, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [kleene_star:optional__explicitConstructorInvocation_2]dd@1
    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(                                                                                       );
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(                                                                                       );
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2554
------------------------------------------------------------


================Reducing node 2575, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


==============Node edit action set cache hit.===============

    NodeDeletionAction{target_node=2575}
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [regular node]compatible replacement
    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey                                                                                 );
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey                                                                                 );
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [regular node]compatible replacement
    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(       IntStream.range(0, maxBpmIndex)                                                 );
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(       IntStream.range(0, maxBpmIndex)                                                 );
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [regular node]compatible replacement
    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(       IntStream.range(0, maxBpmIndex).boxed()                                         );
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(       IntStream.range(0, maxBpmIndex).boxed()                                         );
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [regular node]compatible replacement
    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(       IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                          );
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(       IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                          );
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2575
------------------------------------------------------------


================Reducing node 2578, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [regular node]compatible replacement
    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(       IntStream.range(0, maxBpmIndex                                                 ));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(       IntStream.range(0, maxBpmIndex                                                 ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2578
------------------------------------------------------------


================Reducing node 2733, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


==============Node edit action set cache hit.===============

    NodeDeletionAction{target_node=2733}
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [regular node]compatible replacement
    --file: Main.java--
    import java.util.stream.*;
           class Main {
                                                                                  {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
           class Main {
                                                                                  {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2733
------------------------------------------------------------


================Reducing node 2581, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [kleene_star:optional__explicitConstructorInvocation_2]dd@1
    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(                                                                               ));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(                                                                               ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2581
------------------------------------------------------------


================Reducing node 2582, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


==============Node edit action set cache hit.===============

    NodeDeletionAction{target_node=2582}
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [regular node]compatible replacement
    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream                                                                      ));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream                                                                      ));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [regular node]compatible replacement
    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(                                boxed                                          ));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(                                boxed                                          ));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [regular node]compatible replacement
    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(                                         i                                     ));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(                                         i                                     ));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [regular node]compatible replacement
    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(          range                                                                ));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(          range                                                                ));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [regular node]compatible replacement
    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(                                                   abs                         ));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(                                                   abs                         ));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [regular node]compatible replacement
    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(                                         i ->      abs                         ));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(                                         i ->      abs                         ));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [regular node]compatible replacement
    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex)                                                ));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex)                                                ));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [regular node]compatible replacement
    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed()                                        ));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed()                                        ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2582
------------------------------------------------------------


================Reducing node 2734, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2734
------------------------------------------------------------


================Reducing node 2628, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [regular node]compatible replacement
    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(          range                        , i ->      abs                         ));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(          range                        , i ->      abs                         ));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [regular node]compatible replacement
    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream                              , i ->      abs                         ));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream                              , i ->      abs                         ));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [regular node]compatible replacement
    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(                                boxed  , i ->      abs                         ));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(                                boxed  , i ->      abs                         ));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [regular node]compatible replacement
    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex)        , i ->      abs                         ));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex)        , i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2628
------------------------------------------------------------


================Reducing node 2741, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2741
------------------------------------------------------------


================Reducing node 2496, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [kleene_star:kleene_star__compilationUnit_2]dd@1
    --file: Main.java--
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
    --file: <formatted tokenized program in its original format>--
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2496
------------------------------------------------------------


================Reducing node 2794, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


==============Node edit action set cache hit.===============

    NodeDeletionAction{target_node=2794}
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2794
------------------------------------------------------------


================Reducing node 2744, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [kleene_star:optional__methodDeclarator_1]dd@1
    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(                                              ) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(                                              ) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2744
------------------------------------------------------------


================Reducing node 2747, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


==============Node edit action set cache hit.===============

    NodeDeletionAction{target_node=2747}
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [regular node]compatible replacement
    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(                           T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(                           T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [regular node]compatible replacement
    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream                              ) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream                              ) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2747
------------------------------------------------------------


================Reducing node 2709, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


==============Node edit action set cache hit.===============

    NodeDeletionAction{target_node=2709}
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [regular node]compatible replacement
    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                                                        {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                                                        {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2709
------------------------------------------------------------


================Reducing node 2637, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [regular node]compatible replacement
    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(                0              .boxed(), i ->      abs                         ));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(                0              .boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2637
------------------------------------------------------------


================Reducing node 2640, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2640
------------------------------------------------------------


================Reducing node 2748, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [kleene_star:optional__formalParameterList_2]dd@1
    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(                           T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(                           T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2748
------------------------------------------------------------


================Reducing node 2758, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


==============Node edit action set cache hit.===============

    NodeDeletionAction{target_node=2758}
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2758
------------------------------------------------------------


================Reducing node 2710, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2710
------------------------------------------------------------


================Reducing node 2796, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [regular node]compatible replacement
    --file: Main.java--
    import      util       .*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import      util       .*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [regular node]compatible replacement
    --file: Main.java--
    import java            .*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java            .*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [regular node]compatible replacement
    --file: Main.java--
    import           stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import           stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2796
------------------------------------------------------------


================Reducing node 2762, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2762
------------------------------------------------------------


================Reducing node 2716, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2716
------------------------------------------------------------


================Reducing node 2643, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2643
------------------------------------------------------------


================Reducing node 2801, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [kleene_star:kleene_star__packageName_1]dd@2
    --file: Main.java--
    import java            .*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java            .*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [kleene_star:kleene_star__packageName_1]dd@1
    --file: Main.java--
    import java.util       .*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util       .*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [kleene_star:kleene_star__packageName_1]dd@1
    --file: Main.java--
    import java     .stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java     .stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2801
------------------------------------------------------------


================Reducing node 2768, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [regular node]compatible replacement
    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream    stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream    stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [regular node]compatible replacement
    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(       T  stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(       T  stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2768
------------------------------------------------------------


================Reducing node 2630, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [kleene_star:kleene_star__primary_1]dd@1
    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex)        , i ->      abs                         ));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex)        , i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2630
------------------------------------------------------------


================Reducing node 2632, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


==============Node edit action set cache hit.===============

    NodeDeletionAction{target_node=2632}
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2632
------------------------------------------------------------


================Reducing node 2584, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [kleene_star:kleene_star__argumentList_2]dd@1
    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed()                                        ));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed()                                        ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2584
------------------------------------------------------------


================Reducing node 2585, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


==============Node edit action set cache hit.===============

    NodeDeletionAction{target_node=2585}
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2585
------------------------------------------------------------


================Reducing node 2770, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [kleene_star:optional__classType_lfno_classOrInterfaceType_2]dd@1
    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream    stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream    stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2770
------------------------------------------------------------


================Reducing node 2771, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


==============Node edit action set cache hit.===============

    NodeDeletionAction{target_node=2771}
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2771
------------------------------------------------------------


================Reducing node 2739, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [kleene_star:optional__methodHeader_5]dd@1
    --file: Main.java--
    import java.util.stream.*;
           class Main {
                         T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
           class Main {
                         T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2739
------------------------------------------------------------


================Reducing node 2787, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


==============Node edit action set cache hit.===============

    NodeDeletionAction{target_node=2787}
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2787
------------------------------------------------------------


================Reducing node 2699, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


==============Node edit action set cache hit.===============

    NodeDeletionAction{target_node=2699}
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2699
------------------------------------------------------------


================Reducing node 2646, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [kleene_star:optional__explicitConstructorInvocation_2]dd@1
    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(              ).boxed(), i ->      abs                         ));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(              ).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2646
------------------------------------------------------------


================Reducing node 2647, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


==============Node edit action set cache hit.===============

    NodeDeletionAction{target_node=2647}
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [regular node]compatible replacement
    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0             ).boxed(), i ->      abs                         ));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0             ).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [regular node]compatible replacement
    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(   maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(   maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2647
------------------------------------------------------------


================Reducing node 2588, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [regular node]compatible replacement
    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(),           abs                         ));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(),           abs                         ));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [regular node]compatible replacement
    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i                                     ));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i                                     ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2588
------------------------------------------------------------


================Reducing node 2803, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


==============Node edit action set cache hit.===============

    NodeDeletionAction{target_node=2803}
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2803
------------------------------------------------------------


================Reducing node 2802, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


==============Node edit action set cache hit.===============

    NodeDeletionAction{target_node=2802}
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2802
------------------------------------------------------------


================Reducing node 2750, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2750
------------------------------------------------------------


================Reducing node 2736, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2736
------------------------------------------------------------


================Reducing node 2719, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [kleene_star:optional__methodDeclarator_1]dd@1
    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(                ) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(                ) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2719
------------------------------------------------------------


================Reducing node 2724, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


==============Node edit action set cache hit.===============

    NodeDeletionAction{target_node=2724}
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2724
------------------------------------------------------------


================Reducing node 2712, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2712
------------------------------------------------------------


================Reducing node 2700, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2700
------------------------------------------------------------


================Reducing node 2649, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


============Testing the following program: fail=============

// edit action set type: [kleene_star:kleene_star__argumentList_2]dd@1
    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0             ).boxed(), i ->      abs                         ));
        }
    }
    --file: <formatted tokenized program in its original format>--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0             ).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2649
------------------------------------------------------------


================Reducing node 2650, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


==============Node edit action set cache hit.===============

    NodeDeletionAction{target_node=2650}
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2650
------------------------------------------------------------


================Reducing node 2807, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2807
------------------------------------------------------------


================Reducing node 2806, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2806
------------------------------------------------------------


================Reducing node 2805, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2805
------------------------------------------------------------


================Reducing node 2804, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2804
------------------------------------------------------------


================Reducing node 2800, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2800
------------------------------------------------------------


================Reducing node 2799, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2799
------------------------------------------------------------


================Reducing node 2798, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2798
------------------------------------------------------------


================Reducing node 2797, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2797
------------------------------------------------------------


================Reducing node 2795, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2795
------------------------------------------------------------


================Reducing node 2792, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2792
------------------------------------------------------------


================Reducing node 2790, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2790
------------------------------------------------------------


================Reducing node 2788, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2788
------------------------------------------------------------


================Reducing node 2785, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2785
------------------------------------------------------------


================Reducing node 2780, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2780
------------------------------------------------------------


================Reducing node 2774, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2774
------------------------------------------------------------


================Reducing node 2772, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2772
------------------------------------------------------------


================Reducing node 2769, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2769
------------------------------------------------------------


================Reducing node 2765, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2765
------------------------------------------------------------


================Reducing node 2760, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2760
------------------------------------------------------------


================Reducing node 2757, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2757
------------------------------------------------------------


================Reducing node 2753, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2753
------------------------------------------------------------


================Reducing node 2745, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2745
------------------------------------------------------------


================Reducing node 2743, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2743
------------------------------------------------------------


================Reducing node 2742, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2742
------------------------------------------------------------


================Reducing node 2738, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2738
------------------------------------------------------------


================Reducing node 2737, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2737
------------------------------------------------------------


================Reducing node 2732, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2732
------------------------------------------------------------


================Reducing node 2729, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2729
------------------------------------------------------------


================Reducing node 2727, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2727
------------------------------------------------------------


================Reducing node 2720, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2720
------------------------------------------------------------


================Reducing node 2718, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2718
------------------------------------------------------------


================Reducing node 2717, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2717
------------------------------------------------------------


================Reducing node 2714, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2714
------------------------------------------------------------


================Reducing node 2713, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2713
------------------------------------------------------------


================Reducing node 2708, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2708
------------------------------------------------------------


================Reducing node 2706, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2706
------------------------------------------------------------


================Reducing node 2701, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2701
------------------------------------------------------------


================Reducing node 2698, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2698
------------------------------------------------------------


================Reducing node 2696, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2696
------------------------------------------------------------


================Reducing node 2695, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2695
------------------------------------------------------------


================Reducing node 2693, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2693
------------------------------------------------------------


================Reducing node 2691, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2691
------------------------------------------------------------


================Reducing node 2690, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2690
------------------------------------------------------------


================Reducing node 2670, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2670
------------------------------------------------------------


================Reducing node 2651, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2651
------------------------------------------------------------


================Reducing node 2645, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2645
------------------------------------------------------------


================Reducing node 2644, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2644
------------------------------------------------------------


================Reducing node 2642, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2642
------------------------------------------------------------


================Reducing node 2639, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2639
------------------------------------------------------------


================Reducing node 2636, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2636
------------------------------------------------------------


================Reducing node 2635, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2635
------------------------------------------------------------


================Reducing node 2634, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2634
------------------------------------------------------------


================Reducing node 2633, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2633
------------------------------------------------------------


================Reducing node 2611, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2611
------------------------------------------------------------


================Reducing node 2610, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2610
------------------------------------------------------------


================Reducing node 2590, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2590
------------------------------------------------------------


================Reducing node 2586, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2586
------------------------------------------------------------


================Reducing node 2580, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2580
------------------------------------------------------------


================Reducing node 2577, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2577
------------------------------------------------------------


================Reducing node 2553, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2553
------------------------------------------------------------


================Reducing node 2550, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2550
------------------------------------------------------------


================Reducing node 2527, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2527
------------------------------------------------------------


================Reducing node 2520, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2520
------------------------------------------------------------


================Reducing node 2514, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2514
------------------------------------------------------------


================Reducing node 2512, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2512
------------------------------------------------------------


================Reducing node 2506, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2506
------------------------------------------------------------


================Reducing node 2504, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2504
------------------------------------------------------------


================Reducing node 2502, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2502
------------------------------------------------------------


================Reducing node 2501, size=69=================

The current best program is the following

    --file: Main.java--
    import java.util.stream.*;
           class Main {
                      <T>T maxKey(Stream<T> stream,          T          function) {
        }
                  int getXByFrequency(double frequency) {
        }
                   {
            int maxBpmIndex    ;
            int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
        }
    }
------------------------------------------------------------


===================Node reduction is done===================

Failed to reduce node 2501
------------------------------------------------------------


The history of the reducer invocation.
[0]: StatsSnapshotEvent
---
stats:
  tokenCount: 447
  characterCount: 1266
  fileContents:
  - fileName: "Main.java"
    contentDigest:
      digest: {}
      numOfStrings: 1
numberOfNonDeletionIterations: 0
fileContentChangedWrtPrevious: false

[1]: ReducerCallEvent
---
reducer:
  shortName: "perses_node_priority_with_dfs_delta"
  description: ""
  deterministic: true
  reductionResultSizeTrend: "BEST_RESULT_SIZE_DECREASE"

[2]: StatsSnapshotEvent
---
stats:
  tokenCount: 80
  characterCount: 242
  fileContents:
  - fileName: "Main.java"
    contentDigest:
      digest: {}
      numOfStrings: 1
numberOfNonDeletionIterations: 0
fileContentChangedWrtPrevious: true

[3]: ReducerCallEvent
---
reducer:
  shortName: "perses_node_priority_with_dfs_delta"
  description: ""
  deterministic: true
  reductionResultSizeTrend: "BEST_RESULT_SIZE_DECREASE"

[4]: StatsSnapshotEvent
---
stats:
  tokenCount: 69
  characterCount: 208
  fileContents:
  - fileName: "Main.java"
    contentDigest:
      digest: {}
      numOfStrings: 1
numberOfNonDeletionIterations: 0
fileContentChangedWrtPrevious: true

[5]: ReducerCallEvent
---
reducer:
  shortName: "perses_node_priority_with_dfs_delta"
  description: ""
  deterministic: true
  reductionResultSizeTrend: "BEST_RESULT_SIZE_DECREASE"

[6]: StatsSnapshotEvent
---
stats:
  tokenCount: 69
  characterCount: 208
  fileContents:
  - fileName: "Main.java"
    contentDigest:
      digest: {}
      numOfStrings: 1
numberOfNonDeletionIterations: 0
fileContentChangedWrtPrevious: false


#test success = 22
#test failure = 228
#test result cache hits = 0
#test execution cancelled = 17
#node edit action set cache hits = 58
#external test execution cache hits = 0
