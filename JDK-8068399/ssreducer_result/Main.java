import java.util.function.Function;
import java.util.stream.IntStream;
import java.util.stream.Stream;

public class Main {

    public static class Ref {

        }

    public static <T>T maxKey(Stream<T> stream, Function<T, Double> function) {

        }

    protected int getXByFrequency(double frequency) {
        }

    void foo() {
        getXByFrequency(maxKey(IntStream.range(0, 1).boxed(), i -> Math.abs(((double[]) null)[(java.lang.Integer) null])));
    }
}