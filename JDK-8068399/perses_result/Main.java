import java.util.stream.*;
       class Main {
                  <T>T maxKey(Stream<T> stream,          T          function) {
    }
              int getXByFrequency(double frequency) {
    }
               {
        int maxBpmIndex    ;
        int xcur = getXByFrequency(maxKey(IntStream.range(0, maxBpmIndex).boxed(), i ->      abs                         ));
    }
}
