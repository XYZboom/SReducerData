import java.util.stream.*;
       class a    {
                  <b>b b     (Stream<b> b     ,          b          a       ) {
    }
              int a              (double a        ) {
    }
               {
        int b    = a              (b     (IntStream.range(0, b          ).boxed(), a -> a                                ));
    }
}
