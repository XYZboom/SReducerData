import java.util.Map;
import java.util.stream.Collectors;
             class a       {
         static Map<a     , a     > a                       () {
    return                       null;
  }
                       {
    a      .a                       ().entrySet().stream()
      .map(a ->               ""                                )
      .collect(Collectors.joining(    ));
  }
}
