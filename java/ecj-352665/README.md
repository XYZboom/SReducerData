There are three source code files. 
Since perses can only reduce one file at a time, it is used to reduce ProfileManager.java, 
which is included in the crash log, while ssreducer can reduce multiple files simultaneously.